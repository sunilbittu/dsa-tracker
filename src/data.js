// Auto-generated from uploads/tuf_dsa_checklist.json
export const CATEGORIES = [
  {
    "id": "arrays",
    "name": "Arrays",
    "total": 24
  },
  {
    "id": "binary-search",
    "name": "Binary Search",
    "total": 28
  },
  {
    "id": "recursion",
    "name": "Recursion",
    "total": 17
  },
  {
    "id": "linked-list",
    "name": "Linked List",
    "total": 44
  }
];
export const PROBLEMS = [{"id":1,"title":"Majority Element-I","category":"arrays","categoryName":"Arrays","topic":"Arrays Boyer-Moore Voting Algorithm","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":85,"tags":["arrays","boyer-moore","majority-element"],"companies":["Amazon","Microsoft","Google"],"concept":"Find element appearing more than n/2 times using constant space","explanation":"Use Boyer-Moore Voting Algorithm. Maintain a candidate and count. When count reaches 0, pick new candidate. The candidate with count > 0 at the end is the majority element.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function majorityElement(nums) {\n  let candidate = null, count = 0;\n  for (let num of nums) {\n    if (count === 0) candidate = num;\n    count += (num === candidate) ? 1 : -1;\n  }\n  return candidate;\n}","python":"def majorityElement(nums):\n    candidate, count = None, 0\n    for num in nums:\n        if count == 0:\n            candidate = num\n        count += (1 if num == candidate else -1)\n    return candidate","java":"public int majorityElement(int[] nums) {\n  int candidate = 0, count = 0;\n  for (int num : nums) {\n    if (count == 0) candidate = num;\n    count += (num == candidate) ? 1 : -1;\n  }\n  return candidate;\n}","cpp":"int majorityElement(vector<int>& nums) {\n  int candidate = 0, count = 0;\n  for (int num : nums) {\n    if (count == 0) candidate = num;\n    count += (num == candidate) ? 1 : -1;\n  }\n  return candidate;\n}"},"challenge":"Find majority element in O(1) space","hints":["Use voting algorithm","Track candidate and count","Reset when count reaches zero"],"relatedProblems":[15]},{"id":2,"title":"Leaders in an Array","category":"arrays","categoryName":"Arrays","topic":"Arrays Traversal","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":60,"tags":["arrays","leaders","traversal"],"companies":["Adobe","Samsung"],"concept":"Find all elements greater than all elements to their right","explanation":"Traverse from right to left, keeping track of maximum seen so far. An element is a leader if it's greater than all elements to its right.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function leaders(arr) {\n  const result = [];\n  let maxFromRight = -Infinity;\n  for (let i = arr.length - 1; i >= 0; i--) {\n    if (arr[i] > maxFromRight) {\n      result.push(arr[i]);\n      maxFromRight = arr[i];\n    }\n  }\n  return result.reverse();\n}","python":"def leaders(arr):\n    result = []\n    max_from_right = float('-inf')\n    for i in range(len(arr) - 1, -1, -1):\n        if arr[i] > max_from_right:\n            result.append(arr[i])\n            max_from_right = arr[i]\n    return result[::-1]","java":"public List<Integer> leaders(int[] arr) {\n  List<Integer> result = new ArrayList<>();\n  int maxFromRight = Integer.MIN_VALUE;\n  for (int i = arr.length - 1; i >= 0; i--) {\n    if (arr[i] > maxFromRight) {\n      result.add(arr[i]);\n      maxFromRight = arr[i];\n    }\n  }\n  Collections.reverse(result);\n  return result;\n}","cpp":"vector<int> leaders(vector<int>& arr) {\n  vector<int> result;\n  int maxFromRight = INT_MIN;\n  for (int i = arr.size() - 1; i >= 0; i--) {\n    if (arr[i] > maxFromRight) {\n      result.push_back(arr[i]);\n      maxFromRight = arr[i];\n    }\n  }\n  reverse(result.begin(), result.end());\n  return result;\n}"},"challenge":"Find leaders in single traversal","hints":["Traverse from right","Track maximum so far","Compare current element"],"relatedProblems":[]},{"id":3,"title":"Rearrange array elements by sign","category":"arrays","categoryName":"Arrays","topic":"Arrays Two Pointers","difficulty":"medium","status":"solved","timeSpent":34,"attempts":4,"lastTouched":4,"frequency":65,"tags":["arrays","two-pointers","rearrange"],"companies":["Amazon"],"concept":"Rearrange array so positive and negative numbers alternate","explanation":"Use two pointers or separate arrays to place positive and negative numbers at alternate positions. Positive numbers at even indices, negative at odd indices.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function rearrangeBySign(nums) {\n  const pos = nums.filter(x => x > 0);\n  const neg = nums.filter(x => x < 0);\n  const result = [];\n  for (let i = 0; i < pos.length; i++) {\n    result.push(pos[i], neg[i]);\n  }\n  return result;\n}","python":"def rearrangeBySign(nums):\n    pos = [x for x in nums if x > 0]\n    neg = [x for x in nums if x < 0]\n    result = []\n    for i in range(len(pos)):\n        result.extend([pos[i], neg[i]])\n    return result","java":"public int[] rearrangeBySign(int[] nums) {\n  List<Integer> pos = new ArrayList<>();\n  List<Integer> neg = new ArrayList<>();\n  for (int num : nums) {\n    if (num > 0) pos.add(num);\n    else neg.add(num);\n  }\n  int[] result = new int[nums.length];\n  int idx = 0;\n  for (int i = 0; i < pos.size(); i++) {\n    result[idx++] = pos.get(i);\n    result[idx++] = neg.get(i);\n  }\n  return result;\n}","cpp":"vector<int> rearrangeBySign(vector<int>& nums) {\n  vector<int> pos, neg, result;\n  for (int num : nums) {\n    if (num > 0) pos.push_back(num);\n    else neg.push_back(num);\n  }\n  for (int i = 0; i < pos.size(); i++) {\n    result.push_back(pos[i]);\n    result.push_back(neg[i]);\n  }\n  return result;\n}"},"challenge":"Rearrange without extra space (variation)","hints":["Separate positives and negatives","Merge alternately","Handle unequal counts"],"relatedProblems":[]},{"id":4,"title":"Print the matrix in spiral manner","category":"arrays","categoryName":"Arrays","topic":"Arrays Matrix","difficulty":"medium","status":"solved","timeSpent":40,"attempts":3,"lastTouched":6,"frequency":90,"tags":["arrays","matrix","spiral"],"companies":["Microsoft","Amazon","Adobe"],"concept":"Traverse matrix in spiral order using boundary pointers","explanation":"Use four pointers for top, bottom, left, right boundaries. Traverse right, then down, then left, then up, shrinking boundaries after each direction.","complexity":{"time":"O(m*n)","space":"O(1)"},"code":{"javascript":"function spiralOrder(matrix) {\n  const result = [];\n  let top = 0, bottom = matrix.length - 1;\n  let left = 0, right = matrix[0].length - 1;\n  while (top <= bottom && left <= right) {\n    for (let i = left; i <= right; i++) result.push(matrix[top][i]);\n    top++;\n    for (let i = top; i <= bottom; i++) result.push(matrix[i][right]);\n    right--;\n    if (top <= bottom) {\n      for (let i = right; i >= left; i--) result.push(matrix[bottom][i]);\n      bottom--;\n    }\n    if (left <= right) {\n      for (let i = bottom; i >= top; i--) result.push(matrix[i][left]);\n      left++;\n    }\n  }\n  return result;\n}","python":"def spiralOrder(matrix):\n    result = []\n    top, bottom = 0, len(matrix) - 1\n    left, right = 0, len(matrix[0]) - 1\n    while top <= bottom and left <= right:\n        for i in range(left, right + 1):\n            result.append(matrix[top][i])\n        top += 1\n        for i in range(top, bottom + 1):\n            result.append(matrix[i][right])\n        right -= 1\n        if top <= bottom:\n            for i in range(right, left - 1, -1):\n                result.append(matrix[bottom][i])\n            bottom -= 1\n        if left <= right:\n            for i in range(bottom, top - 1, -1):\n                result.append(matrix[i][left])\n            left += 1\n    return result","java":"public List<Integer> spiralOrder(int[][] matrix) {\n  List<Integer> result = new ArrayList<>();\n  int top = 0, bottom = matrix.length - 1;\n  int left = 0, right = matrix[0].length - 1;\n  while (top <= bottom && left <= right) {\n    for (int i = left; i <= right; i++) result.add(matrix[top][i]);\n    top++;\n    for (int i = top; i <= bottom; i++) result.add(matrix[i][right]);\n    right--;\n    if (top <= bottom) {\n      for (int i = right; i >= left; i--) result.add(matrix[bottom][i]);\n      bottom--;\n    }\n    if (left <= right) {\n      for (int i = bottom; i >= top; i--) result.add(matrix[i][left]);\n      left++;\n    }\n  }\n  return result;\n}","cpp":"vector<int> spiralOrder(vector<vector<int>>& matrix) {\n  vector<int> result;\n  int top = 0, bottom = matrix.size() - 1;\n  int left = 0, right = matrix[0].size() - 1;\n  while (top <= bottom && left <= right) {\n    for (int i = left; i <= right; i++) result.push_back(matrix[top][i]);\n    top++;\n    for (int i = top; i <= bottom; i++) result.push_back(matrix[i][right]);\n    right--;\n    if (top <= bottom) {\n      for (int i = right; i >= left; i--) result.push_back(matrix[bottom][i]);\n      bottom--;\n    }\n    if (left <= right) {\n      for (int i = bottom; i >= top; i--) result.push_back(matrix[i][left]);\n      left++;\n    }\n  }\n  return result;\n}"},"challenge":"Handle non-square matrices correctly","hints":["Use four boundaries","Shrink boundaries after each direction","Check boundary conditions"],"relatedProblems":[8]},{"id":5,"title":"Pascal's Triangle I","category":"arrays","categoryName":"Arrays","topic":"Arrays Dynamic Programming","difficulty":"easy","status":"solved","timeSpent":66,"attempts":2,"lastTouched":5,"frequency":75,"tags":["arrays","pascal-triangle","dp"],"companies":["Amazon","Microsoft"],"concept":"Generate Pascal's triangle up to n rows","explanation":"Generate Pascal's triangle row by row. Each element is sum of two elements above it. Use previous row to build current row.","complexity":{"time":"O(n²)","space":"O(n²)"},"code":{"javascript":"function generate(numRows) {\n  const result = [[1]];\n  for (let i = 1; i < numRows; i++) {\n    const row = [1];\n    const prev = result[i - 1];\n    for (let j = 1; j < prev.length; j++) {\n      row.push(prev[j - 1] + prev[j]);\n    }\n    row.push(1);\n    result.push(row);\n  }\n  return result;\n}","python":"def generate(numRows):\n    result = [[1]]\n    for i in range(1, numRows):\n        row = [1]\n        prev = result[i - 1]\n        for j in range(1, len(prev)):\n            row.append(prev[j - 1] + prev[j])\n        row.append(1)\n        result.append(row)\n    return result","java":"public List<List<Integer>> generate(int numRows) {\n  List<List<Integer>> result = new ArrayList<>();\n  result.add(Arrays.asList(1));\n  for (int i = 1; i < numRows; i++) {\n    List<Integer> row = new ArrayList<>();\n    List<Integer> prev = result.get(i - 1);\n    row.add(1);\n    for (int j = 1; j < prev.size(); j++) {\n      row.add(prev.get(j - 1) + prev.get(j));\n    }\n    row.add(1);\n    result.add(row);\n  }\n  return result;\n}","cpp":"vector<vector<int>> generate(int numRows) {\n  vector<vector<int>> result = {{1}};\n  for (int i = 1; i < numRows; i++) {\n    vector<int> row = {1};\n    vector<int>& prev = result[i - 1];\n    for (int j = 1; j < prev.size(); j++) {\n      row.push_back(prev[j - 1] + prev[j]);\n    }\n    row.push_back(1);\n    result.push_back(row);\n  }\n  return result;\n}"},"challenge":"Generate only specific row efficiently","hints":["Each row starts and ends with 1","Middle elements sum of above two","Build row by row"],"relatedProblems":[6,7]},{"id":6,"title":"Pascal's Triangle II","category":"arrays","categoryName":"Arrays","topic":"Arrays Math","difficulty":"easy","status":"attempted","timeSpent":27,"attempts":1,"lastTouched":21,"frequency":70,"tags":["arrays","pascal-triangle","math"],"companies":["Amazon"],"concept":"Generate only the nth row of Pascal's triangle","explanation":"Generate only the nth row of Pascal's triangle using the formula C(n,k) = C(n,k-1) * (n-k+1)/k to avoid storing all rows.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function getRow(rowIndex) {\n  const row = [1];\n  for (let i = 1; i <= rowIndex; i++) {\n    row.push(row[row.length - 1] * (rowIndex - i + 1) / i);\n  }\n  return row;\n}","python":"def getRow(rowIndex):\n    row = [1]\n    for i in range(1, rowIndex + 1):\n        row.append(row[-1] * (rowIndex - i + 1) // i)\n    return row","java":"public List<Integer> getRow(int rowIndex) {\n  List<Integer> row = new ArrayList<>();\n  row.add(1);\n  for (int i = 1; i <= rowIndex; i++) {\n    row.add((int)((long)row.get(row.size() - 1) * (rowIndex - i + 1) / i));\n  }\n  return row;\n}","cpp":"vector<int> getRow(int rowIndex) {\n  vector<int> row = {1};\n  for (int i = 1; i <= rowIndex; i++) {\n    row.push_back((long long)row.back() * (rowIndex - i + 1) / i);\n  }\n  return row;\n}"},"challenge":"Solve in O(k) space","hints":["Use mathematical formula","Build row iteratively","Avoid storing all rows"],"relatedProblems":[5,7]},{"id":7,"title":"Pascal's Triangle III","category":"arrays","categoryName":"Arrays","topic":"Arrays Combinatorics","difficulty":"medium","status":"solved","timeSpent":28,"attempts":1,"lastTouched":26,"frequency":45,"tags":["arrays","combinatorics","math"],"companies":[],"concept":"Find element at specific position in Pascal's triangle","explanation":"Find element at specific position in Pascal's triangle using combination formula C(n,r) = n!/(r!(n-r)!).","complexity":{"time":"O(r)","space":"O(1)"},"code":{"javascript":"function nCr(n, r) {\n  let res = 1;\n  for (let i = 0; i < r; i++) {\n    res = res * (n - i) / (i + 1);\n  }\n  return res;\n}","python":"def nCr(n, r):\n    res = 1\n    for i in range(r):\n        res = res * (n - i) // (i + 1)\n    return res","java":"public int nCr(int n, int r) {\n  long res = 1;\n  for (int i = 0; i < r; i++) {\n    res = res * (n - i) / (i + 1);\n  }\n  return (int)res;\n}","cpp":"int nCr(int n, int r) {\n  long long res = 1;\n  for (int i = 0; i < r; i++) {\n    res = res * (n - i) / (i + 1);\n  }\n  return (int)res;\n}"},"challenge":"Handle large numbers with modulo","hints":["Use nCr formula","Optimize calculation","Avoid overflow"],"relatedProblems":[5,6]},{"id":8,"title":"Rotate matrix by 90 degrees","category":"arrays","categoryName":"Arrays","topic":"Arrays Matrix","difficulty":"medium","status":"solved","timeSpent":60,"attempts":2,"lastTouched":5,"frequency":88,"tags":["arrays","matrix","rotation"],"companies":["Microsoft","Amazon","Facebook"],"concept":"Rotate matrix 90 degrees clockwise in-place","explanation":"First transpose the matrix (swap rows with columns), then reverse each row to achieve 90-degree clockwise rotation.","complexity":{"time":"O(n²)","space":"O(1)"},"code":{"javascript":"function rotate(matrix) {\n  const n = matrix.length;\n  // Transpose\n  for (let i = 0; i < n; i++) {\n    for (let j = i; j < n; j++) {\n      [matrix[i][j], matrix[j][i]] = [matrix[j][i], matrix[i][j]];\n    }\n  }\n  // Reverse each row\n  for (let i = 0; i < n; i++) {\n    matrix[i].reverse();\n  }\n}","python":"def rotate(matrix):\n    n = len(matrix)\n    # Transpose\n    for i in range(n):\n        for j in range(i, n):\n            matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]\n    # Reverse each row\n    for i in range(n):\n        matrix[i].reverse()","java":"public void rotate(int[][] matrix) {\n  int n = matrix.length;\n  // Transpose\n  for (int i = 0; i < n; i++) {\n    for (int j = i; j < n; j++) {\n      int temp = matrix[i][j];\n      matrix[i][j] = matrix[j][i];\n      matrix[j][i] = temp;\n    }\n  }\n  // Reverse each row\n  for (int i = 0; i < n; i++) {\n    for (int j = 0; j < n / 2; j++) {\n      int temp = matrix[i][j];\n      matrix[i][j] = matrix[i][n - 1 - j];\n      matrix[i][n - 1 - j] = temp;\n    }\n  }\n}","cpp":"void rotate(vector<vector<int>>& matrix) {\n  int n = matrix.size();\n  // Transpose\n  for (int i = 0; i < n; i++) {\n    for (int j = i; j < n; j++) {\n      swap(matrix[i][j], matrix[j][i]);\n    }\n  }\n  // Reverse each row\n  for (int i = 0; i < n; i++) {\n    reverse(matrix[i].begin(), matrix[i].end());\n  }\n}"},"challenge":"Rotate in-place without extra matrix","hints":["Transpose first","Then reverse rows","In-place swapping"],"relatedProblems":[4]},{"id":9,"title":"Two Sum","category":"arrays","categoryName":"Arrays","topic":"Arrays Hashing","difficulty":"easy","status":"revisit","timeSpent":31,"attempts":3,"lastTouched":18,"frequency":100,"tags":["arrays","hashing","two-sum"],"companies":["Amazon","Google","Microsoft","Facebook","Apple"],"concept":"Find two numbers that add up to target","explanation":"Use hash map to store complement of each element. For each number, check if its complement exists in the map.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function twoSum(nums, target) {\n  const map = new Map();\n  for (let i = 0; i < nums.length; i++) {\n    const complement = target - nums[i];\n    if (map.has(complement)) {\n      return [map.get(complement), i];\n    }\n    map.set(nums[i], i);\n  }\n  return [];\n}","python":"def twoSum(nums, target):\n    seen = {}\n    for i, num in enumerate(nums):\n        complement = target - num\n        if complement in seen:\n            return [seen[complement], i]\n        seen[num] = i\n    return []","java":"public int[] twoSum(int[] nums, int target) {\n  Map<Integer, Integer> map = new HashMap<>();\n  for (int i = 0; i < nums.length; i++) {\n    int complement = target - nums[i];\n    if (map.containsKey(complement)) {\n      return new int[]{map.get(complement), i};\n    }\n    map.put(nums[i], i);\n  }\n  return new int[]{};\n}","cpp":"vector<int> twoSum(vector<int>& nums, int target) {\n  unordered_map<int, int> map;\n  for (int i = 0; i < nums.size(); i++) {\n    int complement = target - nums[i];\n    if (map.find(complement) != map.end()) {\n      return {map[complement], i};\n    }\n    map[nums[i]] = i;\n  }\n  return {};\n}"},"challenge":"Solve in O(n log n) without extra space","hints":["Use hash map for O(n)","Store complement","Two-pointer for sorted array"],"relatedProblems":[10,11]},{"id":10,"title":"3 Sum","category":"arrays","categoryName":"Arrays","topic":"Arrays Two Pointers","difficulty":"medium","status":"solved","timeSpent":20,"attempts":2,"lastTouched":1,"frequency":95,"tags":["arrays","two-pointers","three-sum"],"companies":["Facebook","Amazon","Microsoft","Google"],"concept":"Find all unique triplets that sum to zero","explanation":"Sort array first, then use two-pointer technique. Fix one element and find pair with sum equal to target minus fixed element.","complexity":{"time":"O(n²)","space":"O(1)"},"code":{"javascript":"function threeSum(nums) {\n  nums.sort((a, b) => a - b);\n  const result = [];\n  for (let i = 0; i < nums.length - 2; i++) {\n    if (i > 0 && nums[i] === nums[i - 1]) continue;\n    let left = i + 1, right = nums.length - 1;\n    while (left < right) {\n      const sum = nums[i] + nums[left] + nums[right];\n      if (sum === 0) {\n        result.push([nums[i], nums[left], nums[right]]);\n        while (left < right && nums[left] === nums[left + 1]) left++;\n        while (left < right && nums[right] === nums[right - 1]) right--;\n        left++;\n        right--;\n      } else if (sum < 0) {\n        left++;\n      } else {\n        right--;\n      }\n    }\n  }\n  return result;\n}","python":"def threeSum(nums):\n    nums.sort()\n    result = []\n    for i in range(len(nums) - 2):\n        if i > 0 and nums[i] == nums[i - 1]:\n            continue\n        left, right = i + 1, len(nums) - 1\n        while left < right:\n            total = nums[i] + nums[left] + nums[right]\n            if total == 0:\n                result.append([nums[i], nums[left], nums[right]])\n                while left < right and nums[left] == nums[left + 1]:\n                    left += 1\n                while left < right and nums[right] == nums[right - 1]:\n                    right -= 1\n                left += 1\n                right -= 1\n            elif total < 0:\n                left += 1\n            else:\n                right -= 1\n    return result","java":"public List<List<Integer>> threeSum(int[] nums) {\n  Arrays.sort(nums);\n  List<List<Integer>> result = new ArrayList<>();\n  for (int i = 0; i < nums.length - 2; i++) {\n    if (i > 0 && nums[i] == nums[i - 1]) continue;\n    int left = i + 1, right = nums.length - 1;\n    while (left < right) {\n      int sum = nums[i] + nums[left] + nums[right];\n      if (sum == 0) {\n        result.add(Arrays.asList(nums[i], nums[left], nums[right]));\n        while (left < right && nums[left] == nums[left + 1]) left++;\n        while (left < right && nums[right] == nums[right - 1]) right--;\n        left++;\n        right--;\n      } else if (sum < 0) {\n        left++;\n      } else {\n        right--;\n      }\n    }\n  }\n  return result;\n}","cpp":"vector<vector<int>> threeSum(vector<int>& nums) {\n  sort(nums.begin(), nums.end());\n  vector<vector<int>> result;\n  for (int i = 0; i < nums.size() - 2; i++) {\n    if (i > 0 && nums[i] == nums[i - 1]) continue;\n    int left = i + 1, right = nums.size() - 1;\n    while (left < right) {\n      int sum = nums[i] + nums[left] + nums[right];\n      if (sum == 0) {\n        result.push_back({nums[i], nums[left], nums[right]});\n        while (left < right && nums[left] == nums[left + 1]) left++;\n        while (left < right && nums[right] == nums[right - 1]) right--;\n        left++;\n        right--;\n      } else if (sum < 0) {\n        left++;\n      } else {\n        right--;\n      }\n    }\n  }\n  return result;\n}"},"challenge":"Handle duplicates efficiently","hints":["Sort the array first","Use two pointers","Skip duplicates"],"relatedProblems":[9,11]},{"id":11,"title":"4 Sum","category":"arrays","categoryName":"Arrays","topic":"Arrays Two Pointers","difficulty":"hard","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":80,"tags":["arrays","two-pointers","four-sum"],"companies":["Microsoft","Amazon"],"concept":"Find all unique quadruplets that sum to target","explanation":"Extension of 3Sum. Sort array and use nested loops with two pointers. Handle duplicates by skipping same values.","complexity":{"time":"O(n³)","space":"O(1)"},"code":{"javascript":"function fourSum(nums, target) {\n  nums.sort((a, b) => a - b);\n  const result = [];\n  for (let i = 0; i < nums.length - 3; i++) {\n    if (i > 0 && nums[i] === nums[i - 1]) continue;\n    for (let j = i + 1; j < nums.length - 2; j++) {\n      if (j > i + 1 && nums[j] === nums[j - 1]) continue;\n      let left = j + 1, right = nums.length - 1;\n      while (left < right) {\n        const sum = nums[i] + nums[j] + nums[left] + nums[right];\n        if (sum === target) {\n          result.push([nums[i], nums[j], nums[left], nums[right]]);\n          while (left < right && nums[left] === nums[left + 1]) left++;\n          while (left < right && nums[right] === nums[right - 1]) right--;\n          left++;\n          right--;\n        } else if (sum < target) {\n          left++;\n        } else {\n          right--;\n        }\n      }\n    }\n  }\n  return result;\n}","python":"def fourSum(nums, target):\n    nums.sort()\n    result = []\n    n = len(nums)\n    for i in range(n - 3):\n        if i > 0 and nums[i] == nums[i - 1]:\n            continue\n        for j in range(i + 1, n - 2):\n            if j > i + 1 and nums[j] == nums[j - 1]:\n                continue\n            left, right = j + 1, n - 1\n            while left < right:\n                total = nums[i] + nums[j] + nums[left] + nums[right]\n                if total == target:\n                    result.append([nums[i], nums[j], nums[left], nums[right]])\n                    while left < right and nums[left] == nums[left + 1]:\n                        left += 1\n                    while left < right and nums[right] == nums[right - 1]:\n                        right -= 1\n                    left += 1\n                    right -= 1\n                elif total < target:\n                    left += 1\n                else:\n                    right -= 1\n    return result","java":"public List<List<Integer>> fourSum(int[] nums, int target) {\n  Arrays.sort(nums);\n  List<List<Integer>> result = new ArrayList<>();\n  for (int i = 0; i < nums.length - 3; i++) {\n    if (i > 0 && nums[i] == nums[i - 1]) continue;\n    for (int j = i + 1; j < nums.length - 2; j++) {\n      if (j > i + 1 && nums[j] == nums[j - 1]) continue;\n      int left = j + 1, right = nums.length - 1;\n      while (left < right) {\n        long sum = (long)nums[i] + nums[j] + nums[left] + nums[right];\n        if (sum == target) {\n          result.add(Arrays.asList(nums[i], nums[j], nums[left], nums[right]));\n          while (left < right && nums[left] == nums[left + 1]) left++;\n          while (left < right && nums[right] == nums[right - 1]) right--;\n          left++;\n          right--;\n        } else if (sum < target) {\n          left++;\n        } else {\n          right--;\n        }\n      }\n    }\n  }\n  return result;\n}","cpp":"vector<vector<int>> fourSum(vector<int>& nums, int target) {\n  sort(nums.begin(), nums.end());\n  vector<vector<int>> result;\n  for (int i = 0; i < nums.size() - 3; i++) {\n    if (i > 0 && nums[i] == nums[i - 1]) continue;\n    for (int j = i + 1; j < nums.size() - 2; j++) {\n      if (j > i + 1 && nums[j] == nums[j - 1]) continue;\n      int left = j + 1, right = nums.size() - 1;\n      while (left < right) {\n        long long sum = (long long)nums[i] + nums[j] + nums[left] + nums[right];\n        if (sum == target) {\n          result.push_back({nums[i], nums[j], nums[left], nums[right]});\n          while (left < right && nums[left] == nums[left + 1]) left++;\n          while (left < right && nums[right] == nums[right - 1]) right--;\n          left++;\n          right--;\n        } else if (sum < target) {\n          left++;\n        } else {\n          right--;\n        }\n      }\n    }\n  }\n  return result;\n}"},"challenge":"Avoid duplicates and handle large numbers","hints":["Sort first","Nested loops with two pointers","Skip duplicates carefully"],"relatedProblems":[9,10]},{"id":12,"title":"Sort an array of 0's 1's and 2's","category":"arrays","categoryName":"Arrays","topic":"Arrays Dutch National Flag","difficulty":"easy","status":"solved","timeSpent":48,"attempts":3,"lastTouched":27,"frequency":90,"tags":["arrays","dutch-national-flag","sorting"],"companies":["Amazon","Microsoft","Adobe"],"concept":"Sort array containing only 0, 1, 2 in single pass","explanation":"Use Dutch National Flag algorithm with three pointers. 0s to left, 2s to right, 1s in middle. Single pass solution.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function sortColors(nums) {\n  let low = 0, mid = 0, high = nums.length - 1;\n  while (mid <= high) {\n    if (nums[mid] === 0) {\n      [nums[low], nums[mid]] = [nums[mid], nums[low]];\n      low++;\n      mid++;\n    } else if (nums[mid] === 1) {\n      mid++;\n    } else {\n      [nums[mid], nums[high]] = [nums[high], nums[mid]];\n      high--;\n    }\n  }\n}","python":"def sortColors(nums):\n    low, mid, high = 0, 0, len(nums) - 1\n    while mid <= high:\n        if nums[mid] == 0:\n            nums[low], nums[mid] = nums[mid], nums[low]\n            low += 1\n            mid += 1\n        elif nums[mid] == 1:\n            mid += 1\n        else:\n            nums[mid], nums[high] = nums[high], nums[mid]\n            high -= 1","java":"public void sortColors(int[] nums) {\n  int low = 0, mid = 0, high = nums.length - 1;\n  while (mid <= high) {\n    if (nums[mid] == 0) {\n      int temp = nums[low];\n      nums[low] = nums[mid];\n      nums[mid] = temp;\n      low++;\n      mid++;\n    } else if (nums[mid] == 1) {\n      mid++;\n    } else {\n      int temp = nums[mid];\n      nums[mid] = nums[high];\n      nums[high] = temp;\n      high--;\n    }\n  }\n}","cpp":"void sortColors(vector<int>& nums) {\n  int low = 0, mid = 0, high = nums.size() - 1;\n  while (mid <= high) {\n    if (nums[mid] == 0) {\n      swap(nums[low++], nums[mid++]);\n    } else if (nums[mid] == 1) {\n      mid++;\n    } else {\n      swap(nums[mid], nums[high--]);\n    }\n  }\n}"},"challenge":"Single pass O(n) solution","hints":["Three pointers: low, mid, high","0s to left, 2s to right","1s automatically in middle"],"relatedProblems":[]},{"id":13,"title":"Kadane's Algorithm","category":"arrays","categoryName":"Arrays","topic":"Arrays Dynamic Programming","difficulty":"medium","status":"solved","timeSpent":33,"attempts":2,"lastTouched":11,"frequency":100,"tags":["arrays","kadane","dp","maximum-subarray"],"companies":["Amazon","Microsoft","Google","Facebook","LinkedIn"],"concept":"Find maximum sum subarray","explanation":"Track current sum and maximum sum. If current sum becomes negative, reset to 0. Update max sum at each step.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function maxSubArray(nums) {\n  let maxSum = nums[0], currSum = 0;\n  for (let num of nums) {\n    currSum += num;\n    maxSum = Math.max(maxSum, currSum);\n    if (currSum < 0) currSum = 0;\n  }\n  return maxSum;\n}","python":"def maxSubArray(nums):\n    max_sum = nums[0]\n    curr_sum = 0\n    for num in nums:\n        curr_sum += num\n        max_sum = max(max_sum, curr_sum)\n        if curr_sum < 0:\n            curr_sum = 0\n    return max_sum","java":"public int maxSubArray(int[] nums) {\n  int maxSum = nums[0], currSum = 0;\n  for (int num : nums) {\n    currSum += num;\n    maxSum = Math.max(maxSum, currSum);\n    if (currSum < 0) currSum = 0;\n  }\n  return maxSum;\n}","cpp":"int maxSubArray(vector<int>& nums) {\n  int maxSum = nums[0], currSum = 0;\n  for (int num : nums) {\n    currSum += num;\n    maxSum = max(maxSum, currSum);\n    if (currSum < 0) currSum = 0;\n  }\n  return maxSum;\n}"},"challenge":"Also find the subarray indices","hints":["Track current sum","Reset if negative","Update max sum"],"relatedProblems":[19]},{"id":14,"title":"Next Permutation","category":"arrays","categoryName":"Arrays","topic":"Arrays Two Pointers","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":85,"tags":["arrays","permutation","two-pointers"],"companies":["Amazon","Microsoft","Facebook"],"concept":"Find next lexicographical permutation","explanation":"Find first decreasing element from right, swap with smallest larger element to its right, then reverse suffix.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function nextPermutation(nums) {\n  let i = nums.length - 2;\n  while (i >= 0 && nums[i] >= nums[i + 1]) i--;\n  if (i >= 0) {\n    let j = nums.length - 1;\n    while (j >= 0 && nums[j] <= nums[i]) j--;\n    [nums[i], nums[j]] = [nums[j], nums[i]];\n  }\n  let left = i + 1, right = nums.length - 1;\n  while (left < right) {\n    [nums[left], nums[right]] = [nums[right], nums[left]];\n    left++;\n    right--;\n  }\n}","python":"def nextPermutation(nums):\n    i = len(nums) - 2\n    while i >= 0 and nums[i] >= nums[i + 1]:\n        i -= 1\n    if i >= 0:\n        j = len(nums) - 1\n        while j >= 0 and nums[j] <= nums[i]:\n            j -= 1\n        nums[i], nums[j] = nums[j], nums[i]\n    left, right = i + 1, len(nums) - 1\n    while left < right:\n        nums[left], nums[right] = nums[right], nums[left]\n        left += 1\n        right -= 1","java":"public void nextPermutation(int[] nums) {\n  int i = nums.length - 2;\n  while (i >= 0 && nums[i] >= nums[i + 1]) i--;\n  if (i >= 0) {\n    int j = nums.length - 1;\n    while (j >= 0 && nums[j] <= nums[i]) j--;\n    int temp = nums[i];\n    nums[i] = nums[j];\n    nums[j] = temp;\n  }\n  int left = i + 1, right = nums.length - 1;\n  while (left < right) {\n    int temp = nums[left];\n    nums[left] = nums[right];\n    nums[right] = temp;\n    left++;\n    right--;\n  }\n}","cpp":"void nextPermutation(vector<int>& nums) {\n  int i = nums.size() - 2;\n  while (i >= 0 && nums[i] >= nums[i + 1]) i--;\n  if (i >= 0) {\n    int j = nums.size() - 1;\n    while (j >= 0 && nums[j] <= nums[i]) j--;\n    swap(nums[i], nums[j]);\n  }\n  reverse(nums.begin() + i + 1, nums.end());\n}"},"challenge":"In-place with O(1) extra space","hints":["Find pivot from right","Find successor","Reverse suffix"],"relatedProblems":[]},{"id":15,"title":"Majority Element-II","category":"arrays","categoryName":"Arrays","topic":"Arrays Extended Boyer-Moore","difficulty":"medium","status":"solved","timeSpent":33,"attempts":1,"lastTouched":5,"frequency":75,"tags":["arrays","boyer-moore","majority-element"],"companies":["Microsoft","Amazon"],"concept":"Find all elements appearing more than n/3 times","explanation":"Extended Boyer-Moore for two candidates. Elements appearing more than n/3 times. Track two candidates with counts.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function majorityElement(nums) {\n  let candidate1 = null, candidate2 = null, count1 = 0, count2 = 0;\n  for (let num of nums) {\n    if (num === candidate1) count1++;\n    else if (num === candidate2) count2++;\n    else if (count1 === 0) { candidate1 = num; count1 = 1; }\n    else if (count2 === 0) { candidate2 = num; count2 = 1; }\n    else { count1--; count2--; }\n  }\n  count1 = count2 = 0;\n  for (let num of nums) {\n    if (num === candidate1) count1++;\n    else if (num === candidate2) count2++;\n  }\n  const result = [];\n  if (count1 > nums.length / 3) result.push(candidate1);\n  if (count2 > nums.length / 3) result.push(candidate2);\n  return result;\n}","python":"def majorityElement(nums):\n    candidate1, candidate2, count1, count2 = None, None, 0, 0\n    for num in nums:\n        if num == candidate1:\n            count1 += 1\n        elif num == candidate2:\n            count2 += 1\n        elif count1 == 0:\n            candidate1, count1 = num, 1\n        elif count2 == 0:\n            candidate2, count2 = num, 1\n        else:\n            count1, count2 = count1 - 1, count2 - 1\n    count1 = count2 = 0\n    for num in nums:\n        if num == candidate1:\n            count1 += 1\n        elif num == candidate2:\n            count2 += 1\n    result = []\n    if count1 > len(nums) // 3:\n        result.append(candidate1)\n    if count2 > len(nums) // 3:\n        result.append(candidate2)\n    return result","java":"public List<Integer> majorityElement(int[] nums) {\n  Integer candidate1 = null, candidate2 = null;\n  int count1 = 0, count2 = 0;\n  for (int num : nums) {\n    if (candidate1 != null && num == candidate1) count1++;\n    else if (candidate2 != null && num == candidate2) count2++;\n    else if (count1 == 0) { candidate1 = num; count1 = 1; }\n    else if (count2 == 0) { candidate2 = num; count2 = 1; }\n    else { count1--; count2--; }\n  }\n  count1 = count2 = 0;\n  for (int num : nums) {\n    if (candidate1 != null && num == candidate1) count1++;\n    else if (candidate2 != null && num == candidate2) count2++;\n  }\n  List<Integer> result = new ArrayList<>();\n  if (count1 > nums.length / 3) result.add(candidate1);\n  if (count2 > nums.length / 3) result.add(candidate2);\n  return result;\n}","cpp":"vector<int> majorityElement(vector<int>& nums) {\n  int candidate1 = 0, candidate2 = 0, count1 = 0, count2 = 0;\n  for (int num : nums) {\n    if (count1 > 0 && num == candidate1) count1++;\n    else if (count2 > 0 && num == candidate2) count2++;\n    else if (count1 == 0) { candidate1 = num; count1 = 1; }\n    else if (count2 == 0) { candidate2 = num; count2 = 1; }\n    else { count1--; count2--; }\n  }\n  count1 = count2 = 0;\n  for (int num : nums) {\n    if (num == candidate1) count1++;\n    else if (num == candidate2) count2++;\n  }\n  vector<int> result;\n  if (count1 > nums.size() / 3) result.push_back(candidate1);\n  if (count2 > nums.size() / 3) result.push_back(candidate2);\n  return result;\n}"},"challenge":"At most two majority elements possible","hints":["Extended voting algorithm","Two candidates","Verify in second pass"],"relatedProblems":[1]},{"id":16,"title":"Find the repeating and missing number","category":"arrays","categoryName":"Arrays","topic":"Arrays Math","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":80,"tags":["arrays","math","xor"],"companies":["Amazon","Microsoft"],"concept":"Find duplicate and missing number in 1 to N range","explanation":"Use mathematical approach with sum and sum of squares, or use XOR to find the two numbers.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function findErrorNums(nums) {\n  let xor = 0, n = nums.length;\n  for (let i = 1; i <= n; i++) xor ^= i;\n  for (let num of nums) xor ^= num;\n  let diff = xor & -xor;\n  let x = 0, y = 0;\n  for (let i = 1; i <= n; i++) {\n    if (i & diff) x ^= i;\n    else y ^= i;\n  }\n  for (let num of nums) {\n    if (num & diff) x ^= num;\n    else y ^= num;\n  }\n  for (let num of nums) {\n    if (num === x) return [x, y];\n  }\n  return [y, x];\n}","python":"def findErrorNums(nums):\n    xor = 0\n    n = len(nums)\n    for i in range(1, n + 1):\n        xor ^= i\n    for num in nums:\n        xor ^= num\n    diff = xor & -xor\n    x = y = 0\n    for i in range(1, n + 1):\n        if i & diff:\n            x ^= i\n        else:\n            y ^= i\n    for num in nums:\n        if num & diff:\n            x ^= num\n        else:\n            y ^= num\n    for num in nums:\n        if num == x:\n            return [x, y]\n    return [y, x]","java":"public int[] findErrorNums(int[] nums) {\n  int xor = 0, n = nums.length;\n  for (int i = 1; i <= n; i++) xor ^= i;\n  for (int num : nums) xor ^= num;\n  int diff = xor & (-xor);\n  int x = 0, y = 0;\n  for (int i = 1; i <= n; i++) {\n    if ((i & diff) != 0) x ^= i;\n    else y ^= i;\n  }\n  for (int num : nums) {\n    if ((num & diff) != 0) x ^= num;\n    else y ^= num;\n  }\n  for (int num : nums) {\n    if (num == x) return new int[]{x, y};\n  }\n  return new int[]{y, x};\n}","cpp":"vector<int> findErrorNums(vector<int>& nums) {\n  int xor_sum = 0, n = nums.size();\n  for (int i = 1; i <= n; i++) xor_sum ^= i;\n  for (int num : nums) xor_sum ^= num;\n  int diff = xor_sum & (-xor_sum);\n  int x = 0, y = 0;\n  for (int i = 1; i <= n; i++) {\n    if (i & diff) x ^= i;\n    else y ^= i;\n  }\n  for (int num : nums) {\n    if (num & diff) x ^= num;\n    else y ^= num;\n  }\n  for (int num : nums) {\n    if (num == x) return {x, y};\n  }\n  return {y, x};\n}"},"challenge":"Solve with O(1) space","hints":["Use XOR approach","Find differentiating bit","Partition and find"],"relatedProblems":[]},{"id":17,"title":"Count Inversions","category":"arrays","categoryName":"Arrays","topic":"Arrays Merge Sort","difficulty":"hard","status":"solved","timeSpent":23,"attempts":3,"lastTouched":24,"frequency":85,"tags":["arrays","merge-sort","inversions"],"companies":["Amazon","Microsoft","Adobe"],"concept":"Count pairs where i < j and arr[i] > arr[j]","explanation":"Use modified merge sort. Count inversions during merge step when element from right half is smaller than left half.","complexity":{"time":"O(n log n)","space":"O(n)"},"code":{"javascript":"function countInversions(arr) {\n  return mergeSort(arr, 0, arr.length - 1);\n}\n\nfunction mergeSort(arr, left, right) {\n  let invCount = 0;\n  if (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    invCount += mergeSort(arr, left, mid);\n    invCount += mergeSort(arr, mid + 1, right);\n    invCount += merge(arr, left, mid, right);\n  }\n  return invCount;\n}\n\nfunction merge(arr, left, mid, right) {\n  const leftArr = arr.slice(left, mid + 1);\n  const rightArr = arr.slice(mid + 1, right + 1);\n  let i = 0, j = 0, k = left, invCount = 0;\n  while (i < leftArr.length && j < rightArr.length) {\n    if (leftArr[i] <= rightArr[j]) {\n      arr[k++] = leftArr[i++];\n    } else {\n      arr[k++] = rightArr[j++];\n      invCount += leftArr.length - i;\n    }\n  }\n  while (i < leftArr.length) arr[k++] = leftArr[i++];\n  while (j < rightArr.length) arr[k++] = rightArr[j++];\n  return invCount;\n}","python":"def countInversions(arr):\n    return mergeSort(arr, 0, len(arr) - 1)\n\ndef mergeSort(arr, left, right):\n    inv_count = 0\n    if left < right:\n        mid = (left + right) // 2\n        inv_count += mergeSort(arr, left, mid)\n        inv_count += mergeSort(arr, mid + 1, right)\n        inv_count += merge(arr, left, mid, right)\n    return inv_count\n\ndef merge(arr, left, mid, right):\n    left_arr = arr[left:mid + 1]\n    right_arr = arr[mid + 1:right + 1]\n    i = j = 0\n    k = left\n    inv_count = 0\n    while i < len(left_arr) and j < len(right_arr):\n        if left_arr[i] <= right_arr[j]:\n            arr[k] = left_arr[i]\n            i += 1\n        else:\n            arr[k] = right_arr[j]\n            inv_count += len(left_arr) - i\n            j += 1\n        k += 1\n    while i < len(left_arr):\n        arr[k] = left_arr[i]\n        i += 1\n        k += 1\n    while j < len(right_arr):\n        arr[k] = right_arr[j]\n        j += 1\n        k += 1\n    return inv_count","java":"public int countInversions(int[] arr) {\n  return mergeSort(arr, 0, arr.length - 1);\n}\n\nint mergeSort(int[] arr, int left, int right) {\n  int invCount = 0;\n  if (left < right) {\n    int mid = (left + right) / 2;\n    invCount += mergeSort(arr, left, mid);\n    invCount += mergeSort(arr, mid + 1, right);\n    invCount += merge(arr, left, mid, right);\n  }\n  return invCount;\n}\n\nint merge(int[] arr, int left, int mid, int right) {\n  int[] leftArr = Arrays.copyOfRange(arr, left, mid + 1);\n  int[] rightArr = Arrays.copyOfRange(arr, mid + 1, right + 1);\n  int i = 0, j = 0, k = left, invCount = 0;\n  while (i < leftArr.length && j < rightArr.length) {\n    if (leftArr[i] <= rightArr[j]) {\n      arr[k++] = leftArr[i++];\n    } else {\n      arr[k++] = rightArr[j++];\n      invCount += leftArr.length - i;\n    }\n  }\n  while (i < leftArr.length) arr[k++] = leftArr[i++];\n  while (j < rightArr.length) arr[k++] = rightArr[j++];\n  return invCount;\n}","cpp":"int countInversions(vector<int>& arr) {\n  return mergeSort(arr, 0, arr.size() - 1);\n}\n\nint mergeSort(vector<int>& arr, int left, int right) {\n  int invCount = 0;\n  if (left < right) {\n    int mid = left + (right - left) / 2;\n    invCount += mergeSort(arr, left, mid);\n    invCount += mergeSort(arr, mid + 1, right);\n    invCount += merge(arr, left, mid, right);\n  }\n  return invCount;\n}\n\nint merge(vector<int>& arr, int left, int mid, int right) {\n  vector<int> leftArr(arr.begin() + left, arr.begin() + mid + 1);\n  vector<int> rightArr(arr.begin() + mid + 1, arr.begin() + right + 1);\n  int i = 0, j = 0, k = left, invCount = 0;\n  while (i < leftArr.size() && j < rightArr.size()) {\n    if (leftArr[i] <= rightArr[j]) {\n      arr[k++] = leftArr[i++];\n    } else {\n      arr[k++] = rightArr[j++];\n      invCount += leftArr.size() - i;\n    }\n  }\n  while (i < leftArr.size()) arr[k++] = leftArr[i++];\n  while (j < rightArr.size()) arr[k++] = rightArr[j++];\n  return invCount;\n}"},"challenge":"Large array handling","hints":["Modified merge sort","Count during merge","All remaining elements form inversions"],"relatedProblems":[18]},{"id":18,"title":"Reverse Pairs","category":"arrays","categoryName":"Arrays","topic":"Arrays Merge Sort","difficulty":"hard","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":70,"tags":["arrays","merge-sort","reverse-pairs"],"companies":["Google","Facebook"],"concept":"Count pairs where i < j and arr[i] > 2*arr[j]","explanation":"Similar to count inversions but with condition arr[i] > 2*arr[j]. Use modified merge sort with two pointers.","complexity":{"time":"O(n log n)","space":"O(n)"},"code":{"javascript":"function reversePairs(nums) {\n  return mergeSort(nums, 0, nums.length - 1);\n}\n\nfunction mergeSort(nums, left, right) {\n  if (left >= right) return 0;\n  const mid = Math.floor((left + right) / 2);\n  let count = mergeSort(nums, left, mid) + mergeSort(nums, mid + 1, right);\n  let j = mid + 1;\n  for (let i = left; i <= mid; i++) {\n    while (j <= right && nums[i] > 2 * nums[j]) j++;\n    count += j - (mid + 1);\n  }\n  merge(nums, left, mid, right);\n  return count;\n}\n\nfunction merge(nums, left, mid, right) {\n  const temp = [];\n  let i = left, j = mid + 1;\n  while (i <= mid && j <= right) {\n    if (nums[i] <= nums[j]) temp.push(nums[i++]);\n    else temp.push(nums[j++]);\n  }\n  while (i <= mid) temp.push(nums[i++]);\n  while (j <= right) temp.push(nums[j++]);\n  for (let k = 0; k < temp.length; k++) {\n    nums[left + k] = temp[k];\n  }\n}","python":"def reversePairs(nums):\n    return mergeSort(nums, 0, len(nums) - 1)\n\ndef mergeSort(nums, left, right):\n    if left >= right:\n        return 0\n    mid = (left + right) // 2\n    count = mergeSort(nums, left, mid) + mergeSort(nums, mid + 1, right)\n    j = mid + 1\n    for i in range(left, mid + 1):\n        while j <= right and nums[i] > 2 * nums[j]:\n            j += 1\n        count += j - (mid + 1)\n    merge(nums, left, mid, right)\n    return count\n\ndef merge(nums, left, mid, right):\n    temp = []\n    i, j = left, mid + 1\n    while i <= mid and j <= right:\n        if nums[i] <= nums[j]:\n            temp.append(nums[i])\n            i += 1\n        else:\n            temp.append(nums[j])\n            j += 1\n    while i <= mid:\n        temp.append(nums[i])\n        i += 1\n    while j <= right:\n        temp.append(nums[j])\n        j += 1\n    for k in range(len(temp)):\n        nums[left + k] = temp[k]","java":"public int reversePairs(int[] nums) {\n  return mergeSort(nums, 0, nums.length - 1);\n}\n\nint mergeSort(int[] nums, int left, int right) {\n  if (left >= right) return 0;\n  int mid = (left + right) / 2;\n  int count = mergeSort(nums, left, mid) + mergeSort(nums, mid + 1, right);\n  int j = mid + 1;\n  for (int i = left; i <= mid; i++) {\n    while (j <= right && (long)nums[i] > 2L * nums[j]) j++;\n    count += j - (mid + 1);\n  }\n  merge(nums, left, mid, right);\n  return count;\n}\n\nvoid merge(int[] nums, int left, int mid, int right) {\n  int[] temp = new int[right - left + 1];\n  int i = left, j = mid + 1, k = 0;\n  while (i <= mid && j <= right) {\n    if (nums[i] <= nums[j]) temp[k++] = nums[i++];\n    else temp[k++] = nums[j++];\n  }\n  while (i <= mid) temp[k++] = nums[i++];\n  while (j <= right) temp[k++] = nums[j++];\n  System.arraycopy(temp, 0, nums, left, temp.length);\n}","cpp":"int reversePairs(vector<int>& nums) {\n  return mergeSort(nums, 0, nums.size() - 1);\n}\n\nint mergeSort(vector<int>& nums, int left, int right) {\n  if (left >= right) return 0;\n  int mid = left + (right - left) / 2;\n  int count = mergeSort(nums, left, mid) + mergeSort(nums, mid + 1, right);\n  int j = mid + 1;\n  for (int i = left; i <= mid; i++) {\n    while (j <= right && (long long)nums[i] > 2LL * nums[j]) j++;\n    count += j - (mid + 1);\n  }\n  merge(nums, left, mid, right);\n  return count;\n}\n\nvoid merge(vector<int>& nums, int left, int mid, int right) {\n  vector<int> temp;\n  int i = left, j = mid + 1;\n  while (i <= mid && j <= right) {\n    if (nums[i] <= nums[j]) temp.push_back(nums[i++]);\n    else temp.push_back(nums[j++]);\n  }\n  while (i <= mid) temp.push_back(nums[i++]);\n  while (j <= right) temp.push_back(nums[j++]);\n  for (int k = 0; k < temp.size(); k++) {\n    nums[left + k] = temp[k];\n  }\n}"},"challenge":"Handle integer overflow","hints":["Modified merge sort","Two pointer technique","Count valid pairs before merge"],"relatedProblems":[17]},{"id":19,"title":"Maximum Product Subarray in an Array","category":"arrays","categoryName":"Arrays","topic":"Arrays Dynamic Programming","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":85,"tags":["arrays","dp","maximum-product"],"companies":["Amazon","Microsoft","LinkedIn"],"concept":"Find subarray with maximum product","explanation":"Track both max and min product ending at each position. Min can become max when multiplied by negative number.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function maxProduct(nums) {\n  let maxProd = nums[0], minProd = nums[0], result = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    const temp = maxProd;\n    maxProd = Math.max(nums[i], Math.max(maxProd * nums[i], minProd * nums[i]));\n    minProd = Math.min(nums[i], Math.min(temp * nums[i], minProd * nums[i]));\n    result = Math.max(result, maxProd);\n  }\n  return result;\n}","python":"def maxProduct(nums):\n    max_prod = min_prod = result = nums[0]\n    for i in range(1, len(nums)):\n        temp = max_prod\n        max_prod = max(nums[i], max_prod * nums[i], min_prod * nums[i])\n        min_prod = min(nums[i], temp * nums[i], min_prod * nums[i])\n        result = max(result, max_prod)\n    return result","java":"public int maxProduct(int[] nums) {\n  int maxProd = nums[0], minProd = nums[0], result = nums[0];\n  for (int i = 1; i < nums.length; i++) {\n    int temp = maxProd;\n    maxProd = Math.max(nums[i], Math.max(maxProd * nums[i], minProd * nums[i]));\n    minProd = Math.min(nums[i], Math.min(temp * nums[i], minProd * nums[i]));\n    result = Math.max(result, maxProd);\n  }\n  return result;\n}","cpp":"int maxProduct(vector<int>& nums) {\n  int maxProd = nums[0], minProd = nums[0], result = nums[0];\n  for (int i = 1; i < nums.size(); i++) {\n    int temp = maxProd;\n    maxProd = max({nums[i], maxProd * nums[i], minProd * nums[i]});\n    minProd = min({nums[i], temp * nums[i], minProd * nums[i]});\n    result = max(result, maxProd);\n  }\n  return result;\n}"},"challenge":"Handle zeros and negative numbers","hints":["Track both max and min","Swap on negative numbers","Reset on zero"],"relatedProblems":[13]},{"id":20,"title":"Merge two sorted arrays without extra space","category":"arrays","categoryName":"Arrays","topic":"Arrays Two Pointers","difficulty":"hard","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":90,"tags":["arrays","merge","two-pointers"],"companies":["Microsoft","Amazon","Facebook"],"concept":"Merge two sorted arrays in-place without extra space","explanation":"Start from end of both arrays. Compare and place larger element at end of first array. Continue until all elements are merged.","complexity":{"time":"O(m+n)","space":"O(1)"},"code":{"javascript":"function merge(nums1, m, nums2, n) {\n  let i = m - 1, j = n - 1, k = m + n - 1;\n  while (j >= 0) {\n    if (i >= 0 && nums1[i] > nums2[j]) {\n      nums1[k--] = nums1[i--];\n    } else {\n      nums1[k--] = nums2[j--];\n    }\n  }\n}","python":"def merge(nums1, m, nums2, n):\n    i, j, k = m - 1, n - 1, m + n - 1\n    while j >= 0:\n        if i >= 0 and nums1[i] > nums2[j]:\n            nums1[k] = nums1[i]\n            i -= 1\n        else:\n            nums1[k] = nums2[j]\n            j -= 1\n        k -= 1","java":"public void merge(int[] nums1, int m, int[] nums2, int n) {\n  int i = m - 1, j = n - 1, k = m + n - 1;\n  while (j >= 0) {\n    if (i >= 0 && nums1[i] > nums2[j]) {\n      nums1[k--] = nums1[i--];\n    } else {\n      nums1[k--] = nums2[j--];\n    }\n  }\n}","cpp":"void merge(vector<int>& nums1, int m, vector<int>& nums2, int n) {\n  int i = m - 1, j = n - 1, k = m + n - 1;\n  while (j >= 0) {\n    if (i >= 0 && nums1[i] > nums2[j]) {\n      nums1[k--] = nums1[i--];\n    } else {\n      nums1[k--] = nums2[j--];\n    }\n  }\n}"},"challenge":"Truly O(1) space with gap method","hints":["Start from end","Compare and place","Fill remaining"],"relatedProblems":[]},{"id":21,"title":"Longest Consecutive Sequence in an Array","category":"arrays","categoryName":"Arrays","topic":"Arrays Hashing","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":95,"tags":["arrays","hashing","consecutive-sequence"],"companies":["Google","Amazon","Microsoft","Facebook"],"concept":"Find longest consecutive elements sequence","explanation":"Use hash set for O(1) lookup. For each element, if it's start of sequence (no smaller neighbor), count consecutive elements.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function longestConsecutive(nums) {\n  const set = new Set(nums);\n  let maxLen = 0;\n  for (let num of set) {\n    if (!set.has(num - 1)) {\n      let currNum = num;\n      let currLen = 1;\n      while (set.has(currNum + 1)) {\n        currNum++;\n        currLen++;\n      }\n      maxLen = Math.max(maxLen, currLen);\n    }\n  }\n  return maxLen;\n}","python":"def longestConsecutive(nums):\n    num_set = set(nums)\n    max_len = 0\n    for num in num_set:\n        if num - 1 not in num_set:\n            curr_num = num\n            curr_len = 1\n            while curr_num + 1 in num_set:\n                curr_num += 1\n                curr_len += 1\n            max_len = max(max_len, curr_len)\n    return max_len","java":"public int longestConsecutive(int[] nums) {\n  Set<Integer> set = new HashSet<>();\n  for (int num : nums) set.add(num);\n  int maxLen = 0;\n  for (int num : set) {\n    if (!set.contains(num - 1)) {\n      int currNum = num;\n      int currLen = 1;\n      while (set.contains(currNum + 1)) {\n        currNum++;\n        currLen++;\n      }\n      maxLen = Math.max(maxLen, currLen);\n    }\n  }\n  return maxLen;\n}","cpp":"int longestConsecutive(vector<int>& nums) {\n  unordered_set<int> set(nums.begin(), nums.end());\n  int maxLen = 0;\n  for (int num : set) {\n    if (set.find(num - 1) == set.end()) {\n      int currNum = num;\n      int currLen = 1;\n      while (set.find(currNum + 1) != set.end()) {\n        currNum++;\n        currLen++;\n      }\n      maxLen = max(maxLen, currLen);\n    }\n  }\n  return maxLen;\n}"},"challenge":"O(n) time without sorting","hints":["Use hash set","Find sequence starts","Count consecutive"],"relatedProblems":[]},{"id":22,"title":"Longest subarray with sum K","category":"arrays","categoryName":"Arrays","topic":"Arrays Hashing Prefix Sum","difficulty":"medium","status":"revisit","timeSpent":40,"attempts":4,"lastTouched":8,"frequency":80,"tags":["arrays","hashing","prefix-sum","subarray"],"companies":["Amazon","Microsoft"],"concept":"Find longest subarray with given sum K","explanation":"Use prefix sum with hash map. Store first occurrence of each prefix sum. If prefix_sum - K exists, we found a valid subarray.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function longestSubarray(nums, k) {\n  const map = new Map();\n  map.set(0, -1);\n  let prefixSum = 0, maxLen = 0;\n  for (let i = 0; i < nums.length; i++) {\n    prefixSum += nums[i];\n    if (map.has(prefixSum - k)) {\n      maxLen = Math.max(maxLen, i - map.get(prefixSum - k));\n    }\n    if (!map.has(prefixSum)) map.set(prefixSum, i);\n  }\n  return maxLen;\n}","python":"def longestSubarray(nums, k):\n    prefix_map = {0: -1}\n    prefix_sum = max_len = 0\n    for i, num in enumerate(nums):\n        prefix_sum += num\n        if prefix_sum - k in prefix_map:\n            max_len = max(max_len, i - prefix_map[prefix_sum - k])\n        if prefix_sum not in prefix_map:\n            prefix_map[prefix_sum] = i\n    return max_len","java":"public int longestSubarray(int[] nums, int k) {\n  Map<Integer, Integer> map = new HashMap<>();\n  map.put(0, -1);\n  int prefixSum = 0, maxLen = 0;\n  for (int i = 0; i < nums.length; i++) {\n    prefixSum += nums[i];\n    if (map.containsKey(prefixSum - k)) {\n      maxLen = Math.max(maxLen, i - map.get(prefixSum - k));\n    }\n    map.putIfAbsent(prefixSum, i);\n  }\n  return maxLen;\n}","cpp":"int longestSubarray(vector<int>& nums, int k) {\n  unordered_map<int, int> map;\n  map[0] = -1;\n  int prefixSum = 0, maxLen = 0;\n  for (int i = 0; i < nums.size(); i++) {\n    prefixSum += nums[i];\n    if (map.find(prefixSum - k) != map.end()) {\n      maxLen = max(maxLen, i - map[prefixSum - k]);\n    }\n    if (map.find(prefixSum) == map.end()) {\n      map[prefixSum] = i;\n    }\n  }\n  return maxLen;\n}"},"challenge":"Handle positive numbers only with sliding window","hints":["Prefix sum approach","Hash map for indices","Check prefix_sum - K"],"relatedProblems":[23,24]},{"id":23,"title":"Count subarrays with given sum","category":"arrays","categoryName":"Arrays","topic":"Arrays Hashing Prefix Sum","difficulty":"medium","status":"solved","timeSpent":20,"attempts":2,"lastTouched":19,"frequency":85,"tags":["arrays","hashing","prefix-sum","count-subarrays"],"companies":["Amazon","Facebook","Google"],"concept":"Count all subarrays with sum equal to K","explanation":"Use prefix sum with hash map counting occurrences. For each prefix sum, add count of (prefix_sum - K) to result.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function subarraySum(nums, k) {\n  const map = new Map();\n  map.set(0, 1);\n  let count = 0, prefixSum = 0;\n  for (let num of nums) {\n    prefixSum += num;\n    count += map.get(prefixSum - k) || 0;\n    map.set(prefixSum, (map.get(prefixSum) || 0) + 1);\n  }\n  return count;\n}","python":"def subarraySum(nums, k):\n    prefix_map = {0: 1}\n    count = prefix_sum = 0\n    for num in nums:\n        prefix_sum += num\n        count += prefix_map.get(prefix_sum - k, 0)\n        prefix_map[prefix_sum] = prefix_map.get(prefix_sum, 0) + 1\n    return count","java":"public int subarraySum(int[] nums, int k) {\n  Map<Integer, Integer> map = new HashMap<>();\n  map.put(0, 1);\n  int count = 0, prefixSum = 0;\n  for (int num : nums) {\n    prefixSum += num;\n    count += map.getOrDefault(prefixSum - k, 0);\n    map.put(prefixSum, map.getOrDefault(prefixSum, 0) + 1);\n  }\n  return count;\n}","cpp":"int subarraySum(vector<int>& nums, int k) {\n  unordered_map<int, int> map;\n  map[0] = 1;\n  int count = 0, prefixSum = 0;\n  for (int num : nums) {\n    prefixSum += num;\n    count += map[prefixSum - k];\n    map[prefixSum]++;\n  }\n  return count;\n}"},"challenge":"Handle negative numbers correctly","hints":["Prefix sum counts","Use hash map frequency","Add count of prefix_sum - K"],"relatedProblems":[22,24]},{"id":24,"title":"Count subarrays with given xor K","category":"arrays","categoryName":"Arrays","topic":"Arrays Hashing Prefix XOR","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":75,"tags":["arrays","hashing","xor","prefix-xor"],"companies":["Amazon","Microsoft"],"concept":"Count subarrays with XOR equal to K","explanation":"Use prefix XOR with hash map. If prefix_xor ^ K exists in map, those many subarrays have XOR equal to K.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function subarrayXor(nums, k) {\n  const map = new Map();\n  map.set(0, 1);\n  let count = 0, prefixXor = 0;\n  for (let num of nums) {\n    prefixXor ^= num;\n    count += map.get(prefixXor ^ k) || 0;\n    map.set(prefixXor, (map.get(prefixXor) || 0) + 1);\n  }\n  return count;\n}","python":"def subarrayXor(nums, k):\n    prefix_map = {0: 1}\n    count = prefix_xor = 0\n    for num in nums:\n        prefix_xor ^= num\n        count += prefix_map.get(prefix_xor ^ k, 0)\n        prefix_map[prefix_xor] = prefix_map.get(prefix_xor, 0) + 1\n    return count","java":"public int subarrayXor(int[] nums, int k) {\n  Map<Integer, Integer> map = new HashMap<>();\n  map.put(0, 1);\n  int count = 0, prefixXor = 0;\n  for (int num : nums) {\n    prefixXor ^= num;\n    count += map.getOrDefault(prefixXor ^ k, 0);\n    map.put(prefixXor, map.getOrDefault(prefixXor, 0) + 1);\n  }\n  return count;\n}","cpp":"int subarrayXor(vector<int>& nums, int k) {\n  unordered_map<int, int> map;\n  map[0] = 1;\n  int count = 0, prefixXor = 0;\n  for (int num : nums) {\n    prefixXor ^= num;\n    count += map[prefixXor ^ k];\n    map[prefixXor]++;\n  }\n  return count;\n}"},"challenge":"Understand XOR property: a^a = 0","hints":["Use prefix XOR","XOR property: a^b=c => a^c=b","Count occurrences"],"relatedProblems":[22,23]},{"id":25,"title":"Search X in sorted array","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Basic","difficulty":"easy","status":"solved","timeSpent":64,"attempts":4,"lastTouched":9,"frequency":100,"tags":["binary-search","basic"],"companies":["Amazon","Microsoft","Google"],"concept":"Basic binary search to find element","explanation":"Standard binary search. Compare target with middle element, eliminate half of array based on comparison.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function search(nums, target) {\n  let left = 0, right = nums.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] === target) return mid;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}","python":"def search(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1","java":"public int search(int[] nums, int target) {\n  int left = 0, right = nums.length - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) return mid;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}","cpp":"int search(vector<int>& nums, int target) {\n  int left = 0, right = nums.size() - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) return mid;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}"},"challenge":"Handle edge cases","hints":["Standard binary search","Compare with middle","Eliminate half"],"relatedProblems":[26,27]},{"id":26,"title":"Lower Bound","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Implementation","difficulty":"easy","status":"attempted","timeSpent":33,"attempts":1,"lastTouched":28,"frequency":85,"tags":["binary-search","lower-bound"],"companies":["Amazon","Microsoft"],"concept":"Find first element >= target","explanation":"Find the first position where element is not less than target. This is the lower bound.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function lowerBound(nums, target) {\n  let left = 0, right = nums.length;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","python":"def lowerBound(nums, target):\n    left, right = 0, len(nums)\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid\n    return left","java":"public int lowerBound(int[] nums, int target) {\n  int left = 0, right = nums.length;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","cpp":"int lowerBound(vector<int>& nums, int target) {\n  int left = 0, right = nums.size();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}"},"challenge":"Understand right = nums.length","hints":["First element >= target","left < right condition","Return left"],"relatedProblems":[25,27]},{"id":27,"title":"Upper Bound","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Implementation","difficulty":"easy","status":"solved","timeSpent":59,"attempts":3,"lastTouched":12,"frequency":80,"tags":["binary-search","upper-bound"],"companies":["Amazon","Microsoft"],"concept":"Find first element > target","explanation":"Find the first position where element is greater than target. This is the upper bound.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function upperBound(nums, target) {\n  let left = 0, right = nums.length;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] <= target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","python":"def upperBound(nums, target):\n    left, right = 0, len(nums)\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] <= target:\n            left = mid + 1\n        else:\n            right = mid\n    return left","java":"public int upperBound(int[] nums, int target) {\n  int left = 0, right = nums.length;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] <= target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","cpp":"int upperBound(vector<int>& nums, int target) {\n  int left = 0, right = nums.size();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] <= target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}"},"challenge":"Difference from lower bound","hints":["First element > target","<= condition","Return left"],"relatedProblems":[25,26]},{"id":28,"title":"Search insert position","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search","difficulty":"easy","status":"solved","timeSpent":55,"attempts":4,"lastTouched":6,"frequency":90,"tags":["binary-search","insert-position"],"companies":["Amazon","Microsoft","Google"],"concept":"Find position to insert element to maintain sorted order","explanation":"If element exists return its index, otherwise return the position where it would be inserted. Same as lower bound.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function searchInsert(nums, target) {\n  let left = 0, right = nums.length;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","python":"def searchInsert(nums, target):\n    left, right = 0, len(nums)\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid\n    return left","java":"public int searchInsert(int[] nums, int target) {\n  int left = 0, right = nums.length;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","cpp":"int searchInsert(vector<int>& nums, int target) {\n  int left = 0, right = nums.size();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] < target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}"},"challenge":"Return correct insert position","hints":["Same as lower bound","Find first >= target","Return left pointer"],"relatedProblems":[26]},{"id":29,"title":"Floor and Ceil in Sorted Array","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":75,"tags":["binary-search","floor-ceil"],"companies":["Amazon","Microsoft"],"concept":"Find floor (largest <= x) and ceil (smallest >= x)","explanation":"Floor is largest element <= target, Ceil is smallest element >= target. Use modified binary search for both.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function getFloorAndCeil(arr, x) {\n  let floor = -1, ceil = -1;\n  let left = 0, right = arr.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (arr[mid] === x) return [x, x];\n    if (arr[mid] < x) {\n      floor = arr[mid];\n      left = mid + 1;\n    } else {\n      ceil = arr[mid];\n      right = mid - 1;\n    }\n  }\n  return [floor, ceil];\n}","python":"def getFloorAndCeil(arr, x):\n    floor, ceil = -1, -1\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == x:\n            return [x, x]\n        if arr[mid] < x:\n            floor = arr[mid]\n            left = mid + 1\n        else:\n            ceil = arr[mid]\n            right = mid - 1\n    return [floor, ceil]","java":"public int[] getFloorAndCeil(int[] arr, int x) {\n  int floor = -1, ceil = -1;\n  int left = 0, right = arr.length - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (arr[mid] == x) return new int[]{x, x};\n    if (arr[mid] < x) {\n      floor = arr[mid];\n      left = mid + 1;\n    } else {\n      ceil = arr[mid];\n      right = mid - 1;\n    }\n  }\n  return new int[]{floor, ceil};\n}","cpp":"vector<int> getFloorAndCeil(vector<int>& arr, int x) {\n  int floor = -1, ceil = -1;\n  int left = 0, right = arr.size() - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (arr[mid] == x) return {x, x};\n    if (arr[mid] < x) {\n      floor = arr[mid];\n      left = mid + 1;\n    } else {\n      ceil = arr[mid];\n      right = mid - 1;\n    }\n  }\n  return {floor, ceil};\n}"},"challenge":"Handle edge cases when element not present","hints":["Floor: largest <= x","Ceil: smallest >= x","Track both while searching"],"relatedProblems":[26,27]},{"id":30,"title":"First and last occurrence","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search","difficulty":"easy","status":"solved","timeSpent":49,"attempts":3,"lastTouched":18,"frequency":95,"tags":["binary-search","first-last-occurrence"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Find first and last position of element","explanation":"Use binary search twice: once to find first occurrence (lower bound), once to find last occurrence (upper bound - 1).","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function searchRange(nums, target) {\n  const first = findFirst(nums, target);\n  if (first === -1) return [-1, -1];\n  const last = findLast(nums, target);\n  return [first, last];\n}\n\nfunction findFirst(nums, target) {\n  let left = 0, right = nums.length - 1, ans = -1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] === target) {\n      ans = mid;\n      right = mid - 1;\n    } else if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return ans;\n}\n\nfunction findLast(nums, target) {\n  let left = 0, right = nums.length - 1, ans = -1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] === target) {\n      ans = mid;\n      left = mid + 1;\n    } else if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return ans;\n}","python":"def searchRange(nums, target):\n    first = findFirst(nums, target)\n    if first == -1:\n        return [-1, -1]\n    last = findLast(nums, target)\n    return [first, last]\n\ndef findFirst(nums, target):\n    left, right, ans = 0, len(nums) - 1, -1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            ans = mid\n            right = mid - 1\n        elif nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return ans\n\ndef findLast(nums, target):\n    left, right, ans = 0, len(nums) - 1, -1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            ans = mid\n            left = mid + 1\n        elif nums[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return ans","java":"public int[] searchRange(int[] nums, int target) {\n  int first = findFirst(nums, target);\n  if (first == -1) return new int[]{-1, -1};\n  int last = findLast(nums, target);\n  return new int[]{first, last};\n}\n\nint findFirst(int[] nums, int target) {\n  int left = 0, right = nums.length - 1, ans = -1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) {\n      ans = mid;\n      right = mid - 1;\n    } else if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return ans;\n}\n\nint findLast(int[] nums, int target) {\n  int left = 0, right = nums.length - 1, ans = -1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) {\n      ans = mid;\n      left = mid + 1;\n    } else if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return ans;\n}","cpp":"vector<int> searchRange(vector<int>& nums, int target) {\n  int first = findFirst(nums, target);\n  if (first == -1) return {-1, -1};\n  int last = findLast(nums, target);\n  return {first, last};\n}\n\nint findFirst(vector<int>& nums, int target) {\n  int left = 0, right = nums.size() - 1, ans = -1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) {\n      ans = mid;\n      right = mid - 1;\n    } else if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return ans;\n}\n\nint findLast(vector<int>& nums, int target) {\n  int left = 0, right = nums.size() - 1, ans = -1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) {\n      ans = mid;\n      left = mid + 1;\n    } else if (nums[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return ans;\n}"},"challenge":"Handle duplicates efficiently","hints":["Binary search for first","Binary search for last","Move appropriately on find"],"relatedProblems":[26,27]},{"id":31,"title":"Search in rotated sorted array-I","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Rotated Array","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":100,"tags":["binary-search","rotated-array"],"companies":["Amazon","Microsoft","Facebook","Google","LinkedIn"],"concept":"Search in sorted array rotated at unknown pivot","explanation":"One half is always sorted. Check if target is in sorted half, otherwise search in other half.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function search(nums, target) {\n  let left = 0, right = nums.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] === target) return mid;\n    if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return -1;\n}","python":"def search(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[left] <= nums[mid]:\n            if nums[left] <= target < nums[mid]:\n                right = mid - 1\n            else:\n                left = mid + 1\n        else:\n            if nums[mid] < target <= nums[right]:\n                left = mid + 1\n            else:\n                right = mid - 1\n    return -1","java":"public int search(int[] nums, int target) {\n  int left = 0, right = nums.length - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) return mid;\n    if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return -1;\n}","cpp":"int search(vector<int>& nums, int target) {\n  int left = 0, right = nums.size() - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) return mid;\n    if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return -1;\n}"},"challenge":"Identify sorted half correctly","hints":["One half always sorted","Check target in sorted half","Adjust pointers"],"relatedProblems":[32,33,34]},{"id":32,"title":"Search in rotated sorted array-II","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Rotated Array","difficulty":"medium","status":"solved","timeSpent":57,"attempts":1,"lastTouched":29,"frequency":80,"tags":["binary-search","rotated-array","duplicates"],"companies":["Amazon","Microsoft"],"concept":"Search in rotated array with duplicates","explanation":"Handle duplicates by shrinking array when nums[left] === nums[mid]. Worst case O(n), average O(log n).","complexity":{"time":"O(log n) avg, O(n) worst","space":"O(1)"},"code":{"javascript":"function search(nums, target) {\n  let left = 0, right = nums.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] === target) return true;\n    if (nums[left] === nums[mid] && nums[mid] === nums[right]) {\n      left++;\n      right--;\n    } else if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return false;\n}","python":"def search(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return True\n        if nums[left] == nums[mid] == nums[right]:\n            left += 1\n            right -= 1\n        elif nums[left] <= nums[mid]:\n            if nums[left] <= target < nums[mid]:\n                right = mid - 1\n            else:\n                left = mid + 1\n        else:\n            if nums[mid] < target <= nums[right]:\n                left = mid + 1\n            else:\n                right = mid - 1\n    return False","java":"public boolean search(int[] nums, int target) {\n  int left = 0, right = nums.length - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) return true;\n    if (nums[left] == nums[mid] && nums[mid] == nums[right]) {\n      left++;\n      right--;\n    } else if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return false;\n}","cpp":"bool search(vector<int>& nums, int target) {\n  int left = 0, right = nums.size() - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] == target) return true;\n    if (nums[left] == nums[mid] && nums[mid] == nums[right]) {\n      left++;\n      right--;\n    } else if (nums[left] <= nums[mid]) {\n      if (target >= nums[left] && target < nums[mid]) right = mid - 1;\n      else left = mid + 1;\n    } else {\n      if (target > nums[mid] && target <= nums[right]) left = mid + 1;\n      else right = mid - 1;\n    }\n  }\n  return false;\n}"},"challenge":"Handle duplicates efficiently","hints":["Shrink when all equal","Identify sorted half","Handle edge cases"],"relatedProblems":[31,33,34]},{"id":33,"title":"Find minimum in Rotated Sorted Array","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Rotated Array","difficulty":"medium","status":"solved","timeSpent":68,"attempts":2,"lastTouched":23,"frequency":95,"tags":["binary-search","rotated-array","minimum"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Find minimum element in rotated sorted array","explanation":"Minimum is in unsorted half. Compare mid with right to determine which half contains minimum.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function findMin(nums) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return nums[left];\n}","python":"def findMin(nums):\n    left, right = 0, len(nums) - 1\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] > nums[right]:\n            left = mid + 1\n        else:\n            right = mid\n    return nums[left]","java":"public int findMin(int[] nums) {\n  int left = 0, right = nums.length - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return nums[left];\n}","cpp":"int findMin(vector<int>& nums) {\n  int left = 0, right = nums.size() - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return nums[left];\n}"},"challenge":"Handle array not rotated at all","hints":["Compare mid with right","Minimum in unsorted half","left < right condition"],"relatedProblems":[31,32,34]},{"id":34,"title":"Find out how many times the array is rotated","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Rotated Array","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":75,"tags":["binary-search","rotated-array","rotation-count"],"companies":["Amazon","Microsoft"],"concept":"Find rotation count = index of minimum element","explanation":"Number of rotations equals index of minimum element. Use binary search to find minimum index.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function findRotationCount(nums) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","python":"def findRotationCount(nums):\n    left, right = 0, len(nums) - 1\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] > nums[right]:\n            left = mid + 1\n        else:\n            right = mid\n    return left","java":"public int findRotationCount(int[] nums) {\n  int left = 0, right = nums.length - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","cpp":"int findRotationCount(vector<int>& nums) {\n  int left = 0, right = nums.size() - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] > nums[right]) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}"},"challenge":"Same as finding minimum index","hints":["Find minimum index","Use binary search","Index = rotation count"],"relatedProblems":[31,32,33]},{"id":35,"title":"Single element in sorted array","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search","difficulty":"medium","status":"solved","timeSpent":37,"attempts":1,"lastTouched":6,"frequency":85,"tags":["binary-search","single-element"],"companies":["Amazon","Microsoft","Google"],"concept":"Find element that appears only once in sorted array","explanation":"Before single element, pairs start at even index. After single element, pairs start at odd index. Use this to binary search.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function singleNonDuplicate(nums) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (mid % 2 === 1) mid--;\n    if (nums[mid] === nums[mid + 1]) left = mid + 2;\n    else right = mid;\n  }\n  return nums[left];\n}","python":"def singleNonDuplicate(nums):\n    left, right = 0, len(nums) - 1\n    while left < right:\n        mid = (left + right) // 2\n        if mid % 2 == 1:\n            mid -= 1\n        if nums[mid] == nums[mid + 1]:\n            left = mid + 2\n        else:\n            right = mid\n    return nums[left]","java":"public int singleNonDuplicate(int[] nums) {\n  int left = 0, right = nums.length - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (mid % 2 == 1) mid--;\n    if (nums[mid] == nums[mid + 1]) left = mid + 2;\n    else right = mid;\n  }\n  return nums[left];\n}","cpp":"int singleNonDuplicate(vector<int>& nums) {\n  int left = 0, right = nums.size() - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (mid % 2 == 1) mid--;\n    if (nums[mid] == nums[mid + 1]) left = mid + 2;\n    else right = mid;\n  }\n  return nums[left];\n}"},"challenge":"Understand pair index pattern","hints":["Pairs before: even,odd","Pairs after: odd,even","Adjust mid to even"],"relatedProblems":[]},{"id":36,"title":"Find square root of a number","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"easy","status":"attempted","timeSpent":11,"attempts":2,"lastTouched":5,"frequency":90,"tags":["binary-search","square-root"],"companies":["Amazon","Microsoft","Google"],"concept":"Find integer square root using binary search","explanation":"Binary search on answer. Find largest number whose square is <= given number.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function mySqrt(x) {\n  if (x < 2) return x;\n  let left = 1, right = Math.floor(x / 2);\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    const square = mid * mid;\n    if (square === x) return mid;\n    if (square < x) left = mid + 1;\n    else right = mid - 1;\n  }\n  return right;\n}","python":"def mySqrt(x):\n    if x < 2:\n        return x\n    left, right = 1, x // 2\n    while left <= right:\n        mid = (left + right) // 2\n        square = mid * mid\n        if square == x:\n            return mid\n        if square < x:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return right","java":"public int mySqrt(int x) {\n  if (x < 2) return x;\n  int left = 1, right = x / 2;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    long square = (long)mid * mid;\n    if (square == x) return mid;\n    if (square < x) left = mid + 1;\n    else right = mid - 1;\n  }\n  return right;\n}","cpp":"int mySqrt(int x) {\n  if (x < 2) return x;\n  int left = 1, right = x / 2;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    long long square = 1LL * mid * mid;\n    if (square == x) return mid;\n    if (square < x) left = mid + 1;\n    else right = mid - 1;\n  }\n  return right;\n}"},"challenge":"Handle large numbers without overflow","hints":["Binary search on answer","Use long to avoid overflow","Return right when done"],"relatedProblems":[37]},{"id":37,"title":"Find Nth root of a number","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"medium","status":"solved","timeSpent":54,"attempts":1,"lastTouched":3,"frequency":70,"tags":["binary-search","nth-root"],"companies":["Amazon","Microsoft"],"concept":"Find nth root using binary search with precision","explanation":"Binary search on answer with precision. Find number which when raised to power n equals given number.","complexity":{"time":"O(log(precision))","space":"O(1)"},"code":{"javascript":"function nthRoot(n, m) {\n  let low = 1, high = m;\n  const eps = 1e-6;\n  while (high - low > eps) {\n    const mid = (low + high) / 2;\n    if (Math.pow(mid, n) < m) low = mid;\n    else high = mid;\n  }\n  return low.toFixed(6);\n}","python":"def nthRoot(n, m):\n    low, high = 1, m\n    eps = 1e-6\n    while high - low > eps:\n        mid = (low + high) / 2\n        if mid ** n < m:\n            low = mid\n        else:\n            high = mid\n    return round(low, 6)","java":"public double nthRoot(int n, int m) {\n  double low = 1, high = m;\n  double eps = 1e-6;\n  while (high - low > eps) {\n    double mid = (low + high) / 2;\n    if (Math.pow(mid, n) < m) low = mid;\n    else high = mid;\n  }\n  return low;\n}","cpp":"double nthRoot(int n, int m) {\n  double low = 1, high = m;\n  double eps = 1e-6;\n  while (high - low > eps) {\n    double mid = (low + high) / 2;\n    if (pow(mid, n) < m) low = mid;\n    else high = mid;\n  }\n  return low;\n}"},"challenge":"Precision handling","hints":["Binary search with epsilon","Check mid^n","Adjust range"],"relatedProblems":[36]},{"id":38,"title":"Find the smallest divisor","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":75,"tags":["binary-search","minimize-divisor"],"companies":["Amazon","Microsoft"],"concept":"Find smallest divisor such that sum of divisions <= threshold","explanation":"Binary search on divisor. For each divisor, calculate sum of ceil divisions. Find smallest divisor where sum <= threshold.","complexity":{"time":"O(n log(max))","space":"O(1)"},"code":{"javascript":"function smallestDivisor(nums, threshold) {\n  let left = 1, right = Math.max(...nums);\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    const sum = nums.reduce((acc, num) => acc + Math.ceil(num / mid), 0);\n    if (sum <= threshold) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","python":"def smallestDivisor(nums, threshold):\n    left, right = 1, max(nums)\n    while left < right:\n        mid = (left + right) // 2\n        total = sum((num + mid - 1) // mid for num in nums)\n        if total <= threshold:\n            right = mid\n        else:\n            left = mid + 1\n    return left","java":"public int smallestDivisor(int[] nums, int threshold) {\n  int left = 1, right = Arrays.stream(nums).max().getAsInt();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    int sum = 0;\n    for (int num : nums) sum += (num + mid - 1) / mid;\n    if (sum <= threshold) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","cpp":"int smallestDivisor(vector<int>& nums, int threshold) {\n  int left = 1, right = *max_element(nums.begin(), nums.end());\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    int sum = 0;\n    for (int num : nums) sum += (num + mid - 1) / mid;\n    if (sum <= threshold) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}"},"challenge":"Minimize the divisor","hints":["Binary search on answer","Calculate sum of divisions","Find minimum valid divisor"],"relatedProblems":[39,40,41,42]},{"id":39,"title":"Koko eating bananas","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"medium","status":"revisit","timeSpent":12,"attempts":2,"lastTouched":1,"frequency":90,"tags":["binary-search","koko-eating-bananas"],"companies":["Amazon","Google","Facebook"],"concept":"Find minimum eating speed to finish all bananas within hours","explanation":"Binary search on eating speed. For each speed, calculate total hours needed. Find minimum speed where hours <= h.","complexity":{"time":"O(n log(max))","space":"O(1)"},"code":{"javascript":"function minEatingSpeed(piles, h) {\n  let left = 1, right = Math.max(...piles);\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    const hours = piles.reduce((acc, pile) => acc + Math.ceil(pile / mid), 0);\n    if (hours <= h) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","python":"def minEatingSpeed(piles, h):\n    left, right = 1, max(piles)\n    while left < right:\n        mid = (left + right) // 2\n        hours = sum((pile + mid - 1) // mid for pile in piles)\n        if hours <= h:\n            right = mid\n        else:\n            left = mid + 1\n    return left","java":"public int minEatingSpeed(int[] piles, int h) {\n  int left = 1, right = Arrays.stream(piles).max().getAsInt();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    int hours = 0;\n    for (int pile : piles) hours += (pile + mid - 1) / mid;\n    if (hours <= h) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","cpp":"int minEatingSpeed(vector<int>& piles, int h) {\n  int left = 1, right = *max_element(piles.begin(), piles.end());\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    int hours = 0;\n    for (int pile : piles) hours += (pile + mid - 1) / mid;\n    if (hours <= h) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}"},"challenge":"Calculate hours correctly","hints":["Binary search on speed","Calculate total hours","Minimize speed"],"relatedProblems":[38,40,41,42]},{"id":40,"title":"Minimum days to make M bouquets","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"medium","status":"attempted","timeSpent":32,"attempts":4,"lastTouched":21,"frequency":80,"tags":["binary-search","bouquets"],"companies":["Amazon","Microsoft"],"concept":"Find minimum days to make m bouquets with k adjacent flowers each","explanation":"Binary search on days. For each day, count how many bouquets can be made with bloomed flowers. Check if >= m bouquets possible.","complexity":{"time":"O(n log(max))","space":"O(1)"},"code":{"javascript":"function minDays(bloomDay, m, k) {\n  if (bloomDay.length < m * k) return -1;\n  let left = Math.min(...bloomDay), right = Math.max(...bloomDay);\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    let bouquets = 0, flowers = 0;\n    for (let day of bloomDay) {\n      if (day <= mid) {\n        flowers++;\n        if (flowers === k) {\n          bouquets++;\n          flowers = 0;\n        }\n      } else flowers = 0;\n    }\n    if (bouquets >= m) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","python":"def minDays(bloomDay, m, k):\n    if len(bloomDay) < m * k:\n        return -1\n    left, right = min(bloomDay), max(bloomDay)\n    while left < right:\n        mid = (left + right) // 2\n        bouquets = flowers = 0\n        for day in bloomDay:\n            if day <= mid:\n                flowers += 1\n                if flowers == k:\n                    bouquets += 1\n                    flowers = 0\n            else:\n                flowers = 0\n        if bouquets >= m:\n            right = mid\n        else:\n            left = mid + 1\n    return left","java":"public int minDays(int[] bloomDay, int m, int k) {\n  if (bloomDay.length < (long)m * k) return -1;\n  int left = Arrays.stream(bloomDay).min().getAsInt();\n  int right = Arrays.stream(bloomDay).max().getAsInt();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    int bouquets = 0, flowers = 0;\n    for (int day : bloomDay) {\n      if (day <= mid) {\n        flowers++;\n        if (flowers == k) {\n          bouquets++;\n          flowers = 0;\n        }\n      } else flowers = 0;\n    }\n    if (bouquets >= m) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","cpp":"int minDays(vector<int>& bloomDay, int m, int k) {\n  if (bloomDay.size() < (long long)m * k) return -1;\n  int left = *min_element(bloomDay.begin(), bloomDay.end());\n  int right = *max_element(bloomDay.begin(), bloomDay.end());\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    int bouquets = 0, flowers = 0;\n    for (int day : bloomDay) {\n      if (day <= mid) {\n        flowers++;\n        if (flowers == k) {\n          bouquets++;\n          flowers = 0;\n        }\n      } else flowers = 0;\n    }\n    if (bouquets >= m) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}"},"challenge":"Count bouquets correctly with adjacent constraint","hints":["Binary search on days","Count adjacent bloomed flowers","Form bouquets"],"relatedProblems":[38,39,41,42]},{"id":41,"title":"Aggressive Cows","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"hard","status":"solved","timeSpent":51,"attempts":2,"lastTouched":26,"frequency":90,"tags":["binary-search","aggressive-cows"],"companies":["Amazon","Microsoft","Google"],"concept":"Place cows in stalls to maximize minimum distance between any two cows","explanation":"Binary search on minimum distance. Check if cows can be placed with at least 'mid' distance apart. Maximize this distance.","complexity":{"time":"O(n log(max))","space":"O(1)"},"code":{"javascript":"function aggressiveCows(stalls, k) {\n  stalls.sort((a, b) => a - b);\n  let left = 1, right = stalls[stalls.length - 1] - stalls[0];\n  while (left < right) {\n    const mid = Math.floor((left + right + 1) / 2);\n    if (canPlace(stalls, k, mid)) left = mid;\n    else right = mid - 1;\n  }\n  return left;\n}\n\nfunction canPlace(stalls, k, minDist) {\n  let count = 1, lastPos = stalls[0];\n  for (let i = 1; i < stalls.length; i++) {\n    if (stalls[i] - lastPos >= minDist) {\n      count++;\n      lastPos = stalls[i];\n    }\n  }\n  return count >= k;\n}","python":"def aggressiveCows(stalls, k):\n    stalls.sort()\n    left, right = 1, stalls[-1] - stalls[0]\n    while left < right:\n        mid = (left + right + 1) // 2\n        if canPlace(stalls, k, mid):\n            left = mid\n        else:\n            right = mid - 1\n    return left\n\ndef canPlace(stalls, k, min_dist):\n    count = 1\n    last_pos = stalls[0]\n    for i in range(1, len(stalls)):\n        if stalls[i] - last_pos >= min_dist:\n            count += 1\n            last_pos = stalls[i]\n    return count >= k","java":"public int aggressiveCows(int[] stalls, int k) {\n  Arrays.sort(stalls);\n  int left = 1, right = stalls[stalls.length - 1] - stalls[0];\n  while (left < right) {\n    int mid = left + (right - left + 1) / 2;\n    if (canPlace(stalls, k, mid)) left = mid;\n    else right = mid - 1;\n  }\n  return left;\n}\n\nboolean canPlace(int[] stalls, int k, int minDist) {\n  int count = 1, lastPos = stalls[0];\n  for (int i = 1; i < stalls.length; i++) {\n    if (stalls[i] - lastPos >= minDist) {\n      count++;\n      lastPos = stalls[i];\n    }\n  }\n  return count >= k;\n}","cpp":"int aggressiveCows(vector<int>& stalls, int k) {\n  sort(stalls.begin(), stalls.end());\n  int left = 1, right = stalls.back() - stalls[0];\n  while (left < right) {\n    int mid = left + (right - left + 1) / 2;\n    if (canPlace(stalls, k, mid)) left = mid;\n    else right = mid - 1;\n  }\n  return left;\n}\n\nbool canPlace(vector<int>& stalls, int k, int minDist) {\n  int count = 1, lastPos = stalls[0];\n  for (int i = 1; i < stalls.size(); i++) {\n    if (stalls[i] - lastPos >= minDist) {\n      count++;\n      lastPos = stalls[i];\n    }\n  }\n  return count >= k;\n}"},"challenge":"Maximize minimum distance","hints":["Sort stalls first","Binary search on distance","Greedy placement"],"relatedProblems":[38,39,40,42]},{"id":42,"title":"Book Allocation Problem","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"hard","status":"bookmarked","timeSpent":45,"attempts":1,"lastTouched":9,"frequency":95,"tags":["binary-search","book-allocation"],"companies":["Amazon","Microsoft","Google"],"concept":"Allocate books to students to minimize maximum pages","explanation":"Binary search on maximum pages. Check if books can be allocated to m students such that no student gets more than 'mid' pages.","complexity":{"time":"O(n log(sum))","space":"O(1)"},"code":{"javascript":"function allocateBooks(arr, m) {\n  if (m > arr.length) return -1;\n  let left = Math.max(...arr), right = arr.reduce((a, b) => a + b, 0);\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (isPossible(arr, m, mid)) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}\n\nfunction isPossible(arr, m, maxPages) {\n  let students = 1, pages = 0;\n  for (let pages of arr) {\n    if (pages + pages <= maxPages) {\n      pages += pages;\n    } else {\n      students++;\n      pages = pages;\n    }\n  }\n  return students <= m;\n}","python":"def allocateBooks(arr, m):\n    if m > len(arr):\n        return -1\n    left, right = max(arr), sum(arr)\n    while left < right:\n        mid = (left + right) // 2\n        if isPossible(arr, m, mid):\n            right = mid\n        else:\n            left = mid + 1\n    return left\n\ndef isPossible(arr, m, max_pages):\n    students = 1\n    pages = 0\n    for book in arr:\n        if pages + book <= max_pages:\n            pages += book\n        else:\n            students += 1\n            pages = book\n    return students <= m","java":"public int allocateBooks(int[] arr, int m) {\n  if (m > arr.length) return -1;\n  int left = Arrays.stream(arr).max().getAsInt();\n  int right = Arrays.stream(arr).sum();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (isPossible(arr, m, mid)) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}\n\nboolean isPossible(int[] arr, int m, int maxPages) {\n  int students = 1, pages = 0;\n  for (int book : arr) {\n    if (pages + book <= maxPages) {\n      pages += book;\n    } else {\n      students++;\n      pages = book;\n    }\n  }\n  return students <= m;\n}","cpp":"int allocateBooks(vector<int>& arr, int m) {\n  if (m > arr.size()) return -1;\n  int left = *max_element(arr.begin(), arr.end());\n  int right = accumulate(arr.begin(), arr.end(), 0);\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (isPossible(arr, m, mid)) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}\n\nbool isPossible(vector<int>& arr, int m, int maxPages) {\n  int students = 1, pages = 0;\n  for (int book : arr) {\n    if (pages + book <= maxPages) {\n      pages += book;\n    } else {\n      students++;\n      pages = book;\n    }\n  }\n  return students <= m;\n}"},"challenge":"Minimize maximum pages","hints":["Binary search on max pages","Check feasibility","Minimize maximum"],"relatedProblems":[38,39,40,41]},{"id":43,"title":"Find peak element","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search","difficulty":"medium","status":"solved","timeSpent":60,"attempts":1,"lastTouched":0,"frequency":90,"tags":["binary-search","peak-element"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Find element greater than its neighbors","explanation":"Peak element is greater than neighbors. Use binary search: if mid is not peak, move towards the greater neighbor.","complexity":{"time":"O(log n)","space":"O(1)"},"code":{"javascript":"function findPeakElement(nums) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (nums[mid] > nums[mid + 1]) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","python":"def findPeakElement(nums):\n    left, right = 0, len(nums) - 1\n    while left < right:\n        mid = (left + right) // 2\n        if nums[mid] > nums[mid + 1]:\n            right = mid\n        else:\n            left = mid + 1\n    return left","java":"public int findPeakElement(int[] nums) {\n  int left = 0, right = nums.length - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] > nums[mid + 1]) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}","cpp":"int findPeakElement(vector<int>& nums) {\n  int left = 0, right = nums.size() - 1;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (nums[mid] > nums[mid + 1]) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}"},"challenge":"Multiple peaks possible","hints":["Move towards greater neighbor","Compare with right neighbor","Return any peak"],"relatedProblems":[51]},{"id":44,"title":"Median of 2 sorted arrays","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Partition","difficulty":"hard","status":"solved","timeSpent":25,"attempts":4,"lastTouched":16,"frequency":100,"tags":["binary-search","median","partition"],"companies":["Amazon","Microsoft","Facebook","Google","Apple","Adobe"],"concept":"Find median of two sorted arrays in log time","explanation":"Partition both arrays such that left half has equal elements to right half. Binary search on partition position.","complexity":{"time":"O(log(min(m,n)))","space":"O(1)"},"code":{"javascript":"function findMedianSortedArrays(nums1, nums2) {\n  if (nums1.length > nums2.length) [nums1, nums2] = [nums2, nums1];\n  const m = nums1.length, n = nums2.length;\n  let left = 0, right = m;\n  while (left <= right) {\n    const partitionX = Math.floor((left + right) / 2);\n    const partitionY = Math.floor((m + n + 1) / 2) - partitionX;\n    const maxLeftX = partitionX === 0 ? -Infinity : nums1[partitionX - 1];\n    const minRightX = partitionX === m ? Infinity : nums1[partitionX];\n    const maxLeftY = partitionY === 0 ? -Infinity : nums2[partitionY - 1];\n    const minRightY = partitionY === n ? Infinity : nums2[partitionY];\n    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n      if ((m + n) % 2 === 0) {\n        return (Math.max(maxLeftX, maxLeftY) + Math.min(minRightX, minRightY)) / 2;\n      } else {\n        return Math.max(maxLeftX, maxLeftY);\n      }\n    } else if (maxLeftX > minRightY) {\n      right = partitionX - 1;\n    } else {\n      left = partitionX + 1;\n    }\n  }\n}","python":"def findMedianSortedArrays(nums1, nums2):\n    if len(nums1) > len(nums2):\n        nums1, nums2 = nums2, nums1\n    m, n = len(nums1), len(nums2)\n    left, right = 0, m\n    while left <= right:\n        partition_x = (left + right) // 2\n        partition_y = (m + n + 1) // 2 - partition_x\n        max_left_x = float('-inf') if partition_x == 0 else nums1[partition_x - 1]\n        min_right_x = float('inf') if partition_x == m else nums1[partition_x]\n        max_left_y = float('-inf') if partition_y == 0 else nums2[partition_y - 1]\n        min_right_y = float('inf') if partition_y == n else nums2[partition_y]\n        if max_left_x <= min_right_y and max_left_y <= min_right_x:\n            if (m + n) % 2 == 0:\n                return (max(max_left_x, max_left_y) + min(min_right_x, min_right_y)) / 2\n            else:\n                return max(max_left_x, max_left_y)\n        elif max_left_x > min_right_y:\n            right = partition_x - 1\n        else:\n            left = partition_x + 1","java":"public double findMedianSortedArrays(int[] nums1, int[] nums2) {\n  if (nums1.length > nums2.length) {\n    int[] temp = nums1; nums1 = nums2; nums2 = temp;\n  }\n  int m = nums1.length, n = nums2.length;\n  int left = 0, right = m;\n  while (left <= right) {\n    int partitionX = (left + right) / 2;\n    int partitionY = (m + n + 1) / 2 - partitionX;\n    int maxLeftX = (partitionX == 0) ? Integer.MIN_VALUE : nums1[partitionX - 1];\n    int minRightX = (partitionX == m) ? Integer.MAX_VALUE : nums1[partitionX];\n    int maxLeftY = (partitionY == 0) ? Integer.MIN_VALUE : nums2[partitionY - 1];\n    int minRightY = (partitionY == n) ? Integer.MAX_VALUE : nums2[partitionY];\n    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n      if ((m + n) % 2 == 0) {\n        return (Math.max(maxLeftX, maxLeftY) + Math.min(minRightX, minRightY)) / 2.0;\n      } else {\n        return Math.max(maxLeftX, maxLeftY);\n      }\n    } else if (maxLeftX > minRightY) {\n      right = partitionX - 1;\n    } else {\n      left = partitionX + 1;\n    }\n  }\n  return 0;\n}","cpp":"double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {\n  if (nums1.size() > nums2.size()) swap(nums1, nums2);\n  int m = nums1.size(), n = nums2.size();\n  int left = 0, right = m;\n  while (left <= right) {\n    int partitionX = (left + right) / 2;\n    int partitionY = (m + n + 1) / 2 - partitionX;\n    int maxLeftX = (partitionX == 0) ? INT_MIN : nums1[partitionX - 1];\n    int minRightX = (partitionX == m) ? INT_MAX : nums1[partitionX];\n    int maxLeftY = (partitionY == 0) ? INT_MIN : nums2[partitionY - 1];\n    int minRightY = (partitionY == n) ? INT_MAX : nums2[partitionY];\n    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n      if ((m + n) % 2 == 0) {\n        return (max(maxLeftX, maxLeftY) + min(minRightX, minRightY)) / 2.0;\n      } else {\n        return max(maxLeftX, maxLeftY);\n      }\n    } else if (maxLeftX > minRightY) {\n      right = partitionX - 1;\n    } else {\n      left = partitionX + 1;\n    }\n  }\n  return 0;\n}"},"challenge":"Handle all partition cases","hints":["Partition smaller array","Find correct partition","Handle edge cases"],"relatedProblems":[45]},{"id":45,"title":"Kth element of 2 sorted arrays","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Partition","difficulty":"medium","status":"solved","timeSpent":30,"attempts":2,"lastTouched":25,"frequency":85,"tags":["binary-search","kth-element","partition"],"companies":["Amazon","Microsoft","Google"],"concept":"Find kth smallest element in two sorted arrays","explanation":"Similar to median problem. Partition both arrays such that left side has k elements. Binary search on partition.","complexity":{"time":"O(log(min(m,n)))","space":"O(1)"},"code":{"javascript":"function findKthElement(arr1, arr2, k) {\n  if (arr1.length > arr2.length) [arr1, arr2] = [arr2, arr1];\n  const m = arr1.length, n = arr2.length;\n  let left = Math.max(0, k - n), right = Math.min(k, m);\n  while (left <= right) {\n    const partitionX = Math.floor((left + right) / 2);\n    const partitionY = k - partitionX;\n    const maxLeftX = partitionX === 0 ? -Infinity : arr1[partitionX - 1];\n    const minRightX = partitionX === m ? Infinity : arr1[partitionX];\n    const maxLeftY = partitionY === 0 ? -Infinity : arr2[partitionY - 1];\n    const minRightY = partitionY === n ? Infinity : arr2[partitionY];\n    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n      return Math.max(maxLeftX, maxLeftY);\n    } else if (maxLeftX > minRightY) {\n      right = partitionX - 1;\n    } else {\n      left = partitionX + 1;\n    }\n  }\n}","python":"def findKthElement(arr1, arr2, k):\n    if len(arr1) > len(arr2):\n        arr1, arr2 = arr2, arr1\n    m, n = len(arr1), len(arr2)\n    left, right = max(0, k - n), min(k, m)\n    while left <= right:\n        partition_x = (left + right) // 2\n        partition_y = k - partition_x\n        max_left_x = float('-inf') if partition_x == 0 else arr1[partition_x - 1]\n        min_right_x = float('inf') if partition_x == m else arr1[partition_x]\n        max_left_y = float('-inf') if partition_y == 0 else arr2[partition_y - 1]\n        min_right_y = float('inf') if partition_y == n else arr2[partition_y]\n        if max_left_x <= min_right_y and max_left_y <= min_right_x:\n            return max(max_left_x, max_left_y)\n        elif max_left_x > min_right_y:\n            right = partition_x - 1\n        else:\n            left = partition_x + 1","java":"public int findKthElement(int[] arr1, int[] arr2, int k) {\n  if (arr1.length > arr2.length) {\n    int[] temp = arr1; arr1 = arr2; arr2 = temp;\n  }\n  int m = arr1.length, n = arr2.length;\n  int left = Math.max(0, k - n), right = Math.min(k, m);\n  while (left <= right) {\n    int partitionX = (left + right) / 2;\n    int partitionY = k - partitionX;\n    int maxLeftX = (partitionX == 0) ? Integer.MIN_VALUE : arr1[partitionX - 1];\n    int minRightX = (partitionX == m) ? Integer.MAX_VALUE : arr1[partitionX];\n    int maxLeftY = (partitionY == 0) ? Integer.MIN_VALUE : arr2[partitionY - 1];\n    int minRightY = (partitionY == n) ? Integer.MAX_VALUE : arr2[partitionY];\n    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n      return Math.max(maxLeftX, maxLeftY);\n    } else if (maxLeftX > minRightY) {\n      right = partitionX - 1;\n    } else {\n      left = partitionX + 1;\n    }\n  }\n  return 0;\n}","cpp":"int findKthElement(vector<int>& arr1, vector<int>& arr2, int k) {\n  if (arr1.size() > arr2.size()) swap(arr1, arr2);\n  int m = arr1.size(), n = arr2.size();\n  int left = max(0, k - n), right = min(k, m);\n  while (left <= right) {\n    int partitionX = (left + right) / 2;\n    int partitionY = k - partitionX;\n    int maxLeftX = (partitionX == 0) ? INT_MIN : arr1[partitionX - 1];\n    int minRightX = (partitionX == m) ? INT_MAX : arr1[partitionX];\n    int maxLeftY = (partitionY == 0) ? INT_MIN : arr2[partitionY - 1];\n    int minRightY = (partitionY == n) ? INT_MAX : arr2[partitionY];\n    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {\n      return max(maxLeftX, maxLeftY);\n    } else if (maxLeftX > minRightY) {\n      right = partitionX - 1;\n    } else {\n      left = partitionX + 1;\n    }\n  }\n  return 0;\n}"},"challenge":"Handle k larger than one array","hints":["Partition smaller array","Ensure k elements on left","Find max of left parts"],"relatedProblems":[44]},{"id":46,"title":"Minimize Max Distance to Gas Station","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"hard","status":"attempted","timeSpent":32,"attempts":1,"lastTouched":25,"frequency":70,"tags":["binary-search","gas-station"],"companies":["Google","Amazon"],"concept":"Add k gas stations to minimize maximum distance between stations","explanation":"Binary search on maximum distance. For each distance, calculate how many stations needed. Find minimum possible maximum distance.","complexity":{"time":"O(n log(range))","space":"O(1)"},"code":{"javascript":"function minmaxGasDist(stations, k) {\n  let left = 0, right = stations[stations.length - 1] - stations[0];\n  while (right - left > 1e-6) {\n    const mid = (left + right) / 2;\n    let needed = 0;\n    for (let i = 1; i < stations.length; i++) {\n      needed += Math.ceil((stations[i] - stations[i - 1]) / mid) - 1;\n    }\n    if (needed <= k) right = mid;\n    else left = mid;\n  }\n  return right;\n}","python":"def minmaxGasDist(stations, k):\n    left, right = 0, stations[-1] - stations[0]\n    while right - left > 1e-6:\n        mid = (left + right) / 2\n        needed = sum(int((stations[i] - stations[i-1]) / mid) for i in range(1, len(stations)))\n        if needed <= k:\n            right = mid\n        else:\n            left = mid\n    return right","java":"public double minmaxGasDist(int[] stations, int k) {\n  double left = 0, right = stations[stations.length - 1] - stations[0];\n  while (right - left > 1e-6) {\n    double mid = (left + right) / 2;\n    int needed = 0;\n    for (int i = 1; i < stations.length; i++) {\n      needed += Math.ceil((stations[i] - stations[i - 1]) / mid) - 1;\n    }\n    if (needed <= k) right = mid;\n    else left = mid;\n  }\n  return right;\n}","cpp":"double minmaxGasDist(vector<int>& stations, int k) {\n  double left = 0, right = stations.back() - stations[0];\n  while (right - left > 1e-6) {\n    double mid = (left + right) / 2;\n    int needed = 0;\n    for (int i = 1; i < stations.size(); i++) {\n      needed += ceil((stations[i] - stations[i - 1]) / mid) - 1;\n    }\n    if (needed <= k) right = mid;\n    else left = mid;\n  }\n  return right;\n}"},"challenge":"Precision handling with binary search","hints":["Binary search on distance","Calculate stations needed","Minimize maximum"],"relatedProblems":[38,39,40,41,42]},{"id":47,"title":"Split array - largest sum","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search on Answer","difficulty":"hard","status":"stuck","timeSpent":37,"attempts":4,"lastTouched":18,"frequency":85,"tags":["binary-search","split-array"],"companies":["Amazon","Microsoft","Google","Facebook"],"concept":"Split array into m subarrays to minimize largest sum","explanation":"Binary search on largest sum. Check if array can be split into m subarrays with each sum <= mid. Minimize this maximum sum.","complexity":{"time":"O(n log(sum))","space":"O(1)"},"code":{"javascript":"function splitArray(nums, m) {\n  let left = Math.max(...nums), right = nums.reduce((a, b) => a + b, 0);\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (canSplit(nums, m, mid)) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}\n\nfunction canSplit(nums, m, maxSum) {\n  let count = 1, sum = 0;\n  for (let num of nums) {\n    if (sum + num <= maxSum) {\n      sum += num;\n    } else {\n      count++;\n      sum = num;\n    }\n  }\n  return count <= m;\n}","python":"def splitArray(nums, m):\n    left, right = max(nums), sum(nums)\n    while left < right:\n        mid = (left + right) // 2\n        if canSplit(nums, m, mid):\n            right = mid\n        else:\n            left = mid + 1\n    return left\n\ndef canSplit(nums, m, max_sum):\n    count = 1\n    total = 0\n    for num in nums:\n        if total + num <= max_sum:\n            total += num\n        else:\n            count += 1\n            total = num\n    return count <= m","java":"public int splitArray(int[] nums, int m) {\n  int left = Arrays.stream(nums).max().getAsInt();\n  int right = Arrays.stream(nums).sum();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (canSplit(nums, m, mid)) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}\n\nboolean canSplit(int[] nums, int m, int maxSum) {\n  int count = 1, sum = 0;\n  for (int num : nums) {\n    if (sum + num <= maxSum) {\n      sum += num;\n    } else {\n      count++;\n      sum = num;\n    }\n  }\n  return count <= m;\n}","cpp":"int splitArray(vector<int>& nums, int m) {\n  int left = *max_element(nums.begin(), nums.end());\n  int right = accumulate(nums.begin(), nums.end(), 0);\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (canSplit(nums, m, mid)) right = mid;\n    else left = mid + 1;\n  }\n  return left;\n}\n\nbool canSplit(vector<int>& nums, int m, int maxSum) {\n  int count = 1, sum = 0;\n  for (int num : nums) {\n    if (sum + num <= maxSum) {\n      sum += num;\n    } else {\n      count++;\n      sum = num;\n    }\n  }\n  return count <= m;\n}"},"challenge":"Minimize largest sum among m subarrays","hints":["Binary search on sum","Check if m subarrays possible","Minimize maximum"],"relatedProblems":[38,39,40,41,42,46]},{"id":48,"title":"Find row with maximum 1's","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Matrix","difficulty":"easy","status":"solved","timeSpent":65,"attempts":4,"lastTouched":10,"frequency":75,"tags":["binary-search","matrix","max-ones"],"companies":["Amazon","Microsoft"],"concept":"Find row with maximum number of 1s in binary sorted matrix","explanation":"For each row, use binary search to find first 1. Track row with maximum 1s. Can optimize by starting from top-right.","complexity":{"time":"O(m log n)","space":"O(1)"},"code":{"javascript":"function findMaxRow(mat) {\n  let maxRow = 0, maxCount = 0;\n  for (let i = 0; i < mat.length; i++) {\n    const count = countOnes(mat[i]);\n    if (count > maxCount) {\n      maxCount = count;\n      maxRow = i;\n    }\n  }\n  return maxRow;\n}\n\nfunction countOnes(row) {\n  let left = 0, right = row.length;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (row[mid] === 0) left = mid + 1;\n    else right = mid;\n  }\n  return row.length - left;\n}","python":"def findMaxRow(mat):\n    max_row, max_count = 0, 0\n    for i, row in enumerate(mat):\n        count = countOnes(row)\n        if count > max_count:\n            max_count = count\n            max_row = i\n    return max_row\n\ndef countOnes(row):\n    left, right = 0, len(row)\n    while left < right:\n        mid = (left + right) // 2\n        if row[mid] == 0:\n            left = mid + 1\n        else:\n            right = mid\n    return len(row) - left","java":"public int findMaxRow(int[][] mat) {\n  int maxRow = 0, maxCount = 0;\n  for (int i = 0; i < mat.length; i++) {\n    int count = countOnes(mat[i]);\n    if (count > maxCount) {\n      maxCount = count;\n      maxRow = i;\n    }\n  }\n  return maxRow;\n}\n\nint countOnes(int[] row) {\n  int left = 0, right = row.length;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (row[mid] == 0) left = mid + 1;\n    else right = mid;\n  }\n  return row.length - left;\n}","cpp":"int findMaxRow(vector<vector<int>>& mat) {\n  int maxRow = 0, maxCount = 0;\n  for (int i = 0; i < mat.size(); i++) {\n    int count = countOnes(mat[i]);\n    if (count > maxCount) {\n      maxCount = count;\n      maxRow = i;\n    }\n  }\n  return maxRow;\n}\n\nint countOnes(vector<int>& row) {\n  int left = 0, right = row.size();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (row[mid] == 0) left = mid + 1;\n    else right = mid;\n  }\n  return row.size() - left;\n}"},"challenge":"Optimize with O(m+n) approach","hints":["Binary search each row","Count 1s efficiently","Track maximum"],"relatedProblems":[49,50,51,52]},{"id":49,"title":"Search in a 2D matrix","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Matrix","difficulty":"medium","status":"attempted","timeSpent":49,"attempts":3,"lastTouched":25,"frequency":95,"tags":["binary-search","matrix","2d-search"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Search in sorted 2D matrix (row and column sorted)","explanation":"Treat matrix as 1D sorted array. Use binary search. Convert mid to row and column indices.","complexity":{"time":"O(log(m*n))","space":"O(1)"},"code":{"javascript":"function searchMatrix(matrix, target) {\n  if (!matrix.length || !matrix[0].length) return false;\n  const m = matrix.length, n = matrix[0].length;\n  let left = 0, right = m * n - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    const row = Math.floor(mid / n), col = mid % n;\n    if (matrix[row][col] === target) return true;\n    if (matrix[row][col] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return false;\n}","python":"def searchMatrix(matrix, target):\n    if not matrix or not matrix[0]:\n        return False\n    m, n = len(matrix), len(matrix[0])\n    left, right = 0, m * n - 1\n    while left <= right:\n        mid = (left + right) // 2\n        row, col = mid // n, mid % n\n        if matrix[row][col] == target:\n            return True\n        if matrix[row][col] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return False","java":"public boolean searchMatrix(int[][] matrix, int target) {\n  if (matrix.length == 0 || matrix[0].length == 0) return false;\n  int m = matrix.length, n = matrix[0].length;\n  int left = 0, right = m * n - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    int row = mid / n, col = mid % n;\n    if (matrix[row][col] == target) return true;\n    if (matrix[row][col] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return false;\n}","cpp":"bool searchMatrix(vector<vector<int>>& matrix, int target) {\n  if (matrix.empty() || matrix[0].empty()) return false;\n  int m = matrix.size(), n = matrix[0].size();\n  int left = 0, right = m * n - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    int row = mid / n, col = mid % n;\n    if (matrix[row][col] == target) return true;\n    if (matrix[row][col] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return false;\n}"},"challenge":"Treat as 1D array","hints":["Convert index to row,col","Binary search","Matrix fully sorted"],"relatedProblems":[48,50,51,52]},{"id":50,"title":"Search in 2D matrix - II","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Matrix","difficulty":"medium","status":"attempted","timeSpent":66,"attempts":3,"lastTouched":23,"frequency":95,"tags":["binary-search","matrix","2d-search"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Search where each row and column is sorted","explanation":"Start from top-right or bottom-left. Eliminate row or column based on comparison with target.","complexity":{"time":"O(m + n)","space":"O(1)"},"code":{"javascript":"function searchMatrix(matrix, target) {\n  if (!matrix.length || !matrix[0].length) return false;\n  let row = 0, col = matrix[0].length - 1;\n  while (row < matrix.length && col >= 0) {\n    if (matrix[row][col] === target) return true;\n    if (matrix[row][col] < target) row++;\n    else col--;\n  }\n  return false;\n}","python":"def searchMatrix(matrix, target):\n    if not matrix or not matrix[0]:\n        return False\n    row, col = 0, len(matrix[0]) - 1\n    while row < len(matrix) and col >= 0:\n        if matrix[row][col] == target:\n            return True\n        if matrix[row][col] < target:\n            row += 1\n        else:\n            col -= 1\n    return False","java":"public boolean searchMatrix(int[][] matrix, int target) {\n  if (matrix.length == 0 || matrix[0].length == 0) return false;\n  int row = 0, col = matrix[0].length - 1;\n  while (row < matrix.length && col >= 0) {\n    if (matrix[row][col] == target) return true;\n    if (matrix[row][col] < target) row++;\n    else col--;\n  }\n  return false;\n}","cpp":"bool searchMatrix(vector<vector<int>>& matrix, int target) {\n  if (matrix.empty() || matrix[0].empty()) return false;\n  int row = 0, col = matrix[0].size() - 1;\n  while (row < matrix.size() && col >= 0) {\n    if (matrix[row][col] == target) return true;\n    if (matrix[row][col] < target) row++;\n    else col--;\n  }\n  return false;\n}"},"challenge":"O(m+n) solution without binary search","hints":["Start from top-right","Eliminate row or column","Staircase search"],"relatedProblems":[48,49,51,52]},{"id":51,"title":"Find Peak Element - II","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Matrix","difficulty":"hard","status":"solved","timeSpent":12,"attempts":4,"lastTouched":15,"frequency":80,"tags":["binary-search","matrix","peak-element"],"companies":["Google","Amazon"],"concept":"Find peak element in 2D matrix (greater than 4 neighbors)","explanation":"Binary search on columns. Find max in mid column. If it's peak, return. Otherwise move to side with greater neighbor.","complexity":{"time":"O(m log n)","space":"O(1)"},"code":{"javascript":"function findPeakGrid(mat) {\n  let left = 0, right = mat[0].length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    let maxRow = 0;\n    for (let i = 0; i < mat.length; i++) {\n      if (mat[i][mid] > mat[maxRow][mid]) maxRow = i;\n    }\n    const leftIsGreater = mid > left && mat[maxRow][mid - 1] > mat[maxRow][mid];\n    const rightIsGreater = mid < right && mat[maxRow][mid + 1] > mat[maxRow][mid];\n    if (!leftIsGreater && !rightIsGreater) return [maxRow, mid];\n    if (leftIsGreater) right = mid - 1;\n    else left = mid + 1;\n  }\n  return [-1, -1];\n}","python":"def findPeakGrid(mat):\n    left, right = 0, len(mat[0]) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        max_row = 0\n        for i in range(len(mat)):\n            if mat[i][mid] > mat[max_row][mid]:\n                max_row = i\n        left_is_greater = mid > left and mat[max_row][mid - 1] > mat[max_row][mid]\n        right_is_greater = mid < right and mat[max_row][mid + 1] > mat[max_row][mid]\n        if not left_is_greater and not right_is_greater:\n            return [max_row, mid]\n        if left_is_greater:\n            right = mid - 1\n        else:\n            left = mid + 1\n    return [-1, -1]","java":"public int[] findPeakGrid(int[][] mat) {\n  int left = 0, right = mat[0].length - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    int maxRow = 0;\n    for (int i = 0; i < mat.length; i++) {\n      if (mat[i][mid] > mat[maxRow][mid]) maxRow = i;\n    }\n    boolean leftIsGreater = mid > left && mat[maxRow][mid - 1] > mat[maxRow][mid];\n    boolean rightIsGreater = mid < right && mat[maxRow][mid + 1] > mat[maxRow][mid];\n    if (!leftIsGreater && !rightIsGreater) return new int[]{maxRow, mid};\n    if (leftIsGreater) right = mid - 1;\n    else left = mid + 1;\n  }\n  return new int[]{-1, -1};\n}","cpp":"vector<int> findPeakGrid(vector<vector<int>>& mat) {\n  int left = 0, right = mat[0].size() - 1;\n  while (left <= right) {\n    int mid = left + (right - left) / 2;\n    int maxRow = 0;\n    for (int i = 0; i < mat.size(); i++) {\n      if (mat[i][mid] > mat[maxRow][mid]) maxRow = i;\n    }\n    bool leftIsGreater = mid > left && mat[maxRow][mid - 1] > mat[maxRow][mid];\n    bool rightIsGreater = mid < right && mat[maxRow][mid + 1] > mat[maxRow][mid];\n    if (!leftIsGreater && !rightIsGreater) return {maxRow, mid};\n    if (leftIsGreater) right = mid - 1;\n    else left = mid + 1;\n  }\n  return {-1, -1};\n}"},"challenge":"Guaranteed O(m log n) solution","hints":["Binary search on columns","Find max in column","Move to greater side"],"relatedProblems":[43,48,49,50,52]},{"id":52,"title":"Matrix Median","category":"binary-search","categoryName":"Binary Search","topic":"Binary Search Matrix","difficulty":"hard","status":"stuck","timeSpent":30,"attempts":3,"lastTouched":21,"frequency":85,"tags":["binary-search","matrix","median"],"companies":["Amazon","Microsoft","Google"],"concept":"Find median of row-wise sorted matrix","explanation":"Binary search on value range. Count numbers <= mid using binary search in each row. Find value where count equals (m*n+1)/2.","complexity":{"time":"O(m log n log(range))","space":"O(1)"},"code":{"javascript":"function matrixMedian(mat) {\n  let min = Infinity, max = -Infinity;\n  for (let row of mat) {\n    min = Math.min(min, row[0]);\n    max = Math.max(max, row[row.length - 1]);\n  }\n  const desired = Math.floor((mat.length * mat[0].length + 1) / 2);\n  while (min < max) {\n    const mid = Math.floor((min + max) / 2);\n    let count = 0;\n    for (let row of mat) {\n      count += countLessEqual(row, mid);\n    }\n    if (count < desired) min = mid + 1;\n    else max = mid;\n  }\n  return min;\n}\n\nfunction countLessEqual(row, target) {\n  let left = 0, right = row.length;\n  while (left < right) {\n    const mid = Math.floor((left + right) / 2);\n    if (row[mid] <= target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","python":"def matrixMedian(mat):\n    min_val = min(row[0] for row in mat)\n    max_val = max(row[-1] for row in mat)\n    desired = (len(mat) * len(mat[0]) + 1) // 2\n    while min_val < max_val:\n        mid = (min_val + max_val) // 2\n        count = sum(countLessEqual(row, mid) for row in mat)\n        if count < desired:\n            min_val = mid + 1\n        else:\n            max_val = mid\n    return min_val\n\ndef countLessEqual(row, target):\n    left, right = 0, len(row)\n    while left < right:\n        mid = (left + right) // 2\n        if row[mid] <= target:\n            left = mid + 1\n        else:\n            right = mid\n    return left","java":"public int matrixMedian(int[][] mat) {\n  int min = Integer.MAX_VALUE, max = Integer.MIN_VALUE;\n  for (int[] row : mat) {\n    min = Math.min(min, row[0]);\n    max = Math.max(max, row[row.length - 1]);\n  }\n  int desired = (mat.length * mat[0].length + 1) / 2;\n  while (min < max) {\n    int mid = min + (max - min) / 2;\n    int count = 0;\n    for (int[] row : mat) {\n      count += countLessEqual(row, mid);\n    }\n    if (count < desired) min = mid + 1;\n    else max = mid;\n  }\n  return min;\n}\n\nint countLessEqual(int[] row, int target) {\n  int left = 0, right = row.length;\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (row[mid] <= target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}","cpp":"int matrixMedian(vector<vector<int>>& mat) {\n  int min_val = INT_MAX, max_val = INT_MIN;\n  for (auto& row : mat) {\n    min_val = min(min_val, row[0]);\n    max_val = max(max_val, row.back());\n  }\n  int desired = (mat.size() * mat[0].size() + 1) / 2;\n  while (min_val < max_val) {\n    int mid = min_val + (max_val - min_val) / 2;\n    int count = 0;\n    for (auto& row : mat) {\n      count += countLessEqual(row, mid);\n    }\n    if (count < desired) min_val = mid + 1;\n    else max_val = mid;\n  }\n  return min_val;\n}\n\nint countLessEqual(vector<int>& row, int target) {\n  int left = 0, right = row.size();\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (row[mid] <= target) left = mid + 1;\n    else right = mid;\n  }\n  return left;\n}"},"challenge":"Efficient counting in each row","hints":["Binary search on value","Count <= mid in each row","Find median position"],"relatedProblems":[44,48,49,50,51]},{"id":53,"title":"Pow(x,n)","category":"recursion","categoryName":"Recursion","topic":"Recursion Math","difficulty":"medium","status":"attempted","timeSpent":34,"attempts":4,"lastTouched":20,"frequency":90,"tags":["recursion","math","power"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Calculate power using divide and conquer","explanation":"Use binary exponentiation. If n is even, x^n = (x^2)^(n/2). If n is odd, x^n = x * (x^2)^((n-1)/2). Handle negative n.","complexity":{"time":"O(log n)","space":"O(log n)"},"code":{"javascript":"function myPow(x, n) {\n  if (n === 0) return 1;\n  if (n < 0) {\n    x = 1 / x;\n    n = -n;\n  }\n  const half = myPow(x, Math.floor(n / 2));\n  return n % 2 === 0 ? half * half : half * half * x;\n}","python":"def myPow(x, n):\n    if n == 0:\n        return 1\n    if n < 0:\n        x = 1 / x\n        n = -n\n    half = myPow(x, n // 2)\n    return half * half if n % 2 == 0 else half * half * x","java":"public double myPow(double x, int n) {\n  if (n == 0) return 1;\n  if (n < 0) {\n    x = 1 / x;\n    n = -n;\n  }\n  double half = myPow(x, n / 2);\n  return (n % 2 == 0) ? half * half : half * half * x;\n}","cpp":"double myPow(double x, int n) {\n  if (n == 0) return 1;\n  if (n < 0) {\n    x = 1 / x;\n    n = -n;\n  }\n  double half = myPow(x, n / 2);\n  return (n % 2 == 0) ? half * half : half * half * x;\n}"},"challenge":"Handle negative and large n","hints":["Binary exponentiation","Divide and conquer","Handle negative power"],"relatedProblems":[]},{"id":54,"title":"Generate Parentheses","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"solved","timeSpent":29,"attempts":2,"lastTouched":28,"frequency":95,"tags":["recursion","backtracking","parentheses"],"companies":["Amazon","Microsoft","Facebook","Google","Apple"],"concept":"Generate all valid combinations of n pairs of parentheses","explanation":"Use backtracking. Add '(' if open count < n. Add ')' if close count < open count. When both equal n, we have valid string.","complexity":{"time":"O(4^n / sqrt(n))","space":"O(n)"},"code":{"javascript":"function generateParenthesis(n) {\n  const result = [];\n  backtrack(result, '', 0, 0, n);\n  return result;\n}\n\nfunction backtrack(result, curr, open, close, max) {\n  if (curr.length === max * 2) {\n    result.push(curr);\n    return;\n  }\n  if (open < max) backtrack(result, curr + '(', open + 1, close, max);\n  if (close < open) backtrack(result, curr + ')', open, close + 1, max);\n}","python":"def generateParenthesis(n):\n    result = []\n    def backtrack(curr, open_count, close_count):\n        if len(curr) == 2 * n:\n            result.append(curr)\n            return\n        if open_count < n:\n            backtrack(curr + '(', open_count + 1, close_count)\n        if close_count < open_count:\n            backtrack(curr + ')', open_count, close_count + 1)\n    backtrack('', 0, 0)\n    return result","java":"public List<String> generateParenthesis(int n) {\n  List<String> result = new ArrayList<>();\n  backtrack(result, new StringBuilder(), 0, 0, n);\n  return result;\n}\n\nvoid backtrack(List<String> result, StringBuilder curr, int open, int close, int max) {\n  if (curr.length() == max * 2) {\n    result.add(curr.toString());\n    return;\n  }\n  if (open < max) {\n    curr.append('(');\n    backtrack(result, curr, open + 1, close, max);\n    curr.deleteCharAt(curr.length() - 1);\n  }\n  if (close < open) {\n    curr.append(')');\n    backtrack(result, curr, open, close + 1, max);\n    curr.deleteCharAt(curr.length() - 1);\n  }\n}","cpp":"vector<string> generateParenthesis(int n) {\n  vector<string> result;\n  backtrack(result, \"\", 0, 0, n);\n  return result;\n}\n\nvoid backtrack(vector<string>& result, string curr, int open, int close, int max) {\n  if (curr.length() == max * 2) {\n    result.push_back(curr);\n    return;\n  }\n  if (open < max) backtrack(result, curr + '(', open + 1, close, max);\n  if (close < open) backtrack(result, curr + ')', open, close + 1, max);\n}"},"challenge":"Generate only valid combinations","hints":["Track open and close counts","Add '(' if open < n","Add ')' if close < open"],"relatedProblems":[63]},{"id":55,"title":"Power Set","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"stuck","timeSpent":16,"attempts":3,"lastTouched":3,"frequency":90,"tags":["recursion","backtracking","subsets","power-set"],"companies":["Amazon","Microsoft","Facebook"],"concept":"Generate all subsets of a set","explanation":"For each element, either include it or exclude it. Recursively generate all subsets by making both choices for each element.","complexity":{"time":"O(2^n)","space":"O(n)"},"code":{"javascript":"function subsets(nums) {\n  const result = [];\n  backtrack(result, [], nums, 0);\n  return result;\n}\n\nfunction backtrack(result, curr, nums, start) {\n  result.push([...curr]);\n  for (let i = start; i < nums.length; i++) {\n    curr.push(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.pop();\n  }\n}","python":"def subsets(nums):\n    result = []\n    def backtrack(curr, start):\n        result.append(curr[:])\n        for i in range(start, len(nums)):\n            curr.append(nums[i])\n            backtrack(curr, i + 1)\n            curr.pop()\n    backtrack([], 0)\n    return result","java":"public List<List<Integer>> subsets(int[] nums) {\n  List<List<Integer>> result = new ArrayList<>();\n  backtrack(result, new ArrayList<>(), nums, 0);\n  return result;\n}\n\nvoid backtrack(List<List<Integer>> result, List<Integer> curr, int[] nums, int start) {\n  result.add(new ArrayList<>(curr));\n  for (int i = start; i < nums.length; i++) {\n    curr.add(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.remove(curr.size() - 1);\n  }\n}","cpp":"vector<vector<int>> subsets(vector<int>& nums) {\n  vector<vector<int>> result;\n  vector<int> curr;\n  backtrack(result, curr, nums, 0);\n  return result;\n}\n\nvoid backtrack(vector<vector<int>>& result, vector<int>& curr, vector<int>& nums, int start) {\n  result.push_back(curr);\n  for (int i = start; i < nums.size(); i++) {\n    curr.push_back(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.pop_back();\n  }\n}"},"challenge":"Handle duplicates in variation","hints":["Include or exclude each element","Recursion tree","Backtracking"],"relatedProblems":[60,61]},{"id":56,"title":"Check if there exists a subsequence with sum K","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"easy","status":"solved","timeSpent":39,"attempts":2,"lastTouched":29,"frequency":75,"tags":["recursion","backtracking","subsequence-sum"],"companies":["Amazon","Microsoft"],"concept":"Check if any subsequence sums to K","explanation":"For each element, either include it in sum or exclude it. Recursively check if target sum can be achieved.","complexity":{"time":"O(2^n)","space":"O(n)"},"code":{"javascript":"function checkSubsequenceSum(nums, k) {\n  return backtrack(nums, k, 0, 0);\n}\n\nfunction backtrack(nums, k, index, sum) {\n  if (index === nums.length) return sum === k;\n  if (sum === k) return true;\n  return backtrack(nums, k, index + 1, sum + nums[index]) ||\n         backtrack(nums, k, index + 1, sum);\n}","python":"def checkSubsequenceSum(nums, k):\n    def backtrack(index, total):\n        if index == len(nums):\n            return total == k\n        if total == k:\n            return True\n        return backtrack(index + 1, total + nums[index]) or backtrack(index + 1, total)\n    return backtrack(0, 0)","java":"public boolean checkSubsequenceSum(int[] nums, int k) {\n  return backtrack(nums, k, 0, 0);\n}\n\nboolean backtrack(int[] nums, int k, int index, int sum) {\n  if (index == nums.length) return sum == k;\n  if (sum == k) return true;\n  return backtrack(nums, k, index + 1, sum + nums[index]) ||\n         backtrack(nums, k, index + 1, sum);\n}","cpp":"bool checkSubsequenceSum(vector<int>& nums, int k) {\n  return backtrack(nums, k, 0, 0);\n}\n\nbool backtrack(vector<int>& nums, int k, int index, int sum) {\n  if (index == nums.size()) return sum == k;\n  if (sum == k) return true;\n  return backtrack(nums, k, index + 1, sum + nums[index]) ||\n         backtrack(nums, k, index + 1, sum);\n}"},"challenge":"Optimize with memoization","hints":["Include or exclude","Base cases","Early termination"],"relatedProblems":[57,58,59]},{"id":57,"title":"Count all subsequences with sum K","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":70,"tags":["recursion","backtracking","count-subsequences"],"companies":["Amazon","Microsoft"],"concept":"Count all subsequences that sum to K","explanation":"Similar to checking existence, but count all valid subsequences. For each element, recursively count including and excluding it.","complexity":{"time":"O(2^n)","space":"O(n)"},"code":{"javascript":"function countSubsequences(nums, k) {\n  return backtrack(nums, k, 0, 0);\n}\n\nfunction backtrack(nums, k, index, sum) {\n  if (index === nums.length) return sum === k ? 1 : 0;\n  return backtrack(nums, k, index + 1, sum + nums[index]) +\n         backtrack(nums, k, index + 1, sum);\n}","python":"def countSubsequences(nums, k):\n    def backtrack(index, total):\n        if index == len(nums):\n            return 1 if total == k else 0\n        return backtrack(index + 1, total + nums[index]) + backtrack(index + 1, total)\n    return backtrack(0, 0)","java":"public int countSubsequences(int[] nums, int k) {\n  return backtrack(nums, k, 0, 0);\n}\n\nint backtrack(int[] nums, int k, int index, int sum) {\n  if (index == nums.length) return sum == k ? 1 : 0;\n  return backtrack(nums, k, index + 1, sum + nums[index]) +\n         backtrack(nums, k, index + 1, sum);\n}","cpp":"int countSubsequences(vector<int>& nums, int k) {\n  return backtrack(nums, k, 0, 0);\n}\n\nint backtrack(vector<int>& nums, int k, int index, int sum) {\n  if (index == nums.size()) return sum == k ? 1 : 0;\n  return backtrack(nums, k, index + 1, sum + nums[index]) +\n         backtrack(nums, k, index + 1, sum);\n}"},"challenge":"Handle large counts with modulo","hints":["Count include + exclude","Base case when index == n","Sum equals k"],"relatedProblems":[56,58,59]},{"id":58,"title":"Combination Sum","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"attempted","timeSpent":69,"attempts":2,"lastTouched":4,"frequency":95,"tags":["recursion","backtracking","combination-sum"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Find all unique combinations that sum to target (can reuse elements)","explanation":"Backtracking with sorting. For each element, try all possible counts (0 to max). Recursively find combinations for remaining sum.","complexity":{"time":"O(2^t) where t is target","space":"O(t)"},"code":{"javascript":"function combinationSum(candidates, target) {\n  const result = [];\n  candidates.sort((a, b) => a - b);\n  backtrack(result, [], candidates, target, 0);\n  return result;\n}\n\nfunction backtrack(result, curr, candidates, remain, start) {\n  if (remain === 0) {\n    result.push([...curr]);\n    return;\n  }\n  if (remain < 0) return;\n  for (let i = start; i < candidates.length; i++) {\n    curr.push(candidates[i]);\n    backtrack(result, curr, candidates, remain - candidates[i], i);\n    curr.pop();\n  }\n}","python":"def combinationSum(candidates, target):\n    result = []\n    candidates.sort()\n    def backtrack(curr, remain, start):\n        if remain == 0:\n            result.append(curr[:])\n            return\n        if remain < 0:\n            return\n        for i in range(start, len(candidates)):\n            curr.append(candidates[i])\n            backtrack(curr, remain - candidates[i], i)\n            curr.pop()\n    backtrack([], target, 0)\n    return result","java":"public List<List<Integer>> combinationSum(int[] candidates, int target) {\n  List<List<Integer>> result = new ArrayList<>();\n  Arrays.sort(candidates);\n  backtrack(result, new ArrayList<>(), candidates, target, 0);\n  return result;\n}\n\nvoid backtrack(List<List<Integer>> result, List<Integer> curr, int[] candidates, int remain, int start) {\n  if (remain == 0) {\n    result.add(new ArrayList<>(curr));\n    return;\n  }\n  if (remain < 0) return;\n  for (int i = start; i < candidates.length; i++) {\n    curr.add(candidates[i]);\n    backtrack(result, curr, candidates, remain - candidates[i], i);\n    curr.remove(curr.size() - 1);\n  }\n}","cpp":"vector<vector<int>> combinationSum(vector<int>& candidates, int target) {\n  vector<vector<int>> result;\n  sort(candidates.begin(), candidates.end());\n  vector<int> curr;\n  backtrack(result, curr, candidates, target, 0);\n  return result;\n}\n\nvoid backtrack(vector<vector<int>>& result, vector<int>& curr, vector<int>& candidates, int remain, int start) {\n  if (remain == 0) {\n    result.push_back(curr);\n    return;\n  }\n  if (remain < 0) return;\n  for (int i = start; i < candidates.size(); i++) {\n    curr.push_back(candidates[i]);\n    backtrack(result, curr, candidates, remain - candidates[i], i);\n    curr.pop_back();\n  }\n}"},"challenge":"Avoid duplicates with sorting","hints":["Sort first","Reuse same element","Backtrack when remain < 0"],"relatedProblems":[59,62]},{"id":59,"title":"Combination Sum II","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"stuck","timeSpent":21,"attempts":3,"lastTouched":7,"frequency":85,"tags":["recursion","backtracking","combination-sum"],"companies":["Amazon","Microsoft","Facebook"],"concept":"Find all unique combinations that sum to target (each element once, no duplicates)","explanation":"Sort and skip duplicates. Each element can be used only once. Skip same elements at same position to avoid duplicate combinations.","complexity":{"time":"O(2^n)","space":"O(n)"},"code":{"javascript":"function combinationSum2(candidates, target) {\n  const result = [];\n  candidates.sort((a, b) => a - b);\n  backtrack(result, [], candidates, target, 0);\n  return result;\n}\n\nfunction backtrack(result, curr, candidates, remain, start) {\n  if (remain === 0) {\n    result.push([...curr]);\n    return;\n  }\n  if (remain < 0) return;\n  for (let i = start; i < candidates.length; i++) {\n    if (i > start && candidates[i] === candidates[i - 1]) continue;\n    curr.push(candidates[i]);\n    backtrack(result, curr, candidates, remain - candidates[i], i + 1);\n    curr.pop();\n  }\n}","python":"def combinationSum2(candidates, target):\n    result = []\n    candidates.sort()\n    def backtrack(curr, remain, start):\n        if remain == 0:\n            result.append(curr[:])\n            return\n        if remain < 0:\n            return\n        for i in range(start, len(candidates)):\n            if i > start and candidates[i] == candidates[i - 1]:\n                continue\n            curr.append(candidates[i])\n            backtrack(curr, remain - candidates[i], i + 1)\n            curr.pop()\n    backtrack([], target, 0)\n    return result","java":"public List<List<Integer>> combinationSum2(int[] candidates, int target) {\n  List<List<Integer>> result = new ArrayList<>();\n  Arrays.sort(candidates);\n  backtrack(result, new ArrayList<>(), candidates, target, 0);\n  return result;\n}\n\nvoid backtrack(List<List<Integer>> result, List<Integer> curr, int[] candidates, int remain, int start) {\n  if (remain == 0) {\n    result.add(new ArrayList<>(curr));\n    return;\n  }\n  if (remain < 0) return;\n  for (int i = start; i < candidates.length; i++) {\n    if (i > start && candidates[i] == candidates[i - 1]) continue;\n    curr.add(candidates[i]);\n    backtrack(result, curr, candidates, remain - candidates[i], i + 1);\n    curr.remove(curr.size() - 1);\n  }\n}","cpp":"vector<vector<int>> combinationSum2(vector<int>& candidates, int target) {\n  vector<vector<int>> result;\n  sort(candidates.begin(), candidates.end());\n  vector<int> curr;\n  backtrack(result, curr, candidates, target, 0);\n  return result;\n}\n\nvoid backtrack(vector<vector<int>>& result, vector<int>& curr, vector<int>& candidates, int remain, int start) {\n  if (remain == 0) {\n    result.push_back(curr);\n    return;\n  }\n  if (remain < 0) return;\n  for (int i = start; i < candidates.size(); i++) {\n    if (i > start && candidates[i] == candidates[i - 1]) continue;\n    curr.push_back(candidates[i]);\n    backtrack(result, curr, candidates, remain - candidates[i], i + 1);\n    curr.pop_back();\n  }\n}"},"challenge":"Avoid duplicate combinations","hints":["Sort first","Skip duplicates at same level","Use each element once"],"relatedProblems":[58,62]},{"id":60,"title":"Subsets I","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":90,"tags":["recursion","backtracking","subsets"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Generate all subsets (no duplicates in input)","explanation":"For each element, generate subsets including it and excluding it. Classic backtracking problem.","complexity":{"time":"O(2^n)","space":"O(n)"},"code":{"javascript":"function subsets(nums) {\n  const result = [];\n  backtrack(result, [], nums, 0);\n  return result;\n}\n\nfunction backtrack(result, curr, nums, start) {\n  result.push([...curr]);\n  for (let i = start; i < nums.length; i++) {\n    curr.push(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.pop();\n  }\n}","python":"def subsets(nums):\n    result = []\n    def backtrack(curr, start):\n        result.append(curr[:])\n        for i in range(start, len(nums)):\n            curr.append(nums[i])\n            backtrack(curr, i + 1)\n            curr.pop()\n    backtrack([], 0)\n    return result","java":"public List<List<Integer>> subsets(int[] nums) {\n  List<List<Integer>> result = new ArrayList<>();\n  backtrack(result, new ArrayList<>(), nums, 0);\n  return result;\n}\n\nvoid backtrack(List<List<Integer>> result, List<Integer> curr, int[] nums, int start) {\n  result.add(new ArrayList<>(curr));\n  for (int i = start; i < nums.length; i++) {\n    curr.add(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.remove(curr.size() - 1);\n  }\n}","cpp":"vector<vector<int>> subsets(vector<int>& nums) {\n  vector<vector<int>> result;\n  vector<int> curr;\n  backtrack(result, curr, nums, 0);\n  return result;\n}\n\nvoid backtrack(vector<vector<int>>& result, vector<int>& curr, vector<int>& nums, int start) {\n  result.push_back(curr);\n  for (int i = start; i < nums.size(); i++) {\n    curr.push_back(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.pop_back();\n  }\n}"},"challenge":"Iterative solution using bit manipulation","hints":["Include or exclude","Backtracking","Start from current index"],"relatedProblems":[55,61]},{"id":61,"title":"Subsets II","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"revisit","timeSpent":31,"attempts":4,"lastTouched":3,"frequency":85,"tags":["recursion","backtracking","subsets","duplicates"],"companies":["Amazon","Microsoft","Facebook"],"concept":"Generate all subsets with duplicates in input","explanation":"Sort first to group duplicates. Skip duplicates at same recursion level to avoid duplicate subsets.","complexity":{"time":"O(2^n)","space":"O(n)"},"code":{"javascript":"function subsetsWithDup(nums) {\n  const result = [];\n  nums.sort((a, b) => a - b);\n  backtrack(result, [], nums, 0);\n  return result;\n}\n\nfunction backtrack(result, curr, nums, start) {\n  result.push([...curr]);\n  for (let i = start; i < nums.length; i++) {\n    if (i > start && nums[i] === nums[i - 1]) continue;\n    curr.push(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.pop();\n  }\n}","python":"def subsetsWithDup(nums):\n    result = []\n    nums.sort()\n    def backtrack(curr, start):\n        result.append(curr[:])\n        for i in range(start, len(nums)):\n            if i > start and nums[i] == nums[i - 1]:\n                continue\n            curr.append(nums[i])\n            backtrack(curr, i + 1)\n            curr.pop()\n    backtrack([], 0)\n    return result","java":"public List<List<Integer>> subsetsWithDup(int[] nums) {\n  List<List<Integer>> result = new ArrayList<>();\n  Arrays.sort(nums);\n  backtrack(result, new ArrayList<>(), nums, 0);\n  return result;\n}\n\nvoid backtrack(List<List<Integer>> result, List<Integer> curr, int[] nums, int start) {\n  result.add(new ArrayList<>(curr));\n  for (int i = start; i < nums.length; i++) {\n    if (i > start && nums[i] == nums[i - 1]) continue;\n    curr.add(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.remove(curr.size() - 1);\n  }\n}","cpp":"vector<vector<int>> subsetsWithDup(vector<int>& nums) {\n  vector<vector<int>> result;\n  sort(nums.begin(), nums.end());\n  vector<int> curr;\n  backtrack(result, curr, nums, 0);\n  return result;\n}\n\nvoid backtrack(vector<vector<int>>& result, vector<int>& curr, vector<int>& nums, int start) {\n  result.push_back(curr);\n  for (int i = start; i < nums.size(); i++) {\n    if (i > start && nums[i] == nums[i - 1]) continue;\n    curr.push_back(nums[i]);\n    backtrack(result, curr, nums, i + 1);\n    curr.pop_back();\n  }\n}"},"challenge":"Avoid duplicate subsets","hints":["Sort first","Skip duplicates at same level","i > start check"],"relatedProblems":[55,60]},{"id":62,"title":"Combination Sum III","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"solved","timeSpent":32,"attempts":4,"lastTouched":15,"frequency":80,"tags":["recursion","backtracking","combination-sum"],"companies":["Amazon","Microsoft"],"concept":"Find all valid combinations of k numbers that sum to n","explanation":"Find combinations of k numbers from 1-9 that sum to n. Use backtracking with constraints on count and sum.","complexity":{"time":"O(9!/(9-k)!)","space":"O(k)"},"code":{"javascript":"function combinationSum3(k, n) {\n  const result = [];\n  backtrack(result, [], k, n, 1);\n  return result;\n}\n\nfunction backtrack(result, curr, k, remain, start) {\n  if (curr.length === k && remain === 0) {\n    result.push([...curr]);\n    return;\n  }\n  if (curr.length >= k || remain <= 0) return;\n  for (let i = start; i <= 9; i++) {\n    curr.push(i);\n    backtrack(result, curr, k, remain - i, i + 1);\n    curr.pop();\n  }\n}","python":"def combinationSum3(k, n):\n    result = []\n    def backtrack(curr, remain, start):\n        if len(curr) == k and remain == 0:\n            result.append(curr[:])\n            return\n        if len(curr) >= k or remain <= 0:\n            return\n        for i in range(start, 10):\n            curr.append(i)\n            backtrack(curr, remain - i, i + 1)\n            curr.pop()\n    backtrack([], n, 1)\n    return result","java":"public List<List<Integer>> combinationSum3(int k, int n) {\n  List<List<Integer>> result = new ArrayList<>();\n  backtrack(result, new ArrayList<>(), k, n, 1);\n  return result;\n}\n\nvoid backtrack(List<List<Integer>> result, List<Integer> curr, int k, int remain, int start) {\n  if (curr.size() == k && remain == 0) {\n    result.add(new ArrayList<>(curr));\n    return;\n  }\n  if (curr.size() >= k || remain <= 0) return;\n  for (int i = start; i <= 9; i++) {\n    curr.add(i);\n    backtrack(result, curr, k, remain - i, i + 1);\n    curr.remove(curr.size() - 1);\n  }\n}","cpp":"vector<vector<int>> combinationSum3(int k, int n) {\n  vector<vector<int>> result;\n  vector<int> curr;\n  backtrack(result, curr, k, n, 1);\n  return result;\n}\n\nvoid backtrack(vector<vector<int>>& result, vector<int>& curr, int k, int remain, int start) {\n  if (curr.size() == k && remain == 0) {\n    result.push_back(curr);\n    return;\n  }\n  if (curr.size() >= k || remain <= 0) return;\n  for (int i = start; i <= 9; i++) {\n    curr.push_back(i);\n    backtrack(result, curr, k, remain - i, i + 1);\n    curr.pop_back();\n  }\n}"},"challenge":"Exactly k numbers constraint","hints":["Numbers 1-9 only","Exactly k numbers","Sum to n"],"relatedProblems":[58,59]},{"id":63,"title":"Letter Combinations of a Phone Number","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"solved","timeSpent":17,"attempts":2,"lastTouched":6,"frequency":95,"tags":["recursion","backtracking","phone-number"],"companies":["Amazon","Microsoft","Facebook","Google","Apple"],"concept":"Generate all possible letter combinations from phone digits","explanation":"Map digits to letters. For each digit, try all possible letters and recursively build combinations.","complexity":{"time":"O(4^n) where n is digits","space":"O(n)"},"code":{"javascript":"function letterCombinations(digits) {\n  if (!digits) return [];\n  const map = {\n    '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl',\n    '6': 'mno', '7': 'pqrs', '8': 'tuv', '9': 'wxyz'\n  };\n  const result = [];\n  backtrack(result, '', digits, 0, map);\n  return result;\n}\n\nfunction backtrack(result, curr, digits, index, map) {\n  if (index === digits.length) {\n    result.push(curr);\n    return;\n  }\n  const letters = map[digits[index]];\n  for (let letter of letters) {\n    backtrack(result, curr + letter, digits, index + 1, map);\n  }\n}","python":"def letterCombinations(digits):\n    if not digits:\n        return []\n    phone = {\n        '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl',\n        '6': 'mno', '7': 'pqrs', '8': 'tuv', '9': 'wxyz'\n    }\n    result = []\n    def backtrack(curr, index):\n        if index == len(digits):\n            result.append(curr)\n            return\n        for letter in phone[digits[index]]:\n            backtrack(curr + letter, index + 1)\n    backtrack('', 0)\n    return result","java":"public List<String> letterCombinations(String digits) {\n  List<String> result = new ArrayList<>();\n  if (digits.isEmpty()) return result;\n  String[] map = {\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"};\n  backtrack(result, new StringBuilder(), digits, 0, map);\n  return result;\n}\n\nvoid backtrack(List<String> result, StringBuilder curr, String digits, int index, String[] map) {\n  if (index == digits.length()) {\n    result.add(curr.toString());\n    return;\n  }\n  String letters = map[digits.charAt(index) - '0'];\n  for (char letter : letters.toCharArray()) {\n    curr.append(letter);\n    backtrack(result, curr, digits, index + 1, map);\n    curr.deleteCharAt(curr.length() - 1);\n  }\n}","cpp":"vector<string> letterCombinations(string digits) {\n  vector<string> result;\n  if (digits.empty()) return result;\n  vector<string> map = {\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"};\n  string curr;\n  backtrack(result, curr, digits, 0, map);\n  return result;\n}\n\nvoid backtrack(vector<string>& result, string& curr, string& digits, int index, vector<string>& map) {\n  if (index == digits.size()) {\n    result.push_back(curr);\n    return;\n  }\n  string letters = map[digits[index] - '0'];\n  for (char letter : letters) {\n    curr.push_back(letter);\n    backtrack(result, curr, digits, index + 1, map);\n    curr.pop_back();\n  }\n}"},"challenge":"Handle empty input","hints":["Map digits to letters","Try all combinations","Build recursively"],"relatedProblems":[54]},{"id":64,"title":"Palindrome partitioning","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"solved","timeSpent":38,"attempts":2,"lastTouched":29,"frequency":85,"tags":["recursion","backtracking","palindrome","partition"],"companies":["Amazon","Microsoft","Facebook"],"concept":"Partition string such that every substring is palindrome","explanation":"Try all possible partitions. For each position, check if substring is palindrome. If yes, recursively partition remaining string.","complexity":{"time":"O(n * 2^n)","space":"O(n)"},"code":{"javascript":"function partition(s) {\n  const result = [];\n  backtrack(result, [], s, 0);\n  return result;\n}\n\nfunction backtrack(result, curr, s, start) {\n  if (start === s.length) {\n    result.push([...curr]);\n    return;\n  }\n  for (let i = start; i < s.length; i++) {\n    if (isPalindrome(s, start, i)) {\n      curr.push(s.substring(start, i + 1));\n      backtrack(result, curr, s, i + 1);\n      curr.pop();\n    }\n  }\n}\n\nfunction isPalindrome(s, left, right) {\n  while (left < right) {\n    if (s[left++] !== s[right--]) return false;\n  }\n  return true;\n}","python":"def partition(s):\n    result = []\n    def backtrack(curr, start):\n        if start == len(s):\n            result.append(curr[:])\n            return\n        for i in range(start, len(s)):\n            if isPalindrome(s, start, i):\n                curr.append(s[start:i+1])\n                backtrack(curr, i + 1)\n                curr.pop()\n    backtrack([], 0)\n    return result\n\ndef isPalindrome(s, left, right):\n    while left < right:\n        if s[left] != s[right]:\n            return False\n        left += 1\n        right -= 1\n    return True","java":"public List<List<String>> partition(String s) {\n  List<List<String>> result = new ArrayList<>();\n  backtrack(result, new ArrayList<>(), s, 0);\n  return result;\n}\n\nvoid backtrack(List<List<String>> result, List<String> curr, String s, int start) {\n  if (start == s.length()) {\n    result.add(new ArrayList<>(curr));\n    return;\n  }\n  for (int i = start; i < s.length(); i++) {\n    if (isPalindrome(s, start, i)) {\n      curr.add(s.substring(start, i + 1));\n      backtrack(result, curr, s, i + 1);\n      curr.remove(curr.size() - 1);\n    }\n  }\n}\n\nboolean isPalindrome(String s, int left, int right) {\n  while (left < right) {\n    if (s.charAt(left++) != s.charAt(right--)) return false;\n  }\n  return true;\n}","cpp":"vector<vector<string>> partition(string s) {\n  vector<vector<string>> result;\n  vector<string> curr;\n  backtrack(result, curr, s, 0);\n  return result;\n}\n\nvoid backtrack(vector<vector<string>>& result, vector<string>& curr, string& s, int start) {\n  if (start == s.size()) {\n    result.push_back(curr);\n    return;\n  }\n  for (int i = start; i < s.size(); i++) {\n    if (isPalindrome(s, start, i)) {\n      curr.push_back(s.substr(start, i - start + 1));\n      backtrack(result, curr, s, i + 1);\n      curr.pop_back();\n    }\n  }\n}\n\nbool isPalindrome(string& s, int left, int right) {\n  while (left < right) {\n    if (s[left++] != s[right--]) return false;\n  }\n  return true;\n}"},"challenge":"Optimize palindrome checking with DP","hints":["Check palindrome before adding","Try all partitions","Backtrack"],"relatedProblems":[]},{"id":65,"title":"Word Search","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"solved","timeSpent":51,"attempts":2,"lastTouched":15,"frequency":95,"tags":["recursion","backtracking","word-search","dfs"],"companies":["Amazon","Microsoft","Facebook","Google","Apple"],"concept":"Find if word exists in grid (adjacent cells)","explanation":"DFS from each cell. Mark visited cells. Check all 4 directions for next character. Backtrack if path doesn't work.","complexity":{"time":"O(m * n * 4^L) where L is word length","space":"O(L)"},"code":{"javascript":"function exist(board, word) {\n  for (let i = 0; i < board.length; i++) {\n    for (let j = 0; j < board[0].length; j++) {\n      if (dfs(board, word, i, j, 0)) return true;\n    }\n  }\n  return false;\n}\n\nfunction dfs(board, word, i, j, index) {\n  if (index === word.length) return true;\n  if (i < 0 || i >= board.length || j < 0 || j >= board[0].length) return false;\n  if (board[i][j] !== word[index]) return false;\n  const temp = board[i][j];\n  board[i][j] = '#';\n  const found = dfs(board, word, i + 1, j, index + 1) ||\n                dfs(board, word, i - 1, j, index + 1) ||\n                dfs(board, word, i, j + 1, index + 1) ||\n                dfs(board, word, i, j - 1, index + 1);\n  board[i][j] = temp;\n  return found;\n}","python":"def exist(board, word):\n    for i in range(len(board)):\n        for j in range(len(board[0])):\n            if dfs(board, word, i, j, 0):\n                return True\n    return False\n\ndef dfs(board, word, i, j, index):\n    if index == len(word):\n        return True\n    if i < 0 or i >= len(board) or j < 0 or j >= len(board[0]):\n        return False\n    if board[i][j] != word[index]:\n        return False\n    temp = board[i][j]\n    board[i][j] = '#'\n    found = dfs(board, word, i + 1, j, index + 1) or \\\n            dfs(board, word, i - 1, j, index + 1) or \\\n            dfs(board, word, i, j + 1, index + 1) or \\\n            dfs(board, word, i, j - 1, index + 1)\n    board[i][j] = temp\n    return found","java":"public boolean exist(char[][] board, String word) {\n  for (int i = 0; i < board.length; i++) {\n    for (int j = 0; j < board[0].length; j++) {\n      if (dfs(board, word, i, j, 0)) return true;\n    }\n  }\n  return false;\n}\n\nboolean dfs(char[][] board, String word, int i, int j, int index) {\n  if (index == word.length()) return true;\n  if (i < 0 || i >= board.length || j < 0 || j >= board[0].length) return false;\n  if (board[i][j] != word.charAt(index)) return false;\n  char temp = board[i][j];\n  board[i][j] = '#';\n  boolean found = dfs(board, word, i + 1, j, index + 1) ||\n                  dfs(board, word, i - 1, j, index + 1) ||\n                  dfs(board, word, i, j + 1, index + 1) ||\n                  dfs(board, word, i, j - 1, index + 1);\n  board[i][j] = temp;\n  return found;\n}","cpp":"bool exist(vector<vector<char>>& board, string word) {\n  for (int i = 0; i < board.size(); i++) {\n    for (int j = 0; j < board[0].size(); j++) {\n      if (dfs(board, word, i, j, 0)) return true;\n    }\n  }\n  return false;\n}\n\nbool dfs(vector<vector<char>>& board, string& word, int i, int j, int index) {\n  if (index == word.size()) return true;\n  if (i < 0 || i >= board.size() || j < 0 || j >= board[0].size()) return false;\n  if (board[i][j] != word[index]) return false;\n  char temp = board[i][j];\n  board[i][j] = '#';\n  bool found = dfs(board, word, i + 1, j, index + 1) ||\n               dfs(board, word, i - 1, j, index + 1) ||\n               dfs(board, word, i, j + 1, index + 1) ||\n               dfs(board, word, i, j - 1, index + 1);\n  board[i][j] = temp;\n  return found;\n}"},"challenge":"Optimize with pruning","hints":["DFS from each cell","Mark visited","Check all 4 directions"],"relatedProblems":[]},{"id":66,"title":"N Queen","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"hard","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":100,"tags":["recursion","backtracking","n-queen"],"companies":["Amazon","Microsoft","Facebook","Google","Apple","Adobe"],"concept":"Place N queens on NxN board so no two attack each other","explanation":"Place queens row by row. Check if position is safe (no queen in same column or diagonal). Backtrack if stuck.","complexity":{"time":"O(N!)","space":"O(N)"},"code":{"javascript":"function solveNQueens(n) {\n  const result = [];\n  const board = Array(n).fill().map(() => Array(n).fill('.'));\n  backtrack(result, board, 0, n);\n  return result;\n}\n\nfunction backtrack(result, board, row, n) {\n  if (row === n) {\n    result.push(board.map(r => r.join('')));\n    return;\n  }\n  for (let col = 0; col < n; col++) {\n    if (isValid(board, row, col, n)) {\n      board[row][col] = 'Q';\n      backtrack(result, board, row + 1, n);\n      board[row][col] = '.';\n    }\n  }\n}\n\nfunction isValid(board, row, col, n) {\n  for (let i = 0; i < row; i++) {\n    if (board[i][col] === 'Q') return false;\n  }\n  for (let i = row - 1, j = col - 1; i >= 0 && j >= 0; i--, j--) {\n    if (board[i][j] === 'Q') return false;\n  }\n  for (let i = row - 1, j = col + 1; i >= 0 && j < n; i--, j++) {\n    if (board[i][j] === 'Q') return false;\n  }\n  return true;\n}","python":"def solveNQueens(n):\n    result = []\n    board = [['.' for _ in range(n)] for _ in range(n)]\n    def backtrack(row):\n        if row == n:\n            result.append([''.join(r) for r in board])\n            return\n        for col in range(n):\n            if isValid(row, col):\n                board[row][col] = 'Q'\n                backtrack(row + 1)\n                board[row][col] = '.'\n    def isValid(row, col):\n        for i in range(row):\n            if board[i][col] == 'Q':\n                return False\n        i, j = row - 1, col - 1\n        while i >= 0 and j >= 0:\n            if board[i][j] == 'Q':\n                return False\n            i -= 1\n            j -= 1\n        i, j = row - 1, col + 1\n        while i >= 0 and j < n:\n            if board[i][j] == 'Q':\n                return False\n            i -= 1\n            j += 1\n        return True\n    backtrack(0)\n    return result","java":"public List<List<String>> solveNQueens(int n) {\n  List<List<String>> result = new ArrayList<>();\n  char[][] board = new char[n][n];\n  for (char[] row : board) Arrays.fill(row, '.');\n  backtrack(result, board, 0, n);\n  return result;\n}\n\nvoid backtrack(List<List<String>> result, char[][] board, int row, int n) {\n  if (row == n) {\n    List<String> solution = new ArrayList<>();\n    for (char[] r : board) solution.add(new String(r));\n    result.add(solution);\n    return;\n  }\n  for (int col = 0; col < n; col++) {\n    if (isValid(board, row, col, n)) {\n      board[row][col] = 'Q';\n      backtrack(result, board, row + 1, n);\n      board[row][col] = '.';\n    }\n  }\n}\n\nboolean isValid(char[][] board, int row, int col, int n) {\n  for (int i = 0; i < row; i++) {\n    if (board[i][col] == 'Q') return false;\n  }\n  for (int i = row - 1, j = col - 1; i >= 0 && j >= 0; i--, j--) {\n    if (board[i][j] == 'Q') return false;\n  }\n  for (int i = row - 1, j = col + 1; i >= 0 && j < n; i--, j++) {\n    if (board[i][j] == 'Q') return false;\n  }\n  return true;\n}","cpp":"vector<vector<string>> solveNQueens(int n) {\n  vector<vector<string>> result;\n  vector<string> board(n, string(n, '.'));\n  backtrack(result, board, 0, n);\n  return result;\n}\n\nvoid backtrack(vector<vector<string>>& result, vector<string>& board, int row, int n) {\n  if (row == n) {\n    result.push_back(board);\n    return;\n  }\n  for (int col = 0; col < n; col++) {\n    if (isValid(board, row, col, n)) {\n      board[row][col] = 'Q';\n      backtrack(result, board, row + 1, n);\n      board[row][col] = '.';\n    }\n  }\n}\n\nbool isValid(vector<string>& board, int row, int col, int n) {\n  for (int i = 0; i < row; i++) {\n    if (board[i][col] == 'Q') return false;\n  }\n  for (int i = row - 1, j = col - 1; i >= 0 && j >= 0; i--, j--) {\n    if (board[i][j] == 'Q') return false;\n  }\n  for (int i = row - 1, j = col + 1; i >= 0 && j < n; i--, j++) {\n    if (board[i][j] == 'Q') return false;\n  }\n  return true;\n}"},"challenge":"Optimize with bit manipulation or sets","hints":["Check column and diagonals","Place one queen per row","Backtrack if invalid"],"relatedProblems":[67]},{"id":67,"title":"Rat in a Maze","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"solved","timeSpent":31,"attempts":1,"lastTouched":10,"frequency":85,"tags":["recursion","backtracking","rat-in-maze","dfs"],"companies":["Amazon","Microsoft","Adobe"],"concept":"Find path from start to end in maze","explanation":"DFS from start position. Try all 4 directions. Mark visited cells. Backtrack if dead end. Find all possible paths.","complexity":{"time":"O(4^(m*n))","space":"O(m*n)"},"code":{"javascript":"function findPath(maze) {\n  const result = [];\n  const n = maze.length;\n  const visited = Array(n).fill().map(() => Array(n).fill(false));\n  backtrack(result, '', maze, 0, 0, n, visited);\n  return result;\n}\n\nfunction backtrack(result, path, maze, i, j, n, visited) {\n  if (i === n - 1 && j === n - 1) {\n    result.push(path);\n    return;\n  }\n  visited[i][j] = true;\n  const directions = [['D', 1, 0], ['L', 0, -1], ['R', 0, 1], ['U', -1, 0]];\n  for (const [dir, di, dj] of directions) {\n    const ni = i + di, nj = j + dj;\n    if (ni >= 0 && ni < n && nj >= 0 && nj < n && !visited[ni][nj] && maze[ni][nj] === 1) {\n      backtrack(result, path + dir, maze, ni, nj, n, visited);\n    }\n  }\n  visited[i][j] = false;\n}","python":"def findPath(maze):\n    result = []\n    n = len(maze)\n    visited = [[False] * n for _ in range(n)]\n    def backtrack(path, i, j):\n        if i == n - 1 and j == n - 1:\n            result.append(path)\n            return\n        visited[i][j] = True\n        directions = [('D', 1, 0), ('L', 0, -1), ('R', 0, 1), ('U', -1, 0)]\n        for dir, di, dj in directions:\n            ni, nj = i + di, j + dj\n            if 0 <= ni < n and 0 <= nj < n and not visited[ni][nj] and maze[ni][nj] == 1:\n                backtrack(path + dir, ni, nj)\n        visited[i][j] = False\n    if maze[0][0] == 1:\n        backtrack('', 0, 0)\n    return result","java":"public List<String> findPath(int[][] maze) {\n  List<String> result = new ArrayList<>();\n  int n = maze.length;\n  boolean[][] visited = new boolean[n][n];\n  if (maze[0][0] == 1) backtrack(result, new StringBuilder(), maze, 0, 0, n, visited);\n  return result;\n}\n\nvoid backtrack(List<String> result, StringBuilder path, int[][] maze, int i, int j, int n, boolean[][] visited) {\n  if (i == n - 1 && j == n - 1) {\n    result.add(path.toString());\n    return;\n  }\n  visited[i][j] = true;\n  char[] dirs = {'D', 'L', 'R', 'U'};\n  int[][] moves = {{1, 0}, {0, -1}, {0, 1}, {-1, 0}};\n  for (int k = 0; k < 4; k++) {\n    int ni = i + moves[k][0], nj = j + moves[k][1];\n    if (ni >= 0 && ni < n && nj >= 0 && nj < n && !visited[ni][nj] && maze[ni][nj] == 1) {\n      path.append(dirs[k]);\n      backtrack(result, path, maze, ni, nj, n, visited);\n      path.deleteCharAt(path.length() - 1);\n    }\n  }\n  visited[i][j] = false;\n}","cpp":"vector<string> findPath(vector<vector<int>>& maze) {\n  vector<string> result;\n  int n = maze.size();\n  vector<vector<bool>> visited(n, vector<bool>(n, false));\n  if (maze[0][0] == 1) backtrack(result, \"\", maze, 0, 0, n, visited);\n  return result;\n}\n\nvoid backtrack(vector<string>& result, string path, vector<vector<int>>& maze, int i, int j, int n, vector<vector<bool>>& visited) {\n  if (i == n - 1 && j == n - 1) {\n    result.push_back(path);\n    return;\n  }\n  visited[i][j] = true;\n  vector<pair<char, pair<int, int>>> dirs = {{'D', {1, 0}}, {'L', {0, -1}}, {'R', {0, 1}}, {'U', {-1, 0}}};\n  for (auto& [dir, move] : dirs) {\n    int ni = i + move.first, nj = j + move.second;\n    if (ni >= 0 && ni < n && nj >= 0 && nj < n && !visited[ni][nj] && maze[ni][nj] == 1) {\n      backtrack(result, path + dir, maze, ni, nj, n, visited);\n    }\n  }\n  visited[i][j] = false;\n}"},"challenge":"Find all possible paths","hints":["DFS from start","Mark visited","Try all 4 directions"],"relatedProblems":[66]},{"id":68,"title":"M Coloring Problem","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":80,"tags":["recursion","backtracking","graph-coloring"],"companies":["Amazon","Microsoft","Adobe"],"concept":"Color graph with M colors such that no adjacent vertices have same color","explanation":"Try all colors for each vertex. Check if color is safe (no adjacent vertex has same color). Backtrack if stuck.","complexity":{"time":"O(M^V)","space":"O(V)"},"code":{"javascript":"function graphColoring(graph, m) {\n  const n = graph.length;\n  const colors = Array(n).fill(0);\n  return backtrack(graph, m, colors, 0, n);\n}\n\nfunction backtrack(graph, m, colors, vertex, n) {\n  if (vertex === n) return true;\n  for (let color = 1; color <= m; color++) {\n    if (isSafe(graph, colors, vertex, color, n)) {\n      colors[vertex] = color;\n      if (backtrack(graph, m, colors, vertex + 1, n)) return true;\n      colors[vertex] = 0;\n    }\n  }\n  return false;\n}\n\nfunction isSafe(graph, colors, vertex, color, n) {\n  for (let i = 0; i < n; i++) {\n    if (graph[vertex][i] === 1 && colors[i] === color) return false;\n  }\n  return true;\n}","python":"def graphColoring(graph, m):\n    n = len(graph)\n    colors = [0] * n\n    def backtrack(vertex):\n        if vertex == n:\n            return True\n        for color in range(1, m + 1):\n            if isSafe(vertex, color):\n                colors[vertex] = color\n                if backtrack(vertex + 1):\n                    return True\n                colors[vertex] = 0\n        return False\n    def isSafe(vertex, color):\n        for i in range(n):\n            if graph[vertex][i] == 1 and colors[i] == color:\n                return False\n        return True\n    return backtrack(0)","java":"public boolean graphColoring(int[][] graph, int m) {\n  int n = graph.length;\n  int[] colors = new int[n];\n  return backtrack(graph, m, colors, 0, n);\n}\n\nboolean backtrack(int[][] graph, int m, int[] colors, int vertex, int n) {\n  if (vertex == n) return true;\n  for (int color = 1; color <= m; color++) {\n    if (isSafe(graph, colors, vertex, color, n)) {\n      colors[vertex] = color;\n      if (backtrack(graph, m, colors, vertex + 1, n)) return true;\n      colors[vertex] = 0;\n    }\n  }\n  return false;\n}\n\nboolean isSafe(int[][] graph, int[] colors, int vertex, int color, int n) {\n  for (int i = 0; i < n; i++) {\n    if (graph[vertex][i] == 1 && colors[i] == color) return false;\n  }\n  return true;\n}","cpp":"bool graphColoring(vector<vector<int>>& graph, int m) {\n  int n = graph.size();\n  vector<int> colors(n, 0);\n  return backtrack(graph, m, colors, 0, n);\n}\n\nbool backtrack(vector<vector<int>>& graph, int m, vector<int>& colors, int vertex, int n) {\n  if (vertex == n) return true;\n  for (int color = 1; color <= m; color++) {\n    if (isSafe(graph, colors, vertex, color, n)) {\n      colors[vertex] = color;\n      if (backtrack(graph, m, colors, vertex + 1, n)) return true;\n      colors[vertex] = 0;\n    }\n  }\n  return false;\n}\n\nbool isSafe(vector<vector<int>>& graph, vector<int>& colors, int vertex, int color, int n) {\n  for (int i = 0; i < n; i++) {\n    if (graph[vertex][i] == 1 && colors[i] == color) return false;\n  }\n  return true;\n}"},"challenge":"Check if coloring is possible","hints":["Try all colors for each vertex","Check adjacent vertices","Backtrack"],"relatedProblems":[66,67]},{"id":69,"title":"Sudoku Solver","category":"recursion","categoryName":"Recursion","topic":"Recursion Backtracking","difficulty":"hard","status":"solved","timeSpent":51,"attempts":4,"lastTouched":29,"frequency":95,"tags":["recursion","backtracking","sudoku"],"companies":["Amazon","Microsoft","Facebook","Google","Apple"],"concept":"Fill empty cells in Sudoku board","explanation":"Find empty cell. Try digits 1-9. Check if valid (row, column, 3x3 box). Recursively solve. Backtrack if stuck.","complexity":{"time":"O(9^(n*n))","space":"O(n*n)"},"code":{"javascript":"function solveSudoku(board) {\n  solve(board);\n}\n\nfunction solve(board) {\n  for (let i = 0; i < 9; i++) {\n    for (let j = 0; j < 9; j++) {\n      if (board[i][j] === '.') {\n        for (let c = '1'; c <= '9'; c++) {\n          if (isValid(board, i, j, c)) {\n            board[i][j] = c;\n            if (solve(board)) return true;\n            board[i][j] = '.';\n          }\n        }\n        return false;\n      }\n    }\n  }\n  return true;\n}\n\nfunction isValid(board, row, col, c) {\n  for (let i = 0; i < 9; i++) {\n    if (board[i][col] === c) return false;\n    if (board[row][i] === c) return false;\n    if (board[3 * Math.floor(row / 3) + Math.floor(i / 3)][3 * Math.floor(col / 3) + i % 3] === c) return false;\n  }\n  return true;\n}","python":"def solveSudoku(board):\n    solve(board)\n\ndef solve(board):\n    for i in range(9):\n        for j in range(9):\n            if board[i][j] == '.':\n                for c in '123456789':\n                    if isValid(board, i, j, c):\n                        board[i][j] = c\n                        if solve(board):\n                            return True\n                        board[i][j] = '.'\n                return False\n    return True\n\ndef isValid(board, row, col, c):\n    for i in range(9):\n        if board[i][col] == c:\n            return False\n        if board[row][i] == c:\n            return False\n        if board[3 * (row // 3) + i // 3][3 * (col // 3) + i % 3] == c:\n            return False\n    return True","java":"public void solveSudoku(char[][] board) {\n  solve(board);\n}\n\nboolean solve(char[][] board) {\n  for (int i = 0; i < 9; i++) {\n    for (int j = 0; j < 9; j++) {\n      if (board[i][j] == '.') {\n        for (char c = '1'; c <= '9'; c++) {\n          if (isValid(board, i, j, c)) {\n            board[i][j] = c;\n            if (solve(board)) return true;\n            board[i][j] = '.';\n          }\n        }\n        return false;\n      }\n    }\n  }\n  return true;\n}\n\nboolean isValid(char[][] board, int row, int col, char c) {\n  for (int i = 0; i < 9; i++) {\n    if (board[i][col] == c) return false;\n    if (board[row][i] == c) return false;\n    if (board[3 * (row / 3) + i / 3][3 * (col / 3) + i % 3] == c) return false;\n  }\n  return true;\n}","cpp":"void solveSudoku(vector<vector<char>>& board) {\n  solve(board);\n}\n\nbool solve(vector<vector<char>>& board) {\n  for (int i = 0; i < 9; i++) {\n    for (int j = 0; j < 9; j++) {\n      if (board[i][j] == '.') {\n        for (char c = '1'; c <= '9'; c++) {\n          if (isValid(board, i, j, c)) {\n            board[i][j] = c;\n            if (solve(board)) return true;\n            board[i][j] = '.';\n          }\n        }\n        return false;\n      }\n    }\n  }\n  return true;\n}\n\nbool isValid(vector<vector<char>>& board, int row, int col, char c) {\n  for (int i = 0; i < 9; i++) {\n    if (board[i][col] == c) return false;\n    if (board[row][i] == c) return false;\n    if (board[3 * (row / 3) + i / 3][3 * (col / 3) + i % 3] == c) return false;\n  }\n  return true;\n}"},"challenge":"Optimize with bitmasks or pre-computation","hints":["Find empty cell","Try all digits","Check row, column, box"],"relatedProblems":[66,67,68]},{"id":70,"title":"Introduction to Singly LinkedList","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"attempted","timeSpent":24,"attempts":2,"lastTouched":5,"frequency":50,"tags":["linked-list","singly","basic"],"companies":[],"concept":"Basic structure and creation of singly linked list","explanation":"Singly linked list consists of nodes where each node contains data and reference to next node. Learn to create and traverse.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"class ListNode {\n  constructor(val) {\n    this.val = val;\n    this.next = null;\n  }\n}\n\nclass LinkedList {\n  constructor() {\n    this.head = null;\n  }\n  \n  append(val) {\n    const newNode = new ListNode(val);\n    if (!this.head) {\n      this.head = newNode;\n      return;\n    }\n    let current = this.head;\n    while (current.next) {\n      current = current.next;\n    }\n    current.next = newNode;\n  }\n}","python":"class ListNode:\n    def __init__(self, val=0):\n        self.val = val\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    def append(self, val):\n        new_node = ListNode(val)\n        if not self.head:\n            self.head = new_node\n            return\n        current = self.head\n        while current.next:\n            current = current.next\n        current.next = new_node","java":"class ListNode {\n  int val;\n  ListNode next;\n  ListNode(int val) { this.val = val; }\n}\n\nclass LinkedList {\n  ListNode head;\n  \n  void append(int val) {\n    ListNode newNode = new ListNode(val);\n    if (head == null) {\n      head = newNode;\n      return;\n    }\n    ListNode current = head;\n    while (current.next != null) {\n      current = current.next;\n    }\n    current.next = newNode;\n  }\n}","cpp":"struct ListNode {\n  int val;\n  ListNode* next;\n  ListNode(int x) : val(x), next(nullptr) {}\n};\n\nclass LinkedList {\npublic:\n  ListNode* head;\n  \n  void append(int val) {\n    ListNode* newNode = new ListNode(val);\n    if (!head) {\n      head = newNode;\n      return;\n    }\n    ListNode* current = head;\n    while (current->next) {\n      current = current->next;\n    }\n    current->next = newNode;\n  }\n};"},"challenge":"Implement all basic operations","hints":["Node has data and next","Head points to first node","Traverse to end for append"],"relatedProblems":[71,72,73]},{"id":71,"title":"Traversal in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":60,"tags":["linked-list","traversal"],"companies":[],"concept":"Traverse and print linked list elements","explanation":"Start from head and visit each node until null. Print or process each node's data during traversal.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function traverse(head) {\n  const result = [];\n  let current = head;\n  while (current) {\n    result.push(current.val);\n    current = current.next;\n  }\n  return result;\n}","python":"def traverse(head):\n    result = []\n    current = head\n    while current:\n        result.append(current.val)\n        current = current.next\n    return result","java":"public List<Integer> traverse(ListNode head) {\n  List<Integer> result = new ArrayList<>();\n  ListNode current = head;\n  while (current != null) {\n    result.add(current.val);\n    current = current.next;\n  }\n  return result;\n}","cpp":"vector<int> traverse(ListNode* head) {\n  vector<int> result;\n  ListNode* current = head;\n  while (current) {\n    result.push_back(current->val);\n    current = current->next;\n  }\n  return result;\n}"},"challenge":"Recursive traversal","hints":["Start from head","Visit each node","Move to next"],"relatedProblems":[70]},{"id":72,"title":"Deletion in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"attempted","timeSpent":21,"attempts":3,"lastTouched":25,"frequency":75,"tags":["linked-list","deletion"],"companies":["Amazon","Microsoft"],"concept":"Delete node with given value from linked list","explanation":"Find node to delete. Update previous node's next to skip current node. Handle head deletion separately.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function deleteNode(head, val) {\n  if (!head) return null;\n  if (head.val === val) return head.next;\n  let current = head;\n  while (current.next && current.next.val !== val) {\n    current = current.next;\n  }\n  if (current.next) {\n    current.next = current.next.next;\n  }\n  return head;\n}","python":"def deleteNode(head, val):\n    if not head:\n        return None\n    if head.val == val:\n        return head.next\n    current = head\n    while current.next and current.next.val != val:\n        current = current.next\n    if current.next:\n        current.next = current.next.next\n    return head","java":"public ListNode deleteNode(ListNode head, int val) {\n  if (head == null) return null;\n  if (head.val == val) return head.next;\n  ListNode current = head;\n  while (current.next != null && current.next.val != val) {\n    current = current.next;\n  }\n  if (current.next != null) {\n    current.next = current.next.next;\n  }\n  return head;\n}","cpp":"ListNode* deleteNode(ListNode* head, int val) {\n  if (!head) return nullptr;\n  if (head->val == val) return head->next;\n  ListNode* current = head;\n  while (current->next && current->next->val != val) {\n    current = current->next;\n  }\n  if (current->next) {\n    current->next = current->next->next;\n  }\n  return head;\n}"},"challenge":"Delete all occurrences","hints":["Handle head separately","Find previous node","Update next pointer"],"relatedProblems":[74,75,76,77]},{"id":73,"title":"Insertion in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":75,"tags":["linked-list","insertion"],"companies":["Amazon","Microsoft"],"concept":"Insert node at given position in linked list","explanation":"Traverse to position-1. Create new node and insert between current and current.next. Handle head insertion separately.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function insertNode(head, val, position) {\n  const newNode = new ListNode(val);\n  if (position === 0) {\n    newNode.next = head;\n    return newNode;\n  }\n  let current = head;\n  for (let i = 0; i < position - 1 && current; i++) {\n    current = current.next;\n  }\n  if (!current) return head;\n  newNode.next = current.next;\n  current.next = newNode;\n  return head;\n}","python":"def insertNode(head, val, position):\n    new_node = ListNode(val)\n    if position == 0:\n        new_node.next = head\n        return new_node\n    current = head\n    for _ in range(position - 1):\n        if not current:\n            return head\n        current = current.next\n    if not current:\n        return head\n    new_node.next = current.next\n    current.next = new_node\n    return head","java":"public ListNode insertNode(ListNode head, int val, int position) {\n  ListNode newNode = new ListNode(val);\n  if (position == 0) {\n    newNode.next = head;\n    return newNode;\n  }\n  ListNode current = head;\n  for (int i = 0; i < position - 1 && current != null; i++) {\n    current = current.next;\n  }\n  if (current == null) return head;\n  newNode.next = current.next;\n  current.next = newNode;\n  return head;\n}","cpp":"ListNode* insertNode(ListNode* head, int val, int position) {\n  ListNode* newNode = new ListNode(val);\n  if (position == 0) {\n    newNode->next = head;\n    return newNode;\n  }\n  ListNode* current = head;\n  for (int i = 0; i < position - 1 && current; i++) {\n    current = current->next;\n  }\n  if (!current) return head;\n  newNode->next = current->next;\n  current->next = newNode;\n  return head;\n}"},"challenge":"Insert at end efficiently","hints":["Handle position 0","Traverse to position-1","Insert between"],"relatedProblems":[78,79,80,81]},{"id":74,"title":"Deletion of the head of LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"solved","timeSpent":64,"attempts":4,"lastTouched":10,"frequency":60,"tags":["linked-list","deletion","head"],"companies":[],"concept":"Delete first node of linked list","explanation":"Simply move head to next node. Return new head. Handle empty list case.","complexity":{"time":"O(1)","space":"O(1)"},"code":{"javascript":"function deleteHead(head) {\n  if (!head) return null;\n  return head.next;\n}","python":"def deleteHead(head):\n    if not head:\n        return None\n    return head.next","java":"public ListNode deleteHead(ListNode head) {\n  if (head == null) return null;\n  return head.next;\n}","cpp":"ListNode* deleteHead(ListNode* head) {\n  if (!head) return nullptr;\n  return head->next;\n}"},"challenge":"Handle single node list","hints":["Move head to next","O(1) operation","Handle empty"],"relatedProblems":[72,75,76,77]},{"id":75,"title":"Deletion of the tail of Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"solved","timeSpent":20,"attempts":1,"lastTouched":20,"frequency":65,"tags":["linked-list","deletion","tail"],"companies":[],"concept":"Delete last node of linked list","explanation":"Traverse to second last node. Set its next to null. Handle single node case.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function deleteTail(head) {\n  if (!head || !head.next) return null;\n  let current = head;\n  while (current.next.next) {\n    current = current.next;\n  }\n  current.next = null;\n  return head;\n}","python":"def deleteTail(head):\n    if not head or not head.next:\n        return None\n    current = head\n    while current.next.next:\n        current = current.next\n    current.next = None\n    return head","java":"public ListNode deleteTail(ListNode head) {\n  if (head == null || head.next == null) return null;\n  ListNode current = head;\n  while (current.next.next != null) {\n    current = current.next;\n  }\n  current.next = null;\n  return head;\n}","cpp":"ListNode* deleteTail(ListNode* head) {\n  if (!head || !head->next) return nullptr;\n  ListNode* current = head;\n  while (current->next->next) {\n    current = current->next;\n  }\n  current->next = nullptr;\n  return head;\n}"},"challenge":"Handle single node","hints":["Traverse to second last","Set next to null","Handle edge cases"],"relatedProblems":[72,74,76,77]},{"id":76,"title":"Deletion of the Kth element of Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"attempted","timeSpent":65,"attempts":1,"lastTouched":9,"frequency":70,"tags":["linked-list","deletion"],"companies":[],"concept":"Delete node at kth position","explanation":"Traverse to k-1 position. Update next pointer to skip kth node. Handle head case separately.","complexity":{"time":"O(k)","space":"O(1)"},"code":{"javascript":"function deleteKth(head, k) {\n  if (k === 1) return head ? head.next : null;\n  let current = head;\n  for (let i = 1; i < k - 1 && current; i++) {\n    current = current.next;\n  }\n  if (current && current.next) {\n    current.next = current.next.next;\n  }\n  return head;\n}","python":"def deleteKth(head, k):\n    if k == 1:\n        return head.next if head else None\n    current = head\n    for _ in range(k - 2):\n        if not current:\n            return head\n        current = current.next\n    if current and current.next:\n        current.next = current.next.next\n    return head","java":"public ListNode deleteKth(ListNode head, int k) {\n  if (k == 1) return head != null ? head.next : null;\n  ListNode current = head;\n  for (int i = 1; i < k - 1 && current != null; i++) {\n    current = current.next;\n  }\n  if (current != null && current.next != null) {\n    current.next = current.next.next;\n  }\n  return head;\n}","cpp":"ListNode* deleteKth(ListNode* head, int k) {\n  if (k == 1) return head ? head->next : nullptr;\n  ListNode* current = head;\n  for (int i = 1; i < k - 1 && current; i++) {\n    current = current->next;\n  }\n  if (current && current->next) {\n    current->next = current->next->next;\n  }\n  return head;\n}"},"challenge":"Handle k > length","hints":["Handle k=1 separately","Traverse to k-1","Update next pointer"],"relatedProblems":[72,74,75,77]},{"id":77,"title":"Delete the element with value X","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"solved","timeSpent":17,"attempts":4,"lastTouched":17,"frequency":70,"tags":["linked-list","deletion"],"companies":[],"concept":"Delete first node with given value","explanation":"Find node with value X. Update previous node's next to skip it. Handle if X is at head.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function deleteValue(head, x) {\n  if (!head) return null;\n  if (head.val === x) return head.next;\n  let current = head;\n  while (current.next && current.next.val !== x) {\n    current = current.next;\n  }\n  if (current.next) {\n    current.next = current.next.next;\n  }\n  return head;\n}","python":"def deleteValue(head, x):\n    if not head:\n        return None\n    if head.val == x:\n        return head.next\n    current = head\n    while current.next and current.next.val != x:\n        current = current.next\n    if current.next:\n        current.next = current.next.next\n    return head","java":"public ListNode deleteValue(ListNode head, int x) {\n  if (head == null) return null;\n  if (head.val == x) return head.next;\n  ListNode current = head;\n  while (current.next != null && current.next.val != x) {\n    current = current.next;\n  }\n  if (current.next != null) {\n    current.next = current.next.next;\n  }\n  return head;\n}","cpp":"ListNode* deleteValue(ListNode* head, int x) {\n  if (!head) return nullptr;\n  if (head->val == x) return head->next;\n  ListNode* current = head;\n  while (current->next && current->next->val != x) {\n    current = current->next;\n  }\n  if (current->next) {\n    current->next = current->next->next;\n  }\n  return head;\n}"},"challenge":"Delete all occurrences","hints":["Handle head","Find previous","Update next"],"relatedProblems":[72,74,75,76]},{"id":78,"title":"Insertion at the head of Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":60,"tags":["linked-list","insertion","head"],"companies":[],"concept":"Insert new node at beginning of linked list","explanation":"Create new node. Set its next to current head. Update head to new node. O(1) operation.","complexity":{"time":"O(1)","space":"O(1)"},"code":{"javascript":"function insertHead(head, val) {\n  const newNode = new ListNode(val);\n  newNode.next = head;\n  return newNode;\n}","python":"def insertHead(head, val):\n    new_node = ListNode(val)\n    new_node.next = head\n    return new_node","java":"public ListNode insertHead(ListNode head, int val) {\n  ListNode newNode = new ListNode(val);\n  newNode.next = head;\n  return newNode;\n}","cpp":"ListNode* insertHead(ListNode* head, int val) {\n  ListNode* newNode = new ListNode(val);\n  newNode->next = head;\n  return newNode;\n}"},"challenge":"Handle empty list","hints":["O(1) operation","New node next = head","Return new head"],"relatedProblems":[73,79,80,81]},{"id":79,"title":"Insertion at the tail of Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"bookmarked","timeSpent":24,"attempts":3,"lastTouched":2,"frequency":65,"tags":["linked-list","insertion","tail"],"companies":[],"concept":"Insert new node at end of linked list","explanation":"If list empty, new node is head. Otherwise traverse to end and append. O(n) operation.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function insertTail(head, val) {\n  const newNode = new ListNode(val);\n  if (!head) return newNode;\n  let current = head;\n  while (current.next) {\n    current = current.next;\n  }\n  current.next = newNode;\n  return head;\n}","python":"def insertTail(head, val):\n    new_node = ListNode(val)\n    if not head:\n        return new_node\n    current = head\n    while current.next:\n        current = current.next\n    current.next = new_node\n    return head","java":"public ListNode insertTail(ListNode head, int val) {\n  ListNode newNode = new ListNode(val);\n  if (head == null) return newNode;\n  ListNode current = head;\n  while (current.next != null) {\n    current = current.next;\n  }\n  current.next = newNode;\n  return head;\n}","cpp":"ListNode* insertTail(ListNode* head, int val) {\n  ListNode* newNode = new ListNode(val);\n  if (!head) return newNode;\n  ListNode* current = head;\n  while (current->next) {\n    current = current->next;\n  }\n  current->next = newNode;\n  return head;\n}"},"challenge":"Maintain tail pointer for O(1)","hints":["Handle empty list","Traverse to end","Append new node"],"relatedProblems":[73,78,80,81]},{"id":80,"title":"Insertion at the Kth position of Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"bookmarked","timeSpent":65,"attempts":3,"lastTouched":8,"frequency":70,"tags":["linked-list","insertion"],"companies":[],"concept":"Insert new node at kth position","explanation":"If k=1, insert at head. Otherwise traverse to k-1 position and insert. Handle invalid k.","complexity":{"time":"O(k)","space":"O(1)"},"code":{"javascript":"function insertAtK(head, val, k) {\n  const newNode = new ListNode(val);\n  if (k === 1) {\n    newNode.next = head;\n    return newNode;\n  }\n  let current = head;\n  for (let i = 1; i < k - 1 && current; i++) {\n    current = current.next;\n  }\n  if (!current) return head;\n  newNode.next = current.next;\n  current.next = newNode;\n  return head;\n}","python":"def insertAtK(head, val, k):\n    new_node = ListNode(val)\n    if k == 1:\n        new_node.next = head\n        return new_node\n    current = head\n    for _ in range(k - 2):\n        if not current:\n            return head\n        current = current.next\n    if not current:\n        return head\n    new_node.next = current.next\n    current.next = new_node\n    return head","java":"public ListNode insertAtK(ListNode head, int val, int k) {\n  ListNode newNode = new ListNode(val);\n  if (k == 1) {\n    newNode.next = head;\n    return newNode;\n  }\n  ListNode current = head;\n  for (int i = 1; i < k - 1 && current != null; i++) {\n    current = current.next;\n  }\n  if (current == null) return head;\n  newNode.next = current.next;\n  current.next = newNode;\n  return head;\n}","cpp":"ListNode* insertAtK(ListNode* head, int val, int k) {\n  ListNode* newNode = new ListNode(val);\n  if (k == 1) {\n    newNode->next = head;\n    return newNode;\n  }\n  ListNode* current = head;\n  for (int i = 1; i < k - 1 && current; i++) {\n    current = current->next;\n  }\n  if (!current) return head;\n  newNode->next = current->next;\n  current->next = newNode;\n  return head;\n}"},"challenge":"Handle k > length + 1","hints":["Handle k=1","Traverse to k-1","Insert between"],"relatedProblems":[73,78,79,81]},{"id":81,"title":"Insertion before the value X in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Basic","difficulty":"easy","status":"solved","timeSpent":40,"attempts":3,"lastTouched":2,"frequency":70,"tags":["linked-list","insertion"],"companies":[],"concept":"Insert new node before first occurrence of value X","explanation":"If head has value X, insert at head. Otherwise find node with value X and insert before it.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function insertBeforeX(head, val, x) {\n  const newNode = new ListNode(val);\n  if (!head) return newNode;\n  if (head.val === x) {\n    newNode.next = head;\n    return newNode;\n  }\n  let current = head;\n  while (current.next && current.next.val !== x) {\n    current = current.next;\n  }\n  if (current.next) {\n    newNode.next = current.next;\n    current.next = newNode;\n  }\n  return head;\n}","python":"def insertBeforeX(head, val, x):\n    new_node = ListNode(val)\n    if not head:\n        return new_node\n    if head.val == x:\n        new_node.next = head\n        return new_node\n    current = head\n    while current.next and current.next.val != x:\n        current = current.next\n    if current.next:\n        new_node.next = current.next\n        current.next = new_node\n    return head","java":"public ListNode insertBeforeX(ListNode head, int val, int x) {\n  ListNode newNode = new ListNode(val);\n  if (head == null) return newNode;\n  if (head.val == x) {\n    newNode.next = head;\n    return newNode;\n  }\n  ListNode current = head;\n  while (current.next != null && current.next.val != x) {\n    current = current.next;\n  }\n  if (current.next != null) {\n    newNode.next = current.next;\n    current.next = newNode;\n  }\n  return head;\n}","cpp":"ListNode* insertBeforeX(ListNode* head, int val, int x) {\n  ListNode* newNode = new ListNode(val);\n  if (!head) return newNode;\n  if (head->val == x) {\n    newNode->next = head;\n    return newNode;\n  }\n  ListNode* current = head;\n  while (current->next && current->next->val != x) {\n    current = current->next;\n  }\n  if (current->next) {\n    newNode->next = current->next;\n    current->next = newNode;\n  }\n  return head;\n}"},"challenge":"Handle X not found","hints":["Check head first","Find node with X","Insert before"],"relatedProblems":[73,78,79,80]},{"id":82,"title":"Deletion in Doubly LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"solved","timeSpent":57,"attempts":3,"lastTouched":14,"frequency":70,"tags":["linked-list","doubly","deletion"],"companies":[],"concept":"Delete node from doubly linked list","explanation":"Update prev and next pointers of adjacent nodes. Handle head and tail cases separately.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"class DoublyListNode {\n  constructor(val) {\n    this.val = val;\n    this.prev = null;\n    this.next = null;\n  }\n}\n\nfunction deleteNode(head, val) {\n  if (!head) return null;\n  let current = head;\n  while (current && current.val !== val) {\n    current = current.next;\n  }\n  if (!current) return head;\n  if (current.prev) current.prev.next = current.next;\n  if (current.next) current.next.prev = current.prev;\n  return current === head ? current.next : head;\n}","python":"class DoublyListNode:\n    def __init__(self, val=0):\n        self.val = val\n        self.prev = None\n        self.next = None\n\ndef deleteNode(head, val):\n    if not head:\n        return None\n    current = head\n    while current and current.val != val:\n        current = current.next\n    if not current:\n        return head\n    if current.prev:\n        current.prev.next = current.next\n    if current.next:\n        current.next.prev = current.prev\n    return current.next if current == head else head","java":"class DoublyListNode {\n  int val;\n  DoublyListNode prev, next;\n  DoublyListNode(int val) { this.val = val; }\n}\n\npublic DoublyListNode deleteNode(DoublyListNode head, int val) {\n  if (head == null) return null;\n  DoublyListNode current = head;\n  while (current != null && current.val != val) {\n    current = current.next;\n  }\n  if (current == null) return head;\n  if (current.prev != null) current.prev.next = current.next;\n  if (current.next != null) current.next.prev = current.prev;\n  return current == head ? current.next : head;\n}","cpp":"struct DoublyListNode {\n  int val;\n  DoublyListNode *prev, *next;\n  DoublyListNode(int x) : val(x), prev(nullptr), next(nullptr) {}\n};\n\nDoublyListNode* deleteNode(DoublyListNode* head, int val) {\n  if (!head) return nullptr;\n  DoublyListNode* current = head;\n  while (current && current->val != val) {\n    current = current->next;\n  }\n  if (!current) return head;\n  if (current->prev) current->prev->next = current->next;\n  if (current->next) current->next->prev = current->prev;\n  return current == head ? current->next : head;\n}"},"challenge":"Handle all edge cases","hints":["Update both prev and next","Handle head/tail","Find node first"],"relatedProblems":[83,85,86,87,88]},{"id":83,"title":"Insertion in DLL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"attempted","timeSpent":44,"attempts":2,"lastTouched":1,"frequency":70,"tags":["linked-list","doubly","insertion"],"companies":[],"concept":"Insert node in doubly linked list","explanation":"Update four pointers: new node's prev/next and adjacent nodes' pointers. Handle head insertion.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function insertNode(head, val, position) {\n  const newNode = new DoublyListNode(val);\n  if (position === 0) {\n    if (head) head.prev = newNode;\n    newNode.next = head;\n    return newNode;\n  }\n  let current = head;\n  for (let i = 0; i < position - 1 && current; i++) {\n    current = current.next;\n  }\n  if (!current) return head;\n  newNode.next = current.next;\n  newNode.prev = current;\n  if (current.next) current.next.prev = newNode;\n  current.next = newNode;\n  return head;\n}","python":"def insertNode(head, val, position):\n    new_node = DoublyListNode(val)\n    if position == 0:\n        if head:\n            head.prev = new_node\n        new_node.next = head\n        return new_node\n    current = head\n    for _ in range(position - 1):\n        if not current:\n            return head\n        current = current.next\n    if not current:\n        return head\n    new_node.next = current.next\n    new_node.prev = current\n    if current.next:\n        current.next.prev = new_node\n    current.next = new_node\n    return head","java":"public DoublyListNode insertNode(DoublyListNode head, int val, int position) {\n  DoublyListNode newNode = new DoublyListNode(val);\n  if (position == 0) {\n    if (head != null) head.prev = newNode;\n    newNode.next = head;\n    return newNode;\n  }\n  DoublyListNode current = head;\n  for (int i = 0; i < position - 1 && current != null; i++) {\n    current = current.next;\n  }\n  if (current == null) return head;\n  newNode.next = current.next;\n  newNode.prev = current;\n  if (current.next != null) current.next.prev = newNode;\n  current.next = newNode;\n  return head;\n}","cpp":"DoublyListNode* insertNode(DoublyListNode* head, int val, int position) {\n  DoublyListNode* newNode = new DoublyListNode(val);\n  if (position == 0) {\n    if (head) head->prev = newNode;\n    newNode->next = head;\n    return newNode;\n  }\n  DoublyListNode* current = head;\n  for (int i = 0; i < position - 1 && current; i++) {\n    current = current->next;\n  }\n  if (!current) return head;\n  newNode->next = current->next;\n  newNode->prev = current;\n  if (current->next) current->next->prev = newNode;\n  current->next = newNode;\n  return head;\n}"},"challenge":"Update all four pointers correctly","hints":["Update prev and next","Handle head","Link adjacent nodes"],"relatedProblems":[82,84,85,86,87,88]},{"id":84,"title":"Convert Array to Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"solved","timeSpent":30,"attempts":4,"lastTouched":21,"frequency":60,"tags":["linked-list","doubly","array-conversion"],"companies":[],"concept":"Create doubly linked list from array","explanation":"Iterate through array. Create node for each element. Link nodes with prev and next pointers.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function arrayToDLL(arr) {\n  if (arr.length === 0) return null;\n  const head = new DoublyListNode(arr[0]);\n  let current = head;\n  for (let i = 1; i < arr.length; i++) {\n    const newNode = new DoublyListNode(arr[i]);\n    current.next = newNode;\n    newNode.prev = current;\n    current = newNode;\n  }\n  return head;\n}","python":"def arrayToDLL(arr):\n    if not arr:\n        return None\n    head = DoublyListNode(arr[0])\n    current = head\n    for i in range(1, len(arr)):\n        new_node = DoublyListNode(arr[i])\n        current.next = new_node\n        new_node.prev = current\n        current = new_node\n    return head","java":"public DoublyListNode arrayToDLL(int[] arr) {\n  if (arr.length == 0) return null;\n  DoublyListNode head = new DoublyListNode(arr[0]);\n  DoublyListNode current = head;\n  for (int i = 1; i < arr.length; i++) {\n    DoublyListNode newNode = new DoublyListNode(arr[i]);\n    current.next = newNode;\n    newNode.prev = current;\n    current = newNode;\n  }\n  return head;\n}","cpp":"DoublyListNode* arrayToDLL(vector<int>& arr) {\n  if (arr.empty()) return nullptr;\n  DoublyListNode* head = new DoublyListNode(arr[0]);\n  DoublyListNode* current = head;\n  for (int i = 1; i < arr.size(); i++) {\n    DoublyListNode* newNode = new DoublyListNode(arr[i]);\n    current->next = newNode;\n    newNode->prev = current;\n    current = newNode;\n  }\n  return head;\n}"},"challenge":"Handle empty array","hints":["Create head first","Link prev and next","Iterate array"],"relatedProblems":[82,83]},{"id":85,"title":"Delete head of Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":60,"tags":["linked-list","doubly","deletion","head"],"companies":[],"concept":"Delete first node of doubly linked list","explanation":"Move head to next node. Update new head's prev to null. Return new head.","complexity":{"time":"O(1)","space":"O(1)"},"code":{"javascript":"function deleteHead(head) {\n  if (!head) return null;\n  const newHead = head.next;\n  if (newHead) newHead.prev = null;\n  return newHead;\n}","python":"def deleteHead(head):\n    if not head:\n        return None\n    new_head = head.next\n    if new_head:\n        new_head.prev = None\n    return new_head","java":"public DoublyListNode deleteHead(DoublyListNode head) {\n  if (head == null) return null;\n  DoublyListNode newHead = head.next;\n  if (newHead != null) newHead.prev = null;\n  return newHead;\n}","cpp":"DoublyListNode* deleteHead(DoublyListNode* head) {\n  if (!head) return nullptr;\n  DoublyListNode* newHead = head->next;\n  if (newHead) newHead->prev = nullptr;\n  return newHead;\n}"},"challenge":"Handle single node","hints":["Move head to next","Set prev to null","O(1) operation"],"relatedProblems":[82,86,87,88]},{"id":86,"title":"Delete Tail of Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"solved","timeSpent":59,"attempts":2,"lastTouched":1,"frequency":65,"tags":["linked-list","doubly","deletion","tail"],"companies":[],"concept":"Delete last node of doubly linked list","explanation":"Traverse to last node. Update second last node's next to null. Return head.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function deleteTail(head) {\n  if (!head || !head.next) return null;\n  let current = head;\n  while (current.next) {\n    current = current.next;\n  }\n  current.prev.next = null;\n  return head;\n}","python":"def deleteTail(head):\n    if not head or not head.next:\n        return None\n    current = head\n    while current.next:\n        current = current.next\n    current.prev.next = None\n    return head","java":"public DoublyListNode deleteTail(DoublyListNode head) {\n  if (head == null || head.next == null) return null;\n  DoublyListNode current = head;\n  while (current.next != null) {\n    current = current.next;\n  }\n  current.prev.next = null;\n  return head;\n}","cpp":"DoublyListNode* deleteTail(DoublyListNode* head) {\n  if (!head || !head->next) return nullptr;\n  DoublyListNode* current = head;\n  while (current->next) {\n    current = current->next;\n  }\n  current->prev->next = nullptr;\n  return head;\n}"},"challenge":"Maintain tail pointer for O(1)","hints":["Traverse to end","Update prev node's next","Handle edge cases"],"relatedProblems":[82,85,87,88]},{"id":87,"title":"Delete Kth Element of Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":70,"tags":["linked-list","doubly","deletion"],"companies":[],"concept":"Delete node at kth position in doubly linked list","explanation":"Traverse to kth node. Update prev and next pointers of adjacent nodes. Handle head case.","complexity":{"time":"O(k)","space":"O(1)"},"code":{"javascript":"function deleteKth(head, k) {\n  if (!head) return null;\n  if (k === 1) {\n    const newHead = head.next;\n    if (newHead) newHead.prev = null;\n    return newHead;\n  }\n  let current = head;\n  for (let i = 1; i < k && current; i++) {\n    current = current.next;\n  }\n  if (!current) return head;\n  if (current.prev) current.prev.next = current.next;\n  if (current.next) current.next.prev = current.prev;\n  return head;\n}","python":"def deleteKth(head, k):\n    if not head:\n        return None\n    if k == 1:\n        new_head = head.next\n        if new_head:\n            new_head.prev = None\n        return new_head\n    current = head\n    for _ in range(k - 1):\n        if not current:\n            return head\n        current = current.next\n    if not current:\n        return head\n    if current.prev:\n        current.prev.next = current.next\n    if current.next:\n        current.next.prev = current.prev\n    return head","java":"public DoublyListNode deleteKth(DoublyListNode head, int k) {\n  if (head == null) return null;\n  if (k == 1) {\n    DoublyListNode newHead = head.next;\n    if (newHead != null) newHead.prev = null;\n    return newHead;\n  }\n  DoublyListNode current = head;\n  for (int i = 1; i < k && current != null; i++) {\n    current = current.next;\n  }\n  if (current == null) return head;\n  if (current.prev != null) current.prev.next = current.next;\n  if (current.next != null) current.next.prev = current.prev;\n  return head;\n}","cpp":"DoublyListNode* deleteKth(DoublyListNode* head, int k) {\n  if (!head) return nullptr;\n  if (k == 1) {\n    DoublyListNode* newHead = head->next;\n    if (newHead) newHead->prev = nullptr;\n    return newHead;\n  }\n  DoublyListNode* current = head;\n  for (int i = 1; i < k && current; i++) {\n    current = current->next;\n  }\n  if (!current) return head;\n  if (current->prev) current->prev->next = current->next;\n  if (current->next) current->next->prev = current->prev;\n  return head;\n}"},"challenge":"Handle k > length","hints":["Handle k=1","Traverse to kth","Update both pointers"],"relatedProblems":[82,85,86,88]},{"id":88,"title":"Removing given node in Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"solved","timeSpent":22,"attempts":1,"lastTouched":0,"frequency":75,"tags":["linked-list","doubly","deletion"],"companies":["Amazon","Microsoft"],"concept":"Delete given node in doubly linked list (not head)","explanation":"Given node to delete (not head). Update prev and next pointers of adjacent nodes to bypass given node.","complexity":{"time":"O(1)","space":"O(1)"},"code":{"javascript":"function deleteNode(node) {\n  if (!node || !node.prev) return;\n  node.prev.next = node.next;\n  if (node.next) node.next.prev = node.prev;\n}","python":"def deleteNode(node):\n    if not node or not node.prev:\n        return\n    node.prev.next = node.next\n    if node.next:\n        node.next.prev = node.prev","java":"public void deleteNode(DoublyListNode node) {\n  if (node == null || node.prev == null) return;\n  node.prev.next = node.next;\n  if (node.next != null) node.next.prev = node.prev;\n}","cpp":"void deleteNode(DoublyListNode* node) {\n  if (!node || !node->prev) return;\n  node->prev->next = node->next;\n  if (node->next) node->next->prev = node->prev;\n}"},"challenge":"Given only node to delete","hints":["Not given head","Update adjacent pointers","O(1) operation"],"relatedProblems":[82,85,86,87]},{"id":89,"title":"Insert node before head in Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"solved","timeSpent":28,"attempts":4,"lastTouched":22,"frequency":60,"tags":["linked-list","doubly","insertion"],"companies":[],"concept":"Insert new node before head in doubly linked list","explanation":"Create new node. Set its next to current head. Update head's prev to new node. Return new head.","complexity":{"time":"O(1)","space":"O(1)"},"code":{"javascript":"function insertBeforeHead(head, val) {\n  const newNode = new DoublyListNode(val);\n  if (head) {\n    head.prev = newNode;\n    newNode.next = head;\n  }\n  return newNode;\n}","python":"def insertBeforeHead(head, val):\n    new_node = DoublyListNode(val)\n    if head:\n        head.prev = new_node\n        new_node.next = head\n    return new_node","java":"public DoublyListNode insertBeforeHead(DoublyListNode head, int val) {\n  DoublyListNode newNode = new DoublyListNode(val);\n  if (head != null) {\n    head.prev = newNode;\n    newNode.next = head;\n  }\n  return newNode;\n}","cpp":"DoublyListNode* insertBeforeHead(DoublyListNode* head, int val) {\n  DoublyListNode* newNode = new DoublyListNode(val);\n  if (head) {\n    head->prev = newNode;\n    newNode->next = head;\n  }\n  return newNode;\n}"},"challenge":"Handle empty list","hints":["O(1) operation","Update both pointers","Return new head"],"relatedProblems":[83,90,91,92]},{"id":90,"title":"Insert node before tail in Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"revisit","timeSpent":39,"attempts":1,"lastTouched":12,"frequency":65,"tags":["linked-list","doubly","insertion"],"companies":[],"concept":"Insert new node before last node in doubly linked list","explanation":"Traverse to last node. Insert new node between second last and last node. Update four pointers.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function insertBeforeTail(head, val) {\n  if (!head) return new DoublyListNode(val);\n  const newNode = new DoublyListNode(val);\n  if (!head.next) {\n    newNode.next = head;\n    head.prev = newNode;\n    return newNode;\n  }\n  let current = head;\n  while (current.next) {\n    current = current.next;\n  }\n  newNode.prev = current.prev;\n  newNode.next = current;\n  current.prev.next = newNode;\n  current.prev = newNode;\n  return head;\n}","python":"def insertBeforeTail(head, val):\n    if not head:\n        return DoublyListNode(val)\n    new_node = DoublyListNode(val)\n    if not head.next:\n        new_node.next = head\n        head.prev = new_node\n        return new_node\n    current = head\n    while current.next:\n        current = current.next\n    new_node.prev = current.prev\n    new_node.next = current\n    current.prev.next = new_node\n    current.prev = new_node\n    return head","java":"public DoublyListNode insertBeforeTail(DoublyListNode head, int val) {\n  if (head == null) return new DoublyListNode(val);\n  DoublyListNode newNode = new DoublyListNode(val);\n  if (head.next == null) {\n    newNode.next = head;\n    head.prev = newNode;\n    return newNode;\n  }\n  DoublyListNode current = head;\n  while (current.next != null) {\n    current = current.next;\n  }\n  newNode.prev = current.prev;\n  newNode.next = current;\n  current.prev.next = newNode;\n  current.prev = newNode;\n  return head;\n}","cpp":"DoublyListNode* insertBeforeTail(DoublyListNode* head, int val) {\n  if (!head) return new DoublyListNode(val);\n  DoublyListNode* newNode = new DoublyListNode(val);\n  if (!head->next) {\n    newNode->next = head;\n    head->prev = newNode;\n    return newNode;\n  }\n  DoublyListNode* current = head;\n  while (current->next) {\n    current = current->next;\n  }\n  newNode->prev = current->prev;\n  newNode->next = current;\n  current->prev->next = newNode;\n  current->prev = newNode;\n  return head;\n}"},"challenge":"Handle single node list","hints":["Traverse to tail","Insert before","Update four pointers"],"relatedProblems":[83,89,91,92]},{"id":91,"title":"Insert node before (kth node) in Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"solved","timeSpent":36,"attempts":2,"lastTouched":5,"frequency":70,"tags":["linked-list","doubly","insertion"],"companies":[],"concept":"Insert new node before kth node in doubly linked list","explanation":"Traverse to kth node. Insert new node between k-1 and k. Update four pointers. Handle k=1 (before head).","complexity":{"time":"O(k)","space":"O(1)"},"code":{"javascript":"function insertBeforeKth(head, val, k) {\n  if (k === 1) {\n    const newNode = new DoublyListNode(val);\n    if (head) {\n      head.prev = newNode;\n      newNode.next = head;\n    }\n    return newNode;\n  }\n  let current = head;\n  for (let i = 1; i < k && current; i++) {\n    current = current.next;\n  }\n  if (!current) return head;\n  const newNode = new DoublyListNode(val);\n  newNode.prev = current.prev;\n  newNode.next = current;\n  current.prev.next = newNode;\n  current.prev = newNode;\n  return head;\n}","python":"def insertBeforeKth(head, val, k):\n    if k == 1:\n        new_node = DoublyListNode(val)\n        if head:\n            head.prev = new_node\n            new_node.next = head\n        return new_node\n    current = head\n    for _ in range(k - 1):\n        if not current:\n            return head\n        current = current.next\n    if not current:\n        return head\n    new_node = DoublyListNode(val)\n    new_node.prev = current.prev\n    new_node.next = current\n    current.prev.next = new_node\n    current.prev = new_node\n    return head","java":"public DoublyListNode insertBeforeKth(DoublyListNode head, int val, int k) {\n  if (k == 1) {\n    DoublyListNode newNode = new DoublyListNode(val);\n    if (head != null) {\n      head.prev = newNode;\n      newNode.next = head;\n    }\n    return newNode;\n  }\n  DoublyListNode current = head;\n  for (int i = 1; i < k && current != null; i++) {\n    current = current.next;\n  }\n  if (current == null) return head;\n  DoublyListNode newNode = new DoublyListNode(val);\n  newNode.prev = current.prev;\n  newNode.next = current;\n  current.prev.next = newNode;\n  current.prev = newNode;\n  return head;\n}","cpp":"DoublyListNode* insertBeforeKth(DoublyListNode* head, int val, int k) {\n  if (k == 1) {\n    DoublyListNode* newNode = new DoublyListNode(val);\n    if (head) {\n      head->prev = newNode;\n      newNode->next = head;\n    }\n    return newNode;\n  }\n  DoublyListNode* current = head;\n  for (int i = 1; i < k && current; i++) {\n    current = current->next;\n  }\n  if (!current) return head;\n  DoublyListNode* newNode = new DoublyListNode(val);\n  newNode->prev = current->prev;\n  newNode->next = current;\n  current->prev->next = newNode;\n  current->prev = newNode;\n  return head;\n}"},"challenge":"Handle k > length + 1","hints":["Handle k=1","Traverse to kth","Insert before"],"relatedProblems":[83,89,90,92]},{"id":92,"title":"Insert before given node in Doubly Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"easy","status":"attempted","timeSpent":41,"attempts":1,"lastTouched":16,"frequency":65,"tags":["linked-list","doubly","insertion"],"companies":[],"concept":"Insert new node before given node (not head) in doubly linked list","explanation":"Given node to insert before (not head). Insert new node between node's prev and node. Update four pointers.","complexity":{"time":"O(1)","space":"O(1)"},"code":{"javascript":"function insertBeforeNode(node, val) {\n  if (!node || !node.prev) return;\n  const newNode = new DoublyListNode(val);\n  newNode.prev = node.prev;\n  newNode.next = node;\n  node.prev.next = newNode;\n  node.prev = newNode;\n}","python":"def insertBeforeNode(node, val):\n    if not node or not node.prev:\n        return\n    new_node = DoublyListNode(val)\n    new_node.prev = node.prev\n    new_node.next = node\n    node.prev.next = new_node\n    node.prev = new_node","java":"public void insertBeforeNode(DoublyListNode node, int val) {\n  if (node == null || node.prev == null) return;\n  DoublyListNode newNode = new DoublyListNode(val);\n  newNode.prev = node.prev;\n  newNode.next = node;\n  node.prev.next = newNode;\n  node.prev = newNode;\n}","cpp":"void insertBeforeNode(DoublyListNode* node, int val) {\n  if (!node || !node->prev) return;\n  DoublyListNode* newNode = new DoublyListNode(val);\n  newNode->prev = node->prev;\n  newNode->next = node;\n  node->prev->next = newNode;\n  node->prev = newNode;\n}"},"challenge":"Given only node reference","hints":["Not given head","Update four pointers","O(1) operation"],"relatedProblems":[83,89,90,91]},{"id":93,"title":"Delete all occurrences of a key in DLL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"medium","status":"stuck","timeSpent":36,"attempts":1,"lastTouched":28,"frequency":75,"tags":["linked-list","doubly","deletion"],"companies":["Amazon","Microsoft"],"concept":"Delete all nodes with given value in doubly linked list","explanation":"Traverse list. When node with key found, update adjacent pointers to bypass it. Continue until all occurrences removed.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function deleteAllOccurrences(head, key) {\n  let current = head;\n  while (current) {\n    if (current.val === key) {\n      if (current.prev) current.prev.next = current.next;\n      if (current.next) current.next.prev = current.prev;\n      if (current === head) head = current.next;\n    }\n    current = current.next;\n  }\n  return head;\n}","python":"def deleteAllOccurrences(head, key):\n    current = head\n    while current:\n        if current.val == key:\n            if current.prev:\n                current.prev.next = current.next\n            if current.next:\n                current.next.prev = current.prev\n            if current == head:\n                head = current.next\n        current = current.next\n    return head","java":"public DoublyListNode deleteAllOccurrences(DoublyListNode head, int key) {\n  DoublyListNode current = head;\n  while (current != null) {\n    if (current.val == key) {\n      if (current.prev != null) current.prev.next = current.next;\n      if (current.next != null) current.next.prev = current.prev;\n      if (current == head) head = current.next;\n    }\n    current = current.next;\n  }\n  return head;\n}","cpp":"DoublyListNode* deleteAllOccurrences(DoublyListNode* head, int key) {\n  DoublyListNode* current = head;\n  while (current) {\n    if (current->val == key) {\n      if (current->prev) current->prev->next = current->next;\n      if (current->next) current->next->prev = current->prev;\n      if (current == head) head = current->next;\n    }\n    current = current->next;\n  }\n  return head;\n}"},"challenge":"Handle consecutive occurrences","hints":["Traverse entire list","Update pointers","Update head if needed"],"relatedProblems":[82,85,86,87,88]},{"id":94,"title":"Remove duplicated from sorted DLL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Doubly","difficulty":"medium","status":"attempted","timeSpent":35,"attempts":4,"lastTouched":17,"frequency":80,"tags":["linked-list","doubly","duplicates"],"companies":["Amazon","Microsoft"],"concept":"Remove duplicate nodes from sorted doubly linked list","explanation":"Traverse sorted list. When current value equals next value, delete next node. Continue until no duplicates.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function removeDuplicates(head) {\n  let current = head;\n  while (current && current.next) {\n    if (current.val === current.next.val) {\n      const duplicate = current.next;\n      current.next = duplicate.next;\n      if (duplicate.next) duplicate.next.prev = current;\n    } else {\n      current = current.next;\n    }\n  }\n  return head;\n}","python":"def removeDuplicates(head):\n    current = head\n    while current and current.next:\n        if current.val == current.next.val:\n            duplicate = current.next\n            current.next = duplicate.next\n            if duplicate.next:\n                duplicate.next.prev = current\n        else:\n            current = current.next\n    return head","java":"public DoublyListNode removeDuplicates(DoublyListNode head) {\n  DoublyListNode current = head;\n  while (current != null && current.next != null) {\n    if (current.val == current.next.val) {\n      DoublyListNode duplicate = current.next;\n      current.next = duplicate.next;\n      if (duplicate.next != null) duplicate.next.prev = current;\n    } else {\n      current = current.next;\n    }\n  }\n  return head;\n}","cpp":"DoublyListNode* removeDuplicates(DoublyListNode* head) {\n  DoublyListNode* current = head;\n  while (current && current->next) {\n    if (current->val == current->next->val) {\n      DoublyListNode* duplicate = current->next;\n      current->next = duplicate->next;\n      if (duplicate->next) duplicate->next->prev = current;\n    } else {\n      current = current->next;\n    }\n  }\n  return head;\n}"},"challenge":"Remove all duplicates not just consecutive","hints":["List is sorted","Compare with next","Delete duplicates"],"relatedProblems":[93]},{"id":95,"title":"Add two numbers in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Math","difficulty":"medium","status":"solved","timeSpent":12,"attempts":2,"lastTouched":11,"frequency":95,"tags":["linked-list","math","addition"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Add two numbers represented by linked lists","explanation":"Traverse both lists. Add corresponding digits with carry. Create new node for sum digit. Continue until both lists exhausted and no carry.","complexity":{"time":"O(max(m,n))","space":"O(max(m,n))"},"code":{"javascript":"function addTwoNumbers(l1, l2) {\n  const dummy = new ListNode(0);\n  let current = dummy;\n  let carry = 0;\n  while (l1 || l2 || carry) {\n    const sum = (l1 ? l1.val : 0) + (l2 ? l2.val : 0) + carry;\n    carry = Math.floor(sum / 10);\n    current.next = new ListNode(sum % 10);\n    current = current.next;\n    if (l1) l1 = l1.next;\n    if (l2) l2 = l2.next;\n  }\n  return dummy.next;\n}","python":"def addTwoNumbers(l1, l2):\n    dummy = ListNode(0)\n    current = dummy\n    carry = 0\n    while l1 or l2 or carry:\n        sum_val = (l1.val if l1 else 0) + (l2.val if l2 else 0) + carry\n        carry = sum_val // 10\n        current.next = ListNode(sum_val % 10)\n        current = current.next\n        if l1:\n            l1 = l1.next\n        if l2:\n            l2 = l2.next\n    return dummy.next","java":"public ListNode addTwoNumbers(ListNode l1, ListNode l2) {\n  ListNode dummy = new ListNode(0);\n  ListNode current = dummy;\n  int carry = 0;\n  while (l1 != null || l2 != null || carry != 0) {\n    int sum = (l1 != null ? l1.val : 0) + (l2 != null ? l2.val : 0) + carry;\n    carry = sum / 10;\n    current.next = new ListNode(sum % 10);\n    current = current.next;\n    if (l1 != null) l1 = l1.next;\n    if (l2 != null) l2 = l2.next;\n  }\n  return dummy.next;\n}","cpp":"ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {\n  ListNode* dummy = new ListNode(0);\n  ListNode* current = dummy;\n  int carry = 0;\n  while (l1 || l2 || carry) {\n    int sum = (l1 ? l1->val : 0) + (l2 ? l2->val : 0) + carry;\n    carry = sum / 10;\n    current->next = new ListNode(sum % 10);\n    current = current->next;\n    if (l1) l1 = l1->next;\n    if (l2) l2 = l2->next;\n  }\n  return dummy->next;\n}"},"challenge":"Handle different length lists","hints":["Use dummy head","Track carry","Continue until both null and no carry"],"relatedProblems":[100]},{"id":96,"title":"Segregate odd and even nodes in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Reordering","difficulty":"medium","status":"stuck","timeSpent":65,"attempts":4,"lastTouched":3,"frequency":80,"tags":["linked-list","reordering","odd-even"],"companies":["Amazon","Microsoft"],"concept":"Group all odd nodes together followed by even nodes","explanation":"Maintain two lists: odd and even. Traverse original list. Append odd valued nodes to odd list, even to even list. Connect odd tail to even head.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function oddEvenList(head) {\n  if (!head) return null;\n  const oddDummy = new ListNode(0);\n  const evenDummy = new ListNode(0);\n  let odd = oddDummy, even = evenDummy;\n  let current = head;\n  let isOdd = true;\n  while (current) {\n    if (isOdd) {\n      odd.next = current;\n      odd = odd.next;\n    } else {\n      even.next = current;\n      even = even.next;\n    }\n    isOdd = !isOdd;\n    current = current.next;\n  }\n  even.next = null;\n  odd.next = evenDummy.next;\n  return oddDummy.next;\n}","python":"def oddEvenList(head):\n    if not head:\n        return None\n    odd_dummy = ListNode(0)\n    even_dummy = ListNode(0)\n    odd, even = odd_dummy, even_dummy\n    current = head\n    is_odd = True\n    while current:\n        if is_odd:\n            odd.next = current\n            odd = odd.next\n        else:\n            even.next = current\n            even = even.next\n        is_odd = not is_odd\n        current = current.next\n    even.next = None\n    odd.next = even_dummy.next\n    return odd_dummy.next","java":"public ListNode oddEvenList(ListNode head) {\n  if (head == null) return null;\n  ListNode oddDummy = new ListNode(0);\n  ListNode evenDummy = new ListNode(0);\n  ListNode odd = oddDummy, even = evenDummy;\n  ListNode current = head;\n  boolean isOdd = true;\n  while (current != null) {\n    if (isOdd) {\n      odd.next = current;\n      odd = odd.next;\n    } else {\n      even.next = current;\n      even = even.next;\n    }\n    isOdd = !isOdd;\n    current = current.next;\n  }\n  even.next = null;\n  odd.next = evenDummy.next;\n  return oddDummy.next;\n}","cpp":"ListNode* oddEvenList(ListNode* head) {\n  if (!head) return nullptr;\n  ListNode* oddDummy = new ListNode(0);\n  ListNode* evenDummy = new ListNode(0);\n  ListNode* odd = oddDummy, *even = evenDummy;\n  ListNode* current = head;\n  bool isOdd = true;\n  while (current) {\n    if (isOdd) {\n      odd->next = current;\n      odd = odd->next;\n    } else {\n      even->next = current;\n      even = even->next;\n    }\n    isOdd = !isOdd;\n    current = current->next;\n  }\n  even->next = nullptr;\n  odd->next = evenDummy->next;\n  return oddDummy->next;\n}"},"challenge":"Maintain relative order","hints":["Two separate lists","Connect at end","O(1) space"],"relatedProblems":[]},{"id":97,"title":"Sort a Linked List of 0's 1's and 2's","category":"linked-list","categoryName":"Linked List","topic":"Linked List Sorting","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":85,"tags":["linked-list","sorting","dutch-national-flag"],"companies":["Amazon","Microsoft","Adobe"],"concept":"Sort linked list containing only 0, 1, 2","explanation":"Count occurrences of 0, 1, 2. Overwrite list values in order. Or use Dutch National Flag approach with three pointers.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function sortList(head) {\n  let count0 = 0, count1 = 0, count2 = 0;\n  let current = head;\n  while (current) {\n    if (current.val === 0) count0++;\n    else if (current.val === 1) count1++;\n    else count2++;\n    current = current.next;\n  }\n  current = head;\n  while (count0-- > 0) { current.val = 0; current = current.next; }\n  while (count1-- > 0) { current.val = 1; current = current.next; }\n  while (count2-- > 0) { current.val = 2; current = current.next; }\n  return head;\n}","python":"def sortList(head):\n    count = [0, 0, 0]\n    current = head\n    while current:\n        count[current.val] += 1\n        current = current.next\n    current = head\n    for i in range(3):\n        while count[i] > 0:\n            current.val = i\n            count[i] -= 1\n            current = current.next\n    return head","java":"public ListNode sortList(ListNode head) {\n  int count0 = 0, count1 = 0, count2 = 0;\n  ListNode current = head;\n  while (current != null) {\n    if (current.val == 0) count0++;\n    else if (current.val == 1) count1++;\n    else count2++;\n    current = current.next;\n  }\n  current = head;\n  while (count0-- > 0) { current.val = 0; current = current.next; }\n  while (count1-- > 0) { current.val = 1; current = current.next; }\n  while (count2-- > 0) { current.val = 2; current = current.next; }\n  return head;\n}","cpp":"ListNode* sortList(ListNode* head) {\n  int count[3] = {0, 0, 0};\n  ListNode* current = head;\n  while (current) {\n    count[current->val]++;\n    current = current->next;\n  }\n  current = head;\n  for (int i = 0; i < 3; i++) {\n    while (count[i]-- > 0) {\n      current->val = i;\n      current = current->next;\n    }\n  }\n  return head;\n}"},"challenge":"Single traversal Dutch National Flag","hints":["Count occurrences","Overwrite values","Single pass"],"relatedProblems":[12]},{"id":98,"title":"Remove Nth node from the back of the LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Two Pointers","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":95,"tags":["linked-list","two-pointers","remove-nth"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Remove nth node from end of linked list","explanation":"Use two pointers. Move fast pointer n steps ahead. Then move both until fast reaches end. Slow will be at node to remove.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function removeNthFromEnd(head, n) {\n  const dummy = new ListNode(0);\n  dummy.next = head;\n  let fast = dummy, slow = dummy;\n  for (let i = 0; i <= n; i++) fast = fast.next;\n  while (fast) {\n    fast = fast.next;\n    slow = slow.next;\n  }\n  slow.next = slow.next.next;\n  return dummy.next;\n}","python":"def removeNthFromEnd(head, n):\n    dummy = ListNode(0)\n    dummy.next = head\n    fast = slow = dummy\n    for _ in range(n + 1):\n        fast = fast.next\n    while fast:\n        fast = fast.next\n        slow = slow.next\n    slow.next = slow.next.next\n    return dummy.next","java":"public ListNode removeNthFromEnd(ListNode head, int n) {\n  ListNode dummy = new ListNode(0);\n  dummy.next = head;\n  ListNode fast = dummy, slow = dummy;\n  for (int i = 0; i <= n; i++) fast = fast.next;\n  while (fast != null) {\n    fast = fast.next;\n    slow = slow.next;\n  }\n  slow.next = slow.next.next;\n  return dummy.next;\n}","cpp":"ListNode* removeNthFromEnd(ListNode* head, int n) {\n  ListNode* dummy = new ListNode(0);\n  dummy->next = head;\n  ListNode* fast = dummy, *slow = dummy;\n  for (int i = 0; i <= n; i++) fast = fast->next;\n  while (fast) {\n    fast = fast->next;\n    slow = slow->next;\n  }\n  slow->next = slow->next->next;\n  return dummy->next;\n}"},"challenge":"One pass solution","hints":["Two pointers","Fast pointer n ahead","Remove slow.next"],"relatedProblems":[101]},{"id":99,"title":"Reverse a LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Reversal","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":100,"tags":["linked-list","reversal"],"companies":["Amazon","Microsoft","Facebook","Google","Apple","Adobe"],"concept":"Reverse a singly linked list","explanation":"Iterative: Track prev, current, next. Update current.next to prev. Move all pointers forward. Recursive: Reverse rest and append current to end.","complexity":{"time":"O(n)","space":"O(1) iterative, O(n) recursive"},"code":{"javascript":"function reverseList(head) {\n  let prev = null, current = head;\n  while (current) {\n    const next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}","python":"def reverseList(head):\n    prev = None\n    current = head\n    while current:\n        next_node = current.next\n        current.next = prev\n        prev = current\n        current = next_node\n    return prev","java":"public ListNode reverseList(ListNode head) {\n  ListNode prev = null, current = head;\n  while (current != null) {\n    ListNode next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}","cpp":"ListNode* reverseList(ListNode* head) {\n  ListNode* prev = nullptr, *current = head;\n  while (current) {\n    ListNode* next = current->next;\n    current->next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}"},"challenge":"Recursive solution","hints":["Iterative: three pointers","Recursive: reverse rest","Update head.next"],"relatedProblems":[108]},{"id":100,"title":"Add one to a number represented by Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Math","difficulty":"medium","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":80,"tags":["linked-list","math","addition"],"companies":["Amazon","Microsoft"],"concept":"Add 1 to number represented as linked list (MSB at head)","explanation":"Reverse list. Add 1 with carry. Reverse back. Or use recursion to add from end. Handle carry at head.","complexity":{"time":"O(n)","space":"O(1) or O(n) recursive"},"code":{"javascript":"function addOne(head) {\n  head = reverse(head);\n  let current = head;\n  let carry = 1;\n  while (current && carry) {\n    const sum = current.val + carry;\n    current.val = sum % 10;\n    carry = Math.floor(sum / 10);\n    if (!current.next && carry) {\n      current.next = new ListNode(carry);\n      carry = 0;\n    }\n    current = current.next;\n  }\n  return reverse(head);\n}\n\nfunction reverse(head) {\n  let prev = null, current = head;\n  while (current) {\n    const next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}","python":"def addOne(head):\n    def reverse(node):\n        prev = None\n        current = node\n        while current:\n            next_node = current.next\n            current.next = prev\n            prev = current\n            current = next_node\n        return prev\n    \n    head = reverse(head)\n    current = head\n    carry = 1\n    while current and carry:\n        sum_val = current.val + carry\n        current.val = sum_val % 10\n        carry = sum_val // 10\n        if not current.next and carry:\n            current.next = ListNode(carry)\n            carry = 0\n        current = current.next\n    return reverse(head)","java":"public ListNode addOne(ListNode head) {\n  head = reverse(head);\n  ListNode current = head;\n  int carry = 1;\n  while (current != null && carry > 0) {\n    int sum = current.val + carry;\n    current.val = sum % 10;\n    carry = sum / 10;\n    if (current.next == null && carry > 0) {\n      current.next = new ListNode(carry);\n      carry = 0;\n    }\n    current = current.next;\n  }\n  return reverse(head);\n}\n\nListNode reverse(ListNode head) {\n  ListNode prev = null, current = head;\n  while (current != null) {\n    ListNode next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}","cpp":"ListNode* addOne(ListNode* head) {\n  head = reverse(head);\n  ListNode* current = head;\n  int carry = 1;\n  while (current && carry) {\n    int sum = current->val + carry;\n    current->val = sum % 10;\n    carry = sum / 10;\n    if (!current->next && carry) {\n      current->next = new ListNode(carry);\n      carry = 0;\n    }\n    current = current->next;\n  }\n  return reverse(head);\n}\n\nListNode* reverse(ListNode* head) {\n  ListNode* prev = nullptr, *current = head;\n  while (current) {\n    ListNode* next = current->next;\n    current->next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}"},"challenge":"Recursive approach without reversal","hints":["Reverse and add","Handle carry","Reverse back"],"relatedProblems":[95]},{"id":101,"title":"Find Middle of Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Two Pointers","difficulty":"easy","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":95,"tags":["linked-list","two-pointers","middle"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Find middle node of linked list","explanation":"Use slow and fast pointers. Fast moves 2 steps, slow moves 1 step. When fast reaches end, slow is at middle.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function middleNode(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  return slow;\n}","python":"def middleNode(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n    return slow","java":"public ListNode middleNode(ListNode head) {\n  ListNode slow = head, fast = head;\n  while (fast != null && fast.next != null) {\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  return slow;\n}","cpp":"ListNode* middleNode(ListNode* head) {\n  ListNode* slow = head, *fast = head;\n  while (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n  }\n  return slow;\n}"},"challenge":"Return second middle for even length","hints":["Slow and fast pointers","Fast moves 2x","Slow is middle"],"relatedProblems":[98,102]},{"id":102,"title":"Delete the middle node in LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Two Pointers","difficulty":"medium","status":"attempted","timeSpent":62,"attempts":4,"lastTouched":7,"frequency":85,"tags":["linked-list","two-pointers","delete-middle"],"companies":["Amazon","Microsoft"],"concept":"Delete middle node of linked list","explanation":"Use slow and fast pointers. Keep track of previous of slow. When fast reaches end, delete slow by updating prev.next.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function deleteMiddle(head) {\n  if (!head || !head.next) return null;\n  let slow = head, fast = head;\n  let prev = null;\n  while (fast && fast.next) {\n    prev = slow;\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  prev.next = slow.next;\n  return head;\n}","python":"def deleteMiddle(head):\n    if not head or not head.next:\n        return None\n    slow = fast = head\n    prev = None\n    while fast and fast.next:\n        prev = slow\n        slow = slow.next\n        fast = fast.next.next\n    prev.next = slow.next\n    return head","java":"public ListNode deleteMiddle(ListNode head) {\n  if (head == null || head.next == null) return null;\n  ListNode slow = head, fast = head;\n  ListNode prev = null;\n  while (fast != null && fast.next != null) {\n    prev = slow;\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  prev.next = slow.next;\n  return head;\n}","cpp":"ListNode* deleteMiddle(ListNode* head) {\n  if (!head || !head->next) return nullptr;\n  ListNode* slow = head, *fast = head;\n  ListNode* prev = nullptr;\n  while (fast && fast->next) {\n    prev = slow;\n    slow = slow->next;\n    fast = fast->next->next;\n  }\n  prev->next = slow->next;\n  return head;\n}"},"challenge":"Handle single node","hints":["Track previous","Two pointers","Delete slow"],"relatedProblems":[98,101]},{"id":103,"title":"Check if LL is palindrome or not","category":"linked-list","categoryName":"Linked List","topic":"Linked List Two Pointers","difficulty":"medium","status":"revisit","timeSpent":11,"attempts":3,"lastTouched":9,"frequency":95,"tags":["linked-list","palindrome","two-pointers"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Check if linked list is palindrome","explanation":"Find middle. Reverse second half. Compare both halves. Restore list (optional).","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function isPalindrome(head) {\n  if (!head || !head.next) return true;\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  let second = reverse(slow);\n  let first = head;\n  while (second) {\n    if (first.val !== second.val) return false;\n    first = first.next;\n    second = second.next;\n  }\n  return true;\n}\n\nfunction reverse(head) {\n  let prev = null, current = head;\n  while (current) {\n    const next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}","python":"def isPalindrome(head):\n    if not head or not head.next:\n        return True\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n    \n    def reverse(node):\n        prev = None\n        while node:\n            next_node = node.next\n            node.next = prev\n            prev = node\n            node = next_node\n        return prev\n    \n    second = reverse(slow)\n    first = head\n    while second:\n        if first.val != second.val:\n            return False\n        first = first.next\n        second = second.next\n    return True","java":"public boolean isPalindrome(ListNode head) {\n  if (head == null || head.next == null) return true;\n  ListNode slow = head, fast = head;\n  while (fast != null && fast.next != null) {\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  ListNode second = reverse(slow);\n  ListNode first = head;\n  while (second != null) {\n    if (first.val != second.val) return false;\n    first = first.next;\n    second = second.next;\n  }\n  return true;\n}\n\nListNode reverse(ListNode head) {\n  ListNode prev = null, current = head;\n  while (current != null) {\n    ListNode next = current.next;\n    current.next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}","cpp":"bool isPalindrome(ListNode* head) {\n  if (!head || !head->next) return true;\n  ListNode* slow = head, *fast = head;\n  while (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n  }\n  ListNode* second = reverse(slow);\n  ListNode* first = head;\n  while (second) {\n    if (first->val != second->val) return false;\n    first = first->next;\n    second = second->next;\n  }\n  return true;\n}\n\nListNode* reverse(ListNode* head) {\n  ListNode* prev = nullptr, *current = head;\n  while (current) {\n    ListNode* next = current->next;\n    current->next = prev;\n    prev = current;\n    current = next;\n  }\n  return prev;\n}"},"challenge":"O(1) space without modifying list","hints":["Find middle","Reverse second half","Compare"],"relatedProblems":[101]},{"id":104,"title":"Find the intersection point of Y LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Two Pointers","difficulty":"medium","status":"solved","timeSpent":64,"attempts":3,"lastTouched":2,"frequency":95,"tags":["linked-list","intersection","two-pointers"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Find node where two linked lists intersect","explanation":"Calculate lengths. Move longer list's pointer by difference. Move both until they meet. Or use two pointers that switch lists.","complexity":{"time":"O(m + n)","space":"O(1)"},"code":{"javascript":"function getIntersectionNode(headA, headB) {\n  let lenA = 0, lenB = 0;\n  let currA = headA, currB = headB;\n  while (currA) { lenA++; currA = currA.next; }\n  while (currB) { lenB++; currB = currB.next; }\n  currA = headA; currB = headB;\n  while (lenA > lenB) { currA = currA.next; lenA--; }\n  while (lenB > lenA) { currB = currB.next; lenB--; }\n  while (currA !== currB) {\n    currA = currA.next;\n    currB = currB.next;\n  }\n  return currA;\n}","python":"def getIntersectionNode(headA, headB):\n    lenA = lenB = 0\n    currA, currB = headA, headB\n    while currA:\n        lenA += 1\n        currA = currA.next\n    while currB:\n        lenB += 1\n        currB = currB.next\n    currA, currB = headA, headB\n    while lenA > lenB:\n        currA = currA.next\n        lenA -= 1\n    while lenB > lenA:\n        currB = currB.next\n        lenB -= 1\n    while currA != currB:\n        currA = currA.next\n        currB = currB.next\n    return currA","java":"public ListNode getIntersectionNode(ListNode headA, ListNode headB) {\n  int lenA = 0, lenB = 0;\n  ListNode currA = headA, currB = headB;\n  while (currA != null) { lenA++; currA = currA.next; }\n  while (currB != null) { lenB++; currB = currB.next; }\n  currA = headA; currB = headB;\n  while (lenA > lenB) { currA = currA.next; lenA--; }\n  while (lenB > lenA) { currB = currB.next; lenB--; }\n  while (currA != currB) {\n    currA = currA.next;\n    currB = currB.next;\n  }\n  return currA;\n}","cpp":"ListNode* getIntersectionNode(ListNode* headA, ListNode* headB) {\n  int lenA = 0, lenB = 0;\n  ListNode* currA = headA, *currB = headB;\n  while (currA) { lenA++; currA = currA->next; }\n  while (currB) { lenB++; currB = currB->next; }\n  currA = headA; currB = headB;\n  while (lenA > lenB) { currA = currA->next; lenA--; }\n  while (lenB > lenA) { currB = currB->next; lenB--; }\n  while (currA != currB) {\n    currA = currA->next;\n    currB = currB->next;\n  }\n  return currA;\n}"},"challenge":"Two pointer switching approach","hints":["Calculate lengths","Align starting points","Move together"],"relatedProblems":[]},{"id":105,"title":"Detect a loop in Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Floyd's Cycle","difficulty":"medium","status":"stuck","timeSpent":49,"attempts":4,"lastTouched":5,"frequency":100,"tags":["linked-list","cycle","floyd"],"companies":["Amazon","Microsoft","Facebook","Google","Apple","Adobe"],"concept":"Detect if linked list has a cycle","explanation":"Use Floyd's Cycle Detection. Slow moves 1 step, fast moves 2 steps. If they meet, cycle exists.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function hasCycle(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) return true;\n  }\n  return false;\n}","python":"def hasCycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            return True\n    return False","java":"public boolean hasCycle(ListNode head) {\n  ListNode slow = head, fast = head;\n  while (fast != null && fast.next != null) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow == fast) return true;\n  }\n  return false;\n}","cpp":"bool hasCycle(ListNode* head) {\n  ListNode* slow = head, *fast = head;\n  while (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n    if (slow == fast) return true;\n  }\n  return false;\n}"},"challenge":"Find start of cycle","hints":["Floyd's algorithm","Slow and fast pointers","Check if they meet"],"relatedProblems":[106,107]},{"id":106,"title":"Find the starting point in LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Floyd's Cycle","difficulty":"medium","status":"solved","timeSpent":27,"attempts":1,"lastTouched":6,"frequency":95,"tags":["linked-list","cycle","floyd","start-point"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Find node where cycle begins in linked list","explanation":"Detect cycle using Floyd's. When slow and fast meet, move one pointer to head. Move both at same speed. Meeting point is start of cycle.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function detectCycle(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) break;\n  }\n  if (!fast || !fast.next) return null;\n  slow = head;\n  while (slow !== fast) {\n    slow = slow.next;\n    fast = fast.next;\n  }\n  return slow;\n}","python":"def detectCycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            break\n    else:\n        return None\n    slow = head\n    while slow != fast:\n        slow = slow.next\n        fast = fast.next\n    return slow","java":"public ListNode detectCycle(ListNode head) {\n  ListNode slow = head, fast = head;\n  while (fast != null && fast.next != null) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow == fast) break;\n  }\n  if (fast == null || fast.next == null) return null;\n  slow = head;\n  while (slow != fast) {\n    slow = slow.next;\n    fast = fast.next;\n  }\n  return slow;\n}","cpp":"ListNode* detectCycle(ListNode* head) {\n  ListNode* slow = head, *fast = head;\n  while (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n    if (slow == fast) break;\n  }\n  if (!fast || !fast->next) return nullptr;\n  slow = head;\n  while (slow != fast) {\n    slow = slow->next;\n    fast = fast->next;\n  }\n  return slow;\n}"},"challenge":"Prove why this works mathematically","hints":["Detect cycle first","Reset one to head","Move both at same speed"],"relatedProblems":[105,107]},{"id":107,"title":"Length of loop in LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Floyd's Cycle","difficulty":"medium","status":"attempted","timeSpent":39,"attempts":2,"lastTouched":26,"frequency":80,"tags":["linked-list","cycle","length"],"companies":["Amazon","Microsoft"],"concept":"Find length of cycle in linked list","explanation":"Detect cycle using Floyd's. Once meeting point found, keep one pointer fixed and move other until they meet again. Count steps.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function cycleLength(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) {\n      let length = 1;\n      fast = fast.next;\n      while (slow !== fast) {\n        fast = fast.next;\n        length++;\n      }\n      return length;\n    }\n  }\n  return 0;\n}","python":"def cycleLength(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            length = 1\n            fast = fast.next\n            while slow != fast:\n                fast = fast.next\n                length += 1\n            return length\n    return 0","java":"public int cycleLength(ListNode head) {\n  ListNode slow = head, fast = head;\n  while (fast != null && fast.next != null) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow == fast) {\n      int length = 1;\n      fast = fast.next;\n      while (slow != fast) {\n        fast = fast.next;\n        length++;\n      }\n      return length;\n    }\n  }\n  return 0;\n}","cpp":"int cycleLength(ListNode* head) {\n  ListNode* slow = head, *fast = head;\n  while (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n    if (slow == fast) {\n      int length = 1;\n      fast = fast->next;\n      while (slow != fast) {\n        fast = fast->next;\n        length++;\n      }\n      return length;\n    }\n  }\n  return 0;\n}"},"challenge":"Count nodes in cycle","hints":["Find meeting point","Keep one fixed","Count until meet again"],"relatedProblems":[105,106]},{"id":108,"title":"Reverse LL in group of given size K","category":"linked-list","categoryName":"Linked List","topic":"Linked List Reversal","difficulty":"hard","status":"solved","timeSpent":28,"attempts":1,"lastTouched":2,"frequency":95,"tags":["linked-list","reversal","k-group"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Reverse nodes in groups of k","explanation":"Reverse first k nodes. Connect with recursively reversed rest. Handle cases where remaining nodes < k.","complexity":{"time":"O(n)","space":"O(n/k) recursive stack"},"code":{"javascript":"function reverseKGroup(head, k) {\n  let curr = head;\n  let count = 0;\n  while (curr && count < k) {\n    curr = curr.next;\n    count++;\n  }\n  if (count === k) {\n    curr = reverseKGroup(curr, k);\n    while (count-- > 0) {\n      const next = head.next;\n      head.next = curr;\n      curr = head;\n      head = next;\n    }\n    head = curr;\n  }\n  return head;\n}","python":"def reverseKGroup(head, k):\n    curr = head\n    count = 0\n    while curr and count < k:\n        curr = curr.next\n        count += 1\n    if count == k:\n        curr = reverseKGroup(curr, k)\n        while count > 0:\n            next_node = head.next\n            head.next = curr\n            curr = head\n            head = next_node\n            count -= 1\n        head = curr\n    return head","java":"public ListNode reverseKGroup(ListNode head, int k) {\n  ListNode curr = head;\n  int count = 0;\n  while (curr != null && count < k) {\n    curr = curr.next;\n    count++;\n  }\n  if (count == k) {\n    curr = reverseKGroup(curr, k);\n    while (count-- > 0) {\n      ListNode next = head.next;\n      head.next = curr;\n      curr = head;\n      head = next;\n    }\n    head = curr;\n  }\n  return head;\n}","cpp":"ListNode* reverseKGroup(ListNode* head, int k) {\n  ListNode* curr = head;\n  int count = 0;\n  while (curr && count < k) {\n    curr = curr->next;\n    count++;\n  }\n  if (count == k) {\n    curr = reverseKGroup(curr, k);\n    while (count-- > 0) {\n      ListNode* next = head->next;\n      head->next = curr;\n      curr = head;\n      head = next;\n    }\n    head = curr;\n  }\n  return head;\n}"},"challenge":"Iterative solution","hints":["Reverse k nodes","Connect with rest","Recursive approach"],"relatedProblems":[99]},{"id":109,"title":"Rotate a Linked List","category":"linked-list","categoryName":"Linked List","topic":"Linked List Reordering","difficulty":"medium","status":"solved","timeSpent":27,"attempts":1,"lastTouched":8,"frequency":90,"tags":["linked-list","rotation"],"companies":["Amazon","Microsoft","Facebook","Google"],"concept":"Rotate linked list to the right by k places","explanation":"Find length. Connect tail to head (make circular). Find new tail at position length - k % length. Break and return new head.","complexity":{"time":"O(n)","space":"O(1)"},"code":{"javascript":"function rotateRight(head, k) {\n  if (!head || !head.next || k === 0) return head;\n  let length = 1;\n  let tail = head;\n  while (tail.next) {\n    length++;\n    tail = tail.next;\n  }\n  tail.next = head;\n  k = k % length;\n  const stepsToNewHead = length - k;\n  let newTail = tail;\n  while (stepsToNewHead-- > 0) {\n    newTail = newTail.next;\n  }\n  const newHead = newTail.next;\n  newTail.next = null;\n  return newHead;\n}","python":"def rotateRight(head, k):\n    if not head or not head.next or k == 0:\n        return head\n    length = 1\n    tail = head\n    while tail.next:\n        length += 1\n        tail = tail.next\n    tail.next = head\n    k = k % length\n    steps_to_new_head = length - k\n    new_tail = tail\n    while steps_to_new_head > 0:\n        new_tail = new_tail.next\n        steps_to_new_head -= 1\n    new_head = new_tail.next\n    new_tail.next = None\n    return new_head","java":"public ListNode rotateRight(ListNode head, int k) {\n  if (head == null || head.next == null || k == 0) return head;\n  int length = 1;\n  ListNode tail = head;\n  while (tail.next != null) {\n    length++;\n    tail = tail.next;\n  }\n  tail.next = head;\n  k = k % length;\n  int stepsToNewHead = length - k;\n  ListNode newTail = tail;\n  while (stepsToNewHead-- > 0) {\n    newTail = newTail.next;\n  }\n  ListNode newHead = newTail.next;\n  newTail.next = null;\n  return newHead;\n}","cpp":"ListNode* rotateRight(ListNode* head, int k) {\n  if (!head || !head->next || k == 0) return head;\n  int length = 1;\n  ListNode* tail = head;\n  while (tail->next) {\n    length++;\n    tail = tail->next;\n  }\n  tail->next = head;\n  k = k % length;\n  int stepsToNewHead = length - k;\n  ListNode* newTail = tail;\n  while (stepsToNewHead-- > 0) {\n    newTail = newTail->next;\n  }\n  ListNode* newHead = newTail->next;\n  newTail->next = nullptr;\n  return newHead;\n}"},"challenge":"Handle k > length","hints":["Make list circular","Find new tail","Break and return new head"],"relatedProblems":[]},{"id":110,"title":"Merge two Sorted Lists","category":"linked-list","categoryName":"Linked List","topic":"Linked List Merge","difficulty":"easy","status":"solved","timeSpent":32,"attempts":2,"lastTouched":23,"frequency":100,"tags":["linked-list","merge","sorted"],"companies":["Amazon","Microsoft","Facebook","Google","Apple","Adobe"],"concept":"Merge two sorted linked lists into one sorted list","explanation":"Compare heads of both lists. Take smaller and advance its pointer. Continue until one list empty. Append remaining nodes.","complexity":{"time":"O(m + n)","space":"O(1)"},"code":{"javascript":"function mergeTwoLists(l1, l2) {\n  const dummy = new ListNode(0);\n  let current = dummy;\n  while (l1 && l2) {\n    if (l1.val < l2.val) {\n      current.next = l1;\n      l1 = l1.next;\n    } else {\n      current.next = l2;\n      l2 = l2.next;\n    }\n    current = current.next;\n  }\n  current.next = l1 || l2;\n  return dummy.next;\n}","python":"def mergeTwoLists(l1, l2):\n    dummy = ListNode(0)\n    current = dummy\n    while l1 and l2:\n        if l1.val < l2.val:\n            current.next = l1\n            l1 = l1.next\n        else:\n            current.next = l2\n            l2 = l2.next\n        current = current.next\n    current.next = l1 or l2\n    return dummy.next","java":"public ListNode mergeTwoLists(ListNode l1, ListNode l2) {\n  ListNode dummy = new ListNode(0);\n  ListNode current = dummy;\n  while (l1 != null && l2 != null) {\n    if (l1.val < l2.val) {\n      current.next = l1;\n      l1 = l1.next;\n    } else {\n      current.next = l2;\n      l2 = l2.next;\n    }\n    current = current.next;\n  }\n  current.next = l1 != null ? l1 : l2;\n  return dummy.next;\n}","cpp":"ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {\n  ListNode* dummy = new ListNode(0);\n  ListNode* current = dummy;\n  while (l1 && l2) {\n    if (l1->val < l2->val) {\n      current->next = l1;\n      l1 = l1->next;\n    } else {\n      current->next = l2;\n      l2 = l2->next;\n    }\n    current = current->next;\n  }\n  current->next = l1 ? l1 : l2;\n  return dummy->next;\n}"},"challenge":"Recursive solution","hints":["Compare heads","Take smaller","Connect remaining"],"relatedProblems":[111]},{"id":111,"title":"Flattening of LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Merge","difficulty":"medium","status":"solved","timeSpent":38,"attempts":4,"lastTouched":18,"frequency":85,"tags":["linked-list","flatten","multi-level"],"companies":["Amazon","Microsoft","Facebook"],"concept":"Flatten multi-level linked list into single level","explanation":"Use merge approach. Recursively flatten child lists. Merge with next list. Or use stack/queue to process nodes level by level.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function flatten(head) {\n  if (!head) return null;\n  const dummy = new ListNode(0);\n  dummy.next = head;\n  let current = head;\n  while (current) {\n    if (current.child) {\n      const next = current.next;\n      const child = flatten(current.child);\n      current.next = child;\n      child.prev = current;\n      current.child = null;\n      while (current.next) current = current.next;\n      current.next = next;\n      if (next) next.prev = current;\n    }\n    current = current.next;\n  }\n  return dummy.next;\n}","python":"def flatten(head):\n    if not head:\n        return None\n    dummy = ListNode(0)\n    dummy.next = head\n    current = head\n    while current:\n        if current.child:\n            next_node = current.next\n            child = flatten(current.child)\n            current.next = child\n            child.prev = current\n            current.child = None\n            while current.next:\n                current = current.next\n            current.next = next_node\n            if next_node:\n                next_node.prev = current\n        current = current.next\n    return dummy.next","java":"public ListNode flatten(ListNode head) {\n  if (head == null) return null;\n  ListNode dummy = new ListNode(0);\n  dummy.next = head;\n  ListNode current = head;\n  while (current != null) {\n    if (current.child != null) {\n      ListNode next = current.next;\n      ListNode child = flatten(current.child);\n      current.next = child;\n      child.prev = current;\n      current.child = null;\n      while (current.next != null) current = current.next;\n      current.next = next;\n      if (next != null) next.prev = current;\n    }\n    current = current.next;\n  }\n  return dummy.next;\n}","cpp":"ListNode* flatten(ListNode* head) {\n  if (!head) return nullptr;\n  ListNode* dummy = new ListNode(0);\n  dummy->next = head;\n  ListNode* current = head;\n  while (current) {\n    if (current->child) {\n      ListNode* next = current->next;\n      ListNode* child = flatten(current->child);\n      current->next = child;\n      child->prev = current;\n      current->child = nullptr;\n      while (current->next) current = current->next;\n      current->next = next;\n      if (next) next->prev = current;\n    }\n    current = current->next;\n  }\n  return dummy->next;\n}"},"challenge":"Handle multiple levels","hints":["Recursive flatten","Merge child with next","Maintain prev pointers"],"relatedProblems":[110]},{"id":112,"title":"Sort LL","category":"linked-list","categoryName":"Linked List","topic":"Linked List Sorting","difficulty":"medium","status":"solved","timeSpent":51,"attempts":2,"lastTouched":6,"frequency":95,"tags":["linked-list","sorting","merge-sort"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Sort linked list using merge sort","explanation":"Use merge sort. Find middle using slow/fast pointers. Recursively sort both halves. Merge two sorted halves.","complexity":{"time":"O(n log n)","space":"O(log n) recursive stack"},"code":{"javascript":"function sortList(head) {\n  if (!head || !head.next) return head;\n  let slow = head, fast = head;\n  let prev = null;\n  while (fast && fast.next) {\n    prev = slow;\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  prev.next = null;\n  const l1 = sortList(head);\n  const l2 = sortList(slow);\n  return merge(l1, l2);\n}\n\nfunction merge(l1, l2) {\n  const dummy = new ListNode(0);\n  let current = dummy;\n  while (l1 && l2) {\n    if (l1.val < l2.val) {\n      current.next = l1;\n      l1 = l1.next;\n    } else {\n      current.next = l2;\n      l2 = l2.next;\n    }\n    current = current.next;\n  }\n  current.next = l1 || l2;\n  return dummy.next;\n}","python":"def sortList(head):\n    if not head or not head.next:\n        return head\n    slow = fast = head\n    prev = None\n    while fast and fast.next:\n        prev = slow\n        slow = slow.next\n        fast = fast.next.next\n    prev.next = None\n    l1 = sortList(head)\n    l2 = sortList(slow)\n    return merge(l1, l2)\n\ndef merge(l1, l2):\n    dummy = ListNode(0)\n    current = dummy\n    while l1 and l2:\n        if l1.val < l2.val:\n            current.next = l1\n            l1 = l1.next\n        else:\n            current.next = l2\n            l2 = l2.next\n        current = current.next\n    current.next = l1 or l2\n    return dummy.next","java":"public ListNode sortList(ListNode head) {\n  if (head == null || head.next == null) return head;\n  ListNode slow = head, fast = head;\n  ListNode prev = null;\n  while (fast != null && fast.next != null) {\n    prev = slow;\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  prev.next = null;\n  ListNode l1 = sortList(head);\n  ListNode l2 = sortList(slow);\n  return merge(l1, l2);\n}\n\nListNode merge(ListNode l1, ListNode l2) {\n  ListNode dummy = new ListNode(0);\n  ListNode current = dummy;\n  while (l1 != null && l2 != null) {\n    if (l1.val < l2.val) {\n      current.next = l1;\n      l1 = l1.next;\n    } else {\n      current.next = l2;\n      l2 = l2.next;\n    }\n    current = current.next;\n  }\n  current.next = l1 != null ? l1 : l2;\n  return dummy.next;\n}","cpp":"ListNode* sortList(ListNode* head) {\n  if (!head || !head->next) return head;\n  ListNode* slow = head, *fast = head;\n  ListNode* prev = nullptr;\n  while (fast && fast->next) {\n    prev = slow;\n    slow = slow->next;\n    fast = fast->next->next;\n  }\n  prev->next = nullptr;\n  ListNode* l1 = sortList(head);\n  ListNode* l2 = sortList(slow);\n  return merge(l1, l2);\n}\n\nListNode* merge(ListNode* l1, ListNode* l2) {\n  ListNode* dummy = new ListNode(0);\n  ListNode* current = dummy;\n  while (l1 && l2) {\n    if (l1->val < l2->val) {\n      current->next = l1;\n      l1 = l1->next;\n    } else {\n      current->next = l2;\n      l2 = l2->next;\n    }\n    current = current->next;\n  }\n  current->next = l1 ? l1 : l2;\n  return dummy->next;\n}"},"challenge":"O(1) space bottom-up merge sort","hints":["Merge sort","Find middle","Merge sorted halves"],"relatedProblems":[97,110]},{"id":113,"title":"Clone a LL with random and next pointer","category":"linked-list","categoryName":"Linked List","topic":"Linked List Hashing","difficulty":"hard","status":"not_started","timeSpent":0,"attempts":0,"lastTouched":null,"frequency":95,"tags":["linked-list","hashing","deep-copy","random-pointer"],"companies":["Amazon","Microsoft","Facebook","Google","Adobe"],"concept":"Deep copy linked list with random and next pointers","explanation":"Use hash map to store mapping from original to copy. First pass: create copies. Second pass: connect next and random pointers using map.","complexity":{"time":"O(n)","space":"O(n)"},"code":{"javascript":"function copyRandomList(head) {\n  if (!head) return null;\n  const map = new Map();\n  let current = head;\n  while (current) {\n    map.set(current, new ListNode(current.val));\n    current = current.next;\n  }\n  current = head;\n  while (current) {\n    map.get(current).next = map.get(current.next) || null;\n    map.get(current).random = map.get(current.random) || null;\n    current = current.next;\n  }\n  return map.get(head);\n}","python":"def copyRandomList(head):\n    if not head:\n        return None\n    node_map = {}\n    current = head\n    while current:\n        node_map[current] = ListNode(current.val)\n        current = current.next\n    current = head\n    while current:\n        node_map[current].next = node_map.get(current.next)\n        node_map[current].random = node_map.get(current.random)\n        current = current.next\n    return node_map[head]","java":"public ListNode copyRandomList(ListNode head) {\n  if (head == null) return null;\n  Map<ListNode, ListNode> map = new HashMap<>();\n  ListNode current = head;\n  while (current != null) {\n    map.put(current, new ListNode(current.val));\n    current = current.next;\n  }\n  current = head;\n  while (current != null) {\n    map.get(current).next = map.get(current.next);\n    map.get(current).random = map.get(current.random);\n    current = current.next;\n  }\n  return map.get(head);\n}","cpp":"ListNode* copyRandomList(ListNode* head) {\n  if (!head) return nullptr;\n  unordered_map<ListNode*, ListNode*> map;\n  ListNode* current = head;\n  while (current) {\n    map[current] = new ListNode(current->val);\n    current = current->next;\n  }\n  current = head;\n  while (current) {\n    map[current]->next = map[current->next];\n    map[current]->random = map[current->random];\n    current = current->next;\n  }\n  return map[head];\n}"},"challenge":"O(1) space solution without hash map","hints":["Hash map for mapping","Two passes","Connect pointers using map"],"relatedProblems":[]}];
export const ACTIVITY = [1,1,0,0,1,1,0,3,1,1,0,2,2,0,1,0,2,0,1,1,0,0,3,4,0,1,4,0,1,1,2,0,0,4,1,0,1,0,0,2,0,0,0,3,0,3,1,0,1,1,2,2,0,0,1,0,0,1,2,2,4,0,1,1,1,3,1,1,0,0,2,0,0,2,3,4,3,3,2,2,1,0,0,0,0,0,2,2,0,4,0,0,1,1,0,4,1,3,1,1,0,2,3,0,0,0,2,2,2,3,0,3,0,0,0,0,1,3,3,0,1,0,0,1,0,0,0,0,2,0,0,0,2,0,1,0,1,1,0,2,1,2,0,3,2,2,0,0,0,3,0,0,0,0,0,1,1,0,1,3,1,1,0,3,2,1,3,2,1,4,3,1,1,2,2,3,1,4,2,3,1,0,4,1,2,0,2,2,1,1,3,2,0,0,1,4,2,0,2,0,0,1,0,0,4,1,0,0,0,2,0,2,0,3,1,0,3,0,0,1,3,1,0,0,1,0,1,1,0,0,0,2,1,1,3,0,1,0,0,0,0,2,3,0,1,0,0,0,2,1,0,2,3,4,0,4,2,1,0,0,0,2,1,4,0,0,0,0,3,1,2,2,0,1,0,2,4,1,0,0,1,1,0,0,3,2,1,1,1,0,0,0,0,3,2,3,1,3,1,0,3,0,0,0,0,0,0,4,2,1,1,0,1,0,1,0,0,1,1,0,1,2,1,0,0,2,1,0,1,3,3,1,4,3,3,1,1,0,4,1,2,0,0,1,2,4,0,0,0,0,2,3,2,3,3,1,1,2,3,2,1,2,2,2];
export const RECENT = [
  { id: 12, title: "Sort an array of 0's 1's and 2's", categoryName: "Arrays", difficulty: "easy", status: 'solved', time: '2h ago', duration: '34m' },
  { id: 28, title: "Search insert position", categoryName: "Binary Search", difficulty: "easy", status: 'revisit', time: '5h ago', duration: '1h 12m' },
  { id: 5, title: "Pascal's Triangle I", categoryName: "Arrays", difficulty: "easy", status: 'solved', time: 'Yesterday', duration: '42m' },
  { id: 60, title: "Subsets I", categoryName: "Recursion", difficulty: "easy", status: 'solved', time: 'Yesterday', duration: '28m' },
  { id: 90, title: "Insert node before tail in Doubly Linked List", categoryName: "Linked List", difficulty: "easy", status: 'stuck', time: '2d ago', duration: '1h 45m' },
];
